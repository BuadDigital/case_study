import { EmptyIconSearch } from "@platform/ui-kit";

export function Default() {
  return (
    <div style={{ fontSize: 32, color: "var(--ink)", display: "inline-block" }}>
      <EmptyIconSearch />
    </div>
  );
}
