import { KpiCheckIcon } from "@platform/ui-kit";

export function Default() {
  return (
    <div style={{ fontSize: 32, color: "#3f8f5f", display: "inline-block" }}>
      <KpiCheckIcon />
    </div>
  );
}
