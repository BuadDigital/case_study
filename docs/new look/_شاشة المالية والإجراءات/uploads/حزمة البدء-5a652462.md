<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>أوامر العمل (PO) — إجادة للتقييم</title>
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;800&display=swap" rel="stylesheet" />
<style>
  :root{
    --ink:#102B4E; --navy-3:#22406e; --gold:#a4906f; --gold-d:#8c7857; --gold-2:#c8b591; --gold-soft:#f1ece2;
    --bg:#f5f3ee; --surface:#ffffff; --surface-2:#faf8f3; --border:#ece8df; --border-2:#ddd8cc;
    --text:#3a3f4d; --text-2:#73767f; --text-3:#a4a6ad; --row-hover:#faf6ee; --heading:#102B4E;
    --shadow:0 1px 2px rgba(18,40,76,.03),0 6px 16px -18px rgba(18,40,76,.10);
    --cols:116px 108px 82px 82px minmax(150px,1.5fr) minmax(120px,1.15fr) 124px 124px minmax(140px,1.2fr) 46px;
  }
  *{box-sizing:border-box;}
  html,body{margin:0;padding:0;height:100%;}
  body{font-family:'Tajawal','Segoe UI',sans-serif;background:var(--bg);color:var(--text);}
  a{text-decoration:none;}
  button,input,select{font-family:inherit;}
  ::-webkit-scrollbar{width:11px;height:11px;}
  ::-webkit-scrollbar-thumb{background:rgba(120,120,140,.28);border-radius:99px;border:3px solid transparent;background-clip:padding-box;}
  ::-webkit-scrollbar-thumb:hover{background:rgba(120,120,140,.45);background-clip:padding-box;}
  ::-webkit-scrollbar-track{background:transparent;}
  input::placeholder{color:var(--text-3);}

  .app{height:100vh;width:100%;display:grid;grid-template-columns:260px 1fr;overflow:hidden;}

  /* sidebar */
  aside{background:var(--ink);height:100vh;display:flex;flex-direction:column;overflow:hidden;border-inline-end:1px solid var(--border);}
  .brand{padding:20px 18px 18px;border-bottom:1px solid rgba(255,255,255,.08);flex-shrink:0;}
  .brand-card{padding:6px 4px;display:flex;align-items:center;justify-content:center;}
  .brand-card svg{width:155px;height:auto;display:block;}
  nav{flex:1;overflow-y:auto;padding:10px 12px 22px;}
  .nav-group{font-size:11px;font-weight:700;letter-spacing:.04em;color:#6f7b90;padding:12px 12px 0;margin:16px 0 7px;border-top:1px solid rgba(255,255,255,.06);}
  .nav-group:first-of-type{border-top:0;padding-top:0;margin-top:8px;}
  .nav-item{display:flex;align-items:center;gap:11px;padding:10px 12px;border-radius:8px;color:#aeb6c4;font-size:13.5px;font-weight:500;letter-spacing:.01em;cursor:pointer;transition:background .15s,color .15s;}
  .nav-item:hover{background:rgba(255,255,255,.06);color:#fff;}
  .nav-item.active{color:#c8b591;font-weight:700;background:linear-gradient(90deg,color-mix(in srgb,var(--gold) 22%,transparent),color-mix(in srgb,var(--gold) 9%,transparent));position:relative;}
  .nav-item.active::before{content:'';position:absolute;inset-inline-start:0;top:50%;transform:translateY(-50%);width:3px;height:18px;border-radius:99px;background:var(--gold);}
  .nav-badge{margin-inline-start:auto;background:#d9694f;color:#fff;font-size:11px;font-weight:700;min-width:19px;height:19px;border-radius:99px;display:grid;place-items:center;padding:0 5px;}
  .nav-toggle{display:flex;align-items:center;gap:11px;padding:10px 12px;border-radius:8px;color:#aeb6c4;font-size:13.5px;font-weight:500;letter-spacing:.01em;cursor:pointer;transition:background .15s,color .15s;width:100%;background:none;border:0;font-family:inherit;text-align:start;}
  .nav-toggle:hover{background:rgba(255,255,255,.06);color:#fff;}
  .nav-toggle .nav-chevron{margin-inline-start:auto;transition:transform .2s,opacity .2s;opacity:.5;}
  .nav-toggle.open .nav-chevron{transform:rotate(-90deg);opacity:.85;}
  .nav-sub-wrap{display:none;flex-direction:column;margin-inline-start:12px;border-inline-start:1px solid rgba(255,255,255,.07);padding-block:2px;}
  .nav-sub-wrap.open{display:flex;}
  .nav-item.sub{padding-top:8px;padding-bottom:8px;padding-inline-start:26px;padding-inline-end:12px;font-size:12px;gap:8px;}
  .nav-item.sub svg{width:15px;height:15px;}
  .nav-subgroup{display:flex;align-items:center;gap:6px;padding:8px 10px 4px;margin-inline-start:12px;font-size:10px;font-weight:700;letter-spacing:.04em;color:#6f7b90;}
  .nav-subgroup svg{width:11px;height:11px;}
  .fin-subnav{display:flex;flex-direction:column;margin-inline-start:26px;border-inline-start:1px solid rgba(255,255,255,.07);padding-block:2px;gap:1px;}
  .fin-leaf{display:flex;align-items:center;gap:8px;padding:7px 12px 7px 16px;color:#9aa3b2;font-size:11.5px;font-weight:600;cursor:pointer;border-radius:8px;transition:background .15s,color .15s;}
  .fin-leaf svg{width:14px;height:14px;flex-shrink:0;}
  .fin-leaf:hover{background:rgba(255,255,255,.06);color:#fff;}
  .fin-leaf.active{color:#c8b591;font-weight:700;background:linear-gradient(90deg,color-mix(in srgb,var(--gold) 20%,transparent),transparent);}
  .fin-leaf .fin-count{margin-inline-start:auto;background:rgba(255,255,255,.08);color:#c3ccdb;font-size:9.5px;font-weight:700;min-width:17px;height:17px;border-radius:99px;display:grid;place-items:center;padding:0 5px;}
  .fin-leaf .fin-count:empty{display:none;}
  .fin-leaf.active .fin-count{background:color-mix(in srgb,var(--gold) 30%,transparent);color:#fff;}
  .app.nav-collapsed .fin-subnav{display:none;}

  /* main */
  .main{display:flex;flex-direction:column;height:100vh;overflow:hidden;}
  header{display:flex;align-items:center;justify-content:space-between;gap:18px;padding:14px 30px;background:var(--surface);border-bottom:1px solid var(--border);flex-shrink:0;z-index:5;}
  .crumbs{display:flex;align-items:center;gap:7px;font-size:12px;color:var(--text-3);}
  .crumbs .cur{color:var(--gold-d);font-weight:700;}
  h1{font-size:20px;font-weight:800;margin:4px 0 0;color:var(--heading);letter-spacing:-.01em;}
  .h-tools{display:flex;align-items:center;gap:12px;}
  .search{position:relative;display:flex;align-items:center;}
  .search svg{position:absolute;inset-inline-start:12px;color:var(--text-3);pointer-events:none;}
  .search input{padding:9px 38px 9px 14px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface-2);color:var(--text);font-size:13px;outline:none;transition:border-color .15s,box-shadow .15s;}
  .search input:focus{border-color:var(--gold);box-shadow:0 0 0 3px color-mix(in srgb,var(--gold) 22%,transparent);}
  .icon-btn{width:38px;height:38px;display:grid;place-items:center;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);cursor:pointer;transition:background .15s,color .15s;position:relative;}
  .icon-btn:hover{background:var(--surface-2);color:var(--heading);}
  .ghost-btn{display:flex;align-items:center;gap:7px;height:38px;padding:0 13px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-size:13px;font-weight:500;cursor:pointer;transition:border-color .15s,color .15s;}
  .ghost-btn:hover{border-color:var(--gold);color:var(--gold-d);}
  .logout:hover{border-color:#d9694f;color:#d9694f;}
  .dot-red{position:absolute;top:8px;inset-inline-end:9px;width:7px;height:7px;border-radius:99px;background:#d9694f;border:2px solid var(--surface);}
  .divider{width:1px;height:26px;background:var(--border-2);}
  .user{display:flex;align-items:center;gap:10px;padding:4px 6px 4px 10px;border-radius:8px;cursor:pointer;transition:background .15s;}
  .user:hover{background:var(--surface-2);}
  .avatar{width:34px;height:34px;border-radius:8px;display:grid;place-items:center;background:var(--ink);color:var(--gold-2);font-weight:700;font-size:14px;}
  .user b{font-size:13px;font-weight:700;color:var(--heading);display:block;line-height:1.25;}
  .user span{font-size:11px;color:var(--text-3);}
  .user .chev{color:var(--text-3);margin-inline-start:2px;transition:transform .18s;}
  .user-menu{position:relative;}
  .user-menu.open .chev{transform:rotate(180deg);}
  .user-pop{position:absolute;top:calc(100% + 8px);inset-inline-end:0;min-width:200px;background:var(--surface);border:1px solid var(--border-2);border-radius:11px;box-shadow:0 10px 28px -8px rgba(18,40,76,.28);padding:7px;z-index:30;opacity:0;visibility:hidden;transform:translateY(-4px);transition:opacity .16s,transform .16s,visibility .16s;}
  .user-menu.open .user-pop{opacity:1;visibility:visible;transform:none;}
  .up-row{display:flex;align-items:center;gap:10px;padding:9px 10px;border-radius:8px;font-size:13px;font-weight:600;color:var(--text);cursor:pointer;transition:background .13s,color .13s;}
  .up-row:hover{background:var(--row-hover);}
  .up-row.danger{color:#d9694f;}
  .up-row.danger:hover{background:color-mix(in srgb,#d9694f 10%,transparent);}
  .up-sep{height:1px;background:var(--border);margin:5px 4px;}
  .up-label{font-size:11px;font-weight:700;color:var(--text-3);padding:4px 10px 6px;}
  .theme-row{display:flex;gap:8px;padding:2px 10px 6px;}
  .tsw{flex:1;display:flex;flex-direction:column;gap:5px;border:1px solid var(--border-2);border-radius:9px;cursor:pointer;padding:7px;background:none;transition:border-color .15s,box-shadow .15s;}
  .tsw-bars{display:flex;height:24px;border-radius:6px;overflow:hidden;}
  .tsw-bars span{flex:1;display:block;}
  .tsw-cap{font-size:11.5px;font-weight:700;color:var(--text-2);}
  .tsw:hover{border-color:var(--text-3);}
  .tsw.active{border-color:var(--gold);box-shadow:0 0 0 2px color-mix(in srgb,var(--gold) 28%,transparent);}
  .tsw.active .tsw-cap{color:var(--gold-d);}

  .content{flex:1;overflow-y:auto;overflow-x:hidden;padding:26px 30px 44px;}

  /* KPI band */
  .kpi{display:flex;background:var(--surface);border:1px solid var(--border);border-radius:12px;box-shadow:var(--shadow);overflow:hidden;margin-bottom:24px;}
  .kpi > div{flex:1;padding:20px 24px;border-inline-end:1px solid var(--border);}
  .kpi > div:last-child{border-inline-end:none;}
  .kpi .first{position:relative;}
  .kpi .first::before{content:'';position:absolute;top:0;inset-inline-start:0;width:3px;height:100%;background:var(--gold);}
  .kpi-head{display:flex;align-items:center;gap:9px;margin-bottom:14px;}
  .kpi-ico{width:30px;height:30px;border-radius:7px;display:grid;place-items:center;background:color-mix(in srgb,var(--gold) 12%,transparent);color:var(--heading);}
  .kpi-ico.gold{background:var(--gold-soft);color:var(--gold-d);}
  .kpi-head span{font-size:12.5px;color:var(--text-2);font-weight:500;}
  .kpi-num{font-size:32px;font-weight:800;line-height:1;color:var(--heading);}
  .kpi-sub{font-size:12px;color:var(--text-3);margin-top:8px;display:flex;align-items:center;gap:6px;}
  .kpi-sub .g{width:6px;height:6px;border-radius:99px;background:var(--gold);}

  /* toolbar */
  .toolbar{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-bottom:14px;flex-wrap:wrap;}
  .toolbar h2{font-size:17px;font-weight:800;margin:0;color:var(--heading);}
  .chip{display:inline-flex;align-items:center;gap:4px;padding:3px 10px;border-radius:6px;background:var(--gold-soft);color:var(--gold-d);font-size:12px;font-weight:700;}
  .filters{display:flex;align-items:center;gap:10px;flex-wrap:wrap;}
  .filters .search input{width:248px;background:var(--surface);}
  .sel{position:relative;display:flex;align-items:center;}
  .sel select{appearance:none;-webkit-appearance:none;padding:9px 34px 9px 14px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text);font-size:13px;cursor:pointer;outline:none;}
  .sel .caret{position:absolute;right:11px;color:var(--text-3);pointer-events:none;display:grid;place-items:center;}
  .primary{display:flex;align-items:center;gap:7px;padding:10px 16px;border:none;border-radius:8px;background:var(--ink);color:#fff;font-size:13px;font-weight:700;cursor:pointer;box-shadow:0 6px 16px -8px rgba(18,40,76,.6);transition:transform .12s,background .15s;}
  .primary:hover{background:var(--navy-3);transform:translateY(-1px);}

  /* table */
  .card{background:var(--surface);border:1px solid var(--border);border-radius:12px;box-shadow:var(--shadow);}
  .scroll{overflow:visible;border-radius:12px;}
  .grid{min-width:100%;}
  .thead{display:grid;grid-template-columns:var(--cols);background:var(--surface-2);border-bottom:2px solid var(--gold);}
  .th{padding:14px 16px;display:flex;align-items:center;justify-content:center;text-align:center;font-size:12px;font-weight:700;color:var(--heading);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;min-width:0;}
  .th.c{justify-content:center;}
  .th.sortable{cursor:pointer;gap:4px;}
  .th.sortable:hover{color:var(--gold-d);}
  .th .ind{font-size:11px;color:var(--gold-d);}
  .row{display:grid;grid-template-columns:var(--cols);align-items:center;border-bottom:1px solid var(--border);transition:background .12s;min-height:58px;}
  .row:hover{background:var(--row-hover);position:relative;z-index:6;}
  .td{padding:14px 16px;display:flex;align-items:center;min-width:0;overflow:hidden;}
  /* محاذاة موحّدة للجداول: العمود الأول لليمين (بداية السطر) وبقية الأعمدة توسيط */
  .thead .th, .row .td{justify-content:center !important;text-align:center !important;}
  .thead .th:first-child, .row .td:first-child{justify-content:flex-start !important;text-align:start !important;}
  #engRows .td{padding:7px 16px;}
  #feeRows .td{padding:6px 14px;}
  .row[data-dim]:not(:hover){opacity:.55;}
  .td:has(.team),.td:has(.po-wrap),.td:has(.actions){overflow:visible;}
  .td > *{min-width:0;}
  .td.c{justify-content:center;}
  .po{color:var(--gold-d);font-weight:700;font-size:13.5px;cursor:pointer;}
  .po:hover{text-decoration:underline;}
  .po-wrap{position:relative;width:fit-content;}
  .po-tip{position:absolute;top:calc(100% + 8px);inset-inline-start:0;white-space:nowrap;background:var(--ink);color:#fff;font-size:12px;font-weight:600;padding:7px 11px;border-radius:8px;box-shadow:0 8px 22px -8px rgba(18,40,76,.4);z-index:25;opacity:0;visibility:hidden;transform:translateY(-4px);transition:opacity .16s,transform .16s,visibility .16s;pointer-events:none;}
  .po-tip::before{content:'';position:absolute;bottom:100%;inset-inline-start:14px;border:5px solid transparent;border-bottom-color:var(--ink);}
  .po-wrap:hover .po-tip{opacity:1;visibility:visible;transform:none;}
  .muted{font-size:13px;color:var(--text-2);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
  .type-tag{display:inline-flex;align-items:center;padding:3px 11px;border-radius:6px;font-size:12px;font-weight:500;background:var(--surface-2);border:1px solid var(--border-2);color:var(--text-2);white-space:nowrap;}
  .num{font-weight:800;font-size:14px;color:var(--heading);}
  .num-2{font-weight:700;font-size:13.5px;color:var(--text-2);}
  .prog{display:flex;align-items:center;gap:10px;width:100%;}
  .prog-track{flex:1;height:6px;border-radius:3px;background:color-mix(in srgb,var(--text-3) 26%,transparent);overflow:hidden;}
  .prog-fill{height:100%;border-radius:3px;transition:width .5s ease;}
  .prog-pct{font-size:12px;font-weight:700;color:var(--heading);min-width:32px;text-align:start;}
  .status{display:inline-flex;align-items:center;gap:6px;padding:4px 11px;border-radius:6px;font-size:12px;font-weight:700;white-space:nowrap;}
  .status .sd{width:6px;height:6px;border-radius:99px;}
  @keyframes pulseRing{0%{transform:scale(1);opacity:.55;}80%,100%{transform:scale(2.6);opacity:0;}}
  @keyframes pulseFade{0%,100%{opacity:1;}50%{opacity:.4;}}
  .status .sd.live{position:relative;animation:pulseFade 1.4s ease-in-out infinite;}
  .status .sd.live::after{content:'';position:absolute;inset:0;border-radius:99px;background:inherit;animation:pulseRing 1.6s ease-out infinite;}
  .date{font-size:13px;color:var(--text-2);white-space:nowrap;}
  .date.due{color:var(--text-2);font-weight:500;}
  .spec{display:flex;align-items:center;gap:9px;min-width:0;}
  .spec-av{width:28px;height:28px;border-radius:7px;display:grid;place-items:center;background:var(--ink);color:var(--gold-2);font-weight:700;font-size:12px;flex-shrink:0;}
  .spec-name{font-size:13px;font-weight:600;color:var(--heading);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
  .team{display:flex;align-items:center;position:relative;width:fit-content;}
  .team .ava{width:28px;height:28px;border-radius:99px;display:grid;place-items:center;color:#fff;font-weight:700;font-size:11px;border:2px solid var(--surface);margin-inline-start:-8px;background-size:cover;background-position:center;flex-shrink:0;}
  .team .ava:first-child{margin-inline-start:0;}
  .team .more{width:28px;height:28px;border-radius:99px;display:grid;place-items:center;font-weight:700;font-size:11px;border:2px solid var(--surface);margin-inline-start:-8px;background:var(--surface-2);color:var(--ink);flex-shrink:0;}
  .team-pop{position:absolute;top:calc(100% + 8px);inset-inline-end:0;min-width:190px;background:var(--surface);border:1px solid var(--border-2);border-radius:10px;box-shadow:0 8px 24px -8px rgba(18,40,76,.28);padding:8px;z-index:20;opacity:0;visibility:hidden;transform:translateY(-4px);transition:opacity .16s,transform .16s,visibility .16s;pointer-events:none;}
  .team:hover .team-pop{opacity:1;visibility:visible;transform:none;}
  .team-pop .pop-h{font-size:11px;font-weight:700;color:var(--text-3);padding:2px 8px 6px;}
  .team-pop .pop-row{display:flex;align-items:center;gap:9px;padding:6px 8px;border-radius:7px;}
  .team-pop .pop-row:hover{background:var(--row-hover);}
  .team-pop .pop-av{width:26px;height:26px;border-radius:99px;display:grid;place-items:center;color:#fff;font-weight:700;font-size:11px;flex-shrink:0;background-size:cover;background-position:center;}
  .team-pop .pop-name{font-size:13px;font-weight:600;color:var(--heading);white-space:nowrap;}
  .actions{position:relative;display:flex;justify-content:center;width:100%;}
  .actions.open{z-index:40;}
  .kebab{width:32px;height:32px;display:grid;place-items:center;border:1px solid transparent;border-radius:8px;background:none;color:var(--text-2);cursor:pointer;transition:background .15s,color .15s,border-color .15s;}
  .kebab:hover{background:var(--surface-2);color:var(--heading);border-color:var(--border-2);}
  .act-pop{position:absolute;top:calc(100% + 6px);inset-inline-end:0;min-width:196px;background:var(--surface);border:1px solid var(--border-2);border-radius:11px;box-shadow:0 12px 30px -8px rgba(18,40,76,.3);padding:6px;z-index:40;opacity:0;visibility:hidden;transform:translateY(-4px);transition:opacity .15s,transform .15s,visibility .15s;}
  #ppRows .act-pop,#txRows .act-pop{inset-inline-end:auto;inset-inline-start:auto;left:0;right:auto;}
  .actions.open .act-pop{opacity:1;visibility:visible;transform:none;}
  .act-row{display:flex;align-items:center;gap:10px;padding:9px 10px;border-radius:8px;font-size:13px;font-weight:600;color:var(--text);cursor:pointer;white-space:nowrap;transition:background .13s,color .13s;}
  .act-row:hover{background:var(--row-hover);}
  .act-row.danger{color:#d9694f;}
  .act-row.disabled{opacity:.4;pointer-events:none;}
  .act-row.danger:hover{background:color-mix(in srgb,#d9694f 10%,transparent);}
  .act-sep{height:1px;background:var(--border);margin:4px 4px;}
  .empty{padding:54px 20px;text-align:center;color:var(--text-3);}
  .empty svg{margin-bottom:12px;opacity:.6;}
  .empty .t{font-size:14px;font-weight:700;color:var(--text-2);}
  .empty .s{font-size:13px;margin-top:4px;}

  /* pagination */
  .pager{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:16px 4px 0;flex-wrap:wrap;}
  .pager .info{font-size:13px;color:var(--text-3);}
  .pager .info b{color:var(--heading);font-weight:700;}
  .pager .pages{display:flex;align-items:center;gap:6px;}
  .pg{width:34px;height:34px;display:grid;place-items:center;border:1px solid var(--border-2);border-radius:7px;background:var(--surface);color:var(--text-2);cursor:pointer;transition:background .15s;}
  .pg:hover{background:var(--surface-2);}
  .pg.on{min-width:34px;padding:0 10px;border:none;background:var(--ink);color:#fff;font-size:13px;font-weight:700;}

  .liaison{position:relative;display:inline-flex;align-items:center;gap:6px;cursor:default;}
  .liaison .l-tip{display:none;position:absolute;top:calc(100% + 8px);inset-inline-start:0;z-index:60;min-width:220px;background:var(--surface);border:1px solid var(--border-2);border-radius:11px;box-shadow:0 12px 30px -8px rgba(18,40,70,.25);padding:12px 14px;}
  .liaison:hover .l-tip{display:block;}
  .l-tip .l-row{display:flex;align-items:center;gap:8px;font-size:12px;color:var(--text-2);margin-top:6px;}
  .l-tip .l-row:first-child{margin-top:0;}
  /* ── multi-screen ── */
  .view[hidden]{display:none;}
  .back-link{display:inline-flex;align-items:center;gap:7px;padding:6px 0;margin-bottom:8px;background:none;border:none;color:var(--text-2);font-size:12.5px;font-weight:600;cursor:pointer;transition:color .15s;}
  .back-link:hover{color:var(--gold-d);}
  .back-link svg{transform:scaleX(-1);}
  .pp-head{background:var(--surface);border:1px solid var(--border);border-radius:14px;box-shadow:var(--shadow);padding:18px 22px;margin-bottom:18px;}
  .pp-title{display:flex;align-items:center;gap:10px;flex-wrap:wrap;font-size:18px;font-weight:800;color:var(--heading);margin:0;}
  .pp-po{font-size:14px;color:var(--gold-d);font-weight:700;direction:ltr;}
  .pp-meta{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-top:8px;font-size:12.5px;color:var(--text-2);}
  .pp-badge{display:inline-flex;align-items:center;padding:3px 11px;border-radius:6px;font-size:12px;font-weight:700;}
  .pp-summary{display:flex;flex-wrap:wrap;gap:0;margin-top:16px;border-top:1px solid var(--border);padding-top:14px;}
  .pp-cell{padding:0 18px;border-inline-start:1px solid var(--border);}
  .pp-cell:first-child{padding-inline-start:0;border-inline-start:none;}
  .pp-cell .k{font-size:11px;color:var(--text-3);margin-bottom:3px;}
  .pp-cell .v{font-size:13.5px;font-weight:600;color:var(--heading);}
  .pp-cell .v.ltr{direction:ltr;}
  .ppt-num{display:inline-grid;place-items:center;width:22px;height:22px;border-radius:6px;background:var(--surface-2);border:1px solid var(--border-2);font-size:10px;font-weight:700;color:var(--text-2);}
  .ppt-deed{font-size:12px;font-weight:700;color:var(--gold-d);direction:ltr;}

  /* property detail */
  .pd-hero{background:var(--surface);border:1px solid var(--border);border-radius:14px;box-shadow:var(--shadow);padding:20px 24px;margin-bottom:16px;}
  .pd-eyebrow{display:flex;align-items:center;gap:6px;font-size:11.5px;color:var(--text-3);margin-bottom:6px;}
  .pd-title{display:flex;align-items:center;gap:10px;flex-wrap:wrap;font-size:20px;font-weight:800;color:var(--heading);margin:0;}
  .pd-req{margin-inline-start:auto;text-align:end;}
  .pd-req .k{font-size:11px;color:var(--text-3);}
  .pd-req .v{font-size:22px;font-weight:800;color:var(--gold-d);direction:ltr;}
  .pd-strip{display:flex;flex-wrap:wrap;gap:0;margin-top:16px;border-top:1px solid var(--border);padding-top:14px;}
  .tabs{display:flex;gap:4px;flex-wrap:wrap;border-bottom:1px solid var(--border);margin-bottom:16px;}
  .tab{padding:10px 15px;font-size:13px;font-weight:600;color:var(--text-2);cursor:pointer;border:none;background:none;border-bottom:2px solid transparent;transition:color .15s,border-color .15s;}
  .tab:hover{color:var(--heading);}
  .tab.on{color:var(--gold-d);border-bottom-color:var(--gold);}
  #view-property .tab{font-weight:400;border-bottom:none;border-radius:0;transition:background .15s,color .15s;position:relative;}
  #view-property .tab.hasnew::after{content:'';position:absolute;top:6px;inset-inline-end:3px;width:7px;height:7px;border-radius:99px;background:#c0392b;box-shadow:0 0 0 2px var(--surface);}
  #view-property .tab:hover{background:color-mix(in srgb,var(--ink) 6%,transparent);color:var(--heading);}
  #view-property .tab.on{background:var(--ink);color:#fff;}
  #view-property{zoom:.95;}
  .fields{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:14px;background:var(--surface);border:1px solid var(--border);border-radius:14px;box-shadow:var(--shadow);padding:20px 24px;}
  .field .k{font-size:11.5px;color:var(--text-3);margin-bottom:4px;}
  .field .v{font-size:14px;font-weight:600;color:var(--heading);}
  .field .v.ltr{direction:ltr;}
  .panel-note{background:var(--surface);border:1px dashed var(--border-2);border-radius:12px;padding:26px;text-align:center;color:var(--text-3);font-size:13px;}
  /* dashboard */
  .dash-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:4px;}
  .dash-card{background:var(--surface);border:1px solid var(--border);border-radius:14px;box-shadow:var(--shadow);padding:18px 20px;}
  .dash-card h3{font-size:14px;font-weight:700;color:var(--heading);margin:0 0 14px;}
  .dcard-kpi{background:var(--surface);border:1px solid var(--border);border-radius:14px;box-shadow:var(--shadow);padding:15px 18px;cursor:pointer;transition:border-color .15s,box-shadow .15s,transform .1s;}
  .dcard-kpi:hover{border-color:var(--border-2);box-shadow:0 4px 14px rgba(15,52,96,.09);}
  .dcard-kpi:active{transform:translateY(1px);}
  .dash-line{display:flex;align-items:center;gap:11px;padding:9px 4px;border-bottom:1px solid var(--border);}
  .dash-line:last-child{border-bottom:0;}
  .dash-line:hover{background:var(--surface-2);border-radius:8px;}
  .dash-line[data-new="1"]{box-shadow:inset -3px 0 0 var(--gold);}
  @keyframes dashdraw{to{stroke-dashoffset:0}}
  @keyframes chartfade{from{opacity:0}to{opacity:1}}
  @keyframes barrise{from{transform:scaleY(0)}to{transform:scaleY(1)}}
  .chart-dot{opacity:0;animation:chartfade .4s ease forwards;}
  .chart-bar{transform-origin:bottom;transform-box:fill-box;animation:barrise .9s cubic-bezier(.22,1,.36,1) forwards;}
  .dash-ico{width:30px;height:30px;border-radius:8px;flex:none;display:grid;place-items:center;}
  .bar-row{display:flex;align-items:center;gap:10px;margin-bottom:10px;font-size:12.5px;}
  .bar-row .lbl{width:120px;color:var(--text-2);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
  .bar-row .bar{flex:1;height:8px;border-radius:99px;background:color-mix(in srgb,var(--text-3) 20%,transparent);overflow:hidden;}
  .bar-row .bar span{display:block;height:100%;border-radius:99px;}
  .bar-row .n{width:26px;text-align:start;font-weight:700;color:var(--heading);}
  .generic-empty{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:12px;min-height:60vh;color:var(--text-3);text-align:center;}
  .generic-empty svg{opacity:.5;}

  /* modal */
  .modal-overlay{position:fixed;inset:0;z-index:200;background:rgba(16,43,78,.42);backdrop-filter:blur(2px);display:flex;align-items:flex-start;justify-content:center;padding:6vh 16px;overflow-y:auto;}
  .modal-overlay[hidden]{display:none;}
  .modal{width:100%;max-width:640px;background:var(--surface);border:1px solid var(--border);border-radius:16px;box-shadow:0 24px 60px -18px rgba(16,43,78,.5);overflow:hidden;animation:mIn .22s ease both;}
  @keyframes mIn{from{opacity:0;transform:translateY(14px) scale(.98)}to{opacity:1;transform:none}}
  .modal-head{display:flex;align-items:center;justify-content:space-between;padding:16px 22px;border-bottom:1px solid var(--border);}
  .modal-head h2{margin:0;font-size:16px;font-weight:800;color:var(--heading);}
  .modal-x{width:32px;height:32px;display:grid;place-items:center;border:none;background:var(--surface-2);border-radius:9px;color:var(--text-2);font-size:15px;cursor:pointer;transition:background .15s,color .15s;}
  .modal-x:hover{background:var(--row-hover);color:var(--heading);}
  .modal-body{padding:20px 22px;}
  .modal-error{background:color-mix(in srgb,#d9694f 12%,transparent);color:#a32d2d;border:1px solid color-mix(in srgb,#d9694f 30%,transparent);border-radius:10px;padding:10px 13px;font-size:12.5px;font-weight:600;margin-bottom:16px;}
  .modal-error[hidden]{display:none;}
  .form-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;}
  .fld{display:flex;flex-direction:column;gap:6px;}
  .fld.full{grid-column:1 / -1;}
  .fld label{font-size:12px;font-weight:600;color:var(--text-2);}
  .fld input,.fld select,.fld textarea{font-family:inherit;padding:9px 12px;border:1px solid var(--border-2);border-radius:9px;background:var(--surface-2);color:var(--text);font-size:13px;outline:none;transition:border-color .15s,box-shadow .15s;}
  .fld textarea{resize:vertical;}
  .fld input:focus,.fld select:focus,.fld textarea:focus{border-color:var(--gold);box-shadow:0 0 0 3px color-mix(in srgb,var(--gold) 20%,transparent);}
  .fld.err input,.fld.err select{border-color:#d9694f;box-shadow:0 0 0 3px color-mix(in srgb,#d9694f 18%,transparent);}
  .fld .msg{font-size:11px;color:#d9694f;font-weight:600;}
  .modal-foot{display:flex;justify-content:flex-end;gap:10px;padding:14px 22px;border-top:1px solid var(--border);background:var(--surface-2);}
  .btn-ghost{padding:10px 18px;border:1px solid var(--border-2);border-radius:9px;background:var(--surface);color:var(--text-2);font-size:13px;font-weight:600;cursor:pointer;transition:background .15s;}
  .btn-ghost:hover{background:var(--row-hover);}

  /* tasks */
  .tf-seg-row{display:inline-flex;}
  .tf-seg{padding:9px 15px;border:1px solid var(--border-2);background:var(--surface-2);color:var(--text-2);font-size:12.5px;font-weight:600;cursor:pointer;font-family:inherit;transition:background .13s,color .13s;}
  .tf-seg + .tf-seg{border-inline-start:none;}
  .tf-seg:first-child{border-radius:0 9px 9px 0;}
  .tf-seg:last-child{border-radius:9px 0 0 9px;}
  .tf-seg.active{background:var(--ink);color:#fff;border-color:var(--ink);}
  .tf-note{padding:12px 14px;background:var(--surface-2);border:1px dashed var(--border-2);border-radius:10px;color:var(--text-3);font-size:12.5px;}
  .tf-lbl{display:block;font-size:12px;font-weight:600;color:var(--text-2);margin-bottom:7px;}
  .tf-chip{padding:8px 15px;border:1px solid var(--border-2);border-radius:99px;background:var(--surface);color:var(--text-2);font-size:12.5px;font-weight:600;cursor:pointer;font-family:inherit;transition:background .13s,color .13s;}
  .tf-chip.active{background:var(--gold-soft);color:var(--gold-d);border-color:var(--gold-2);}
  .tf-deeds{max-height:200px;overflow-y:auto;border:1px solid var(--border-2);border-radius:10px;padding:6px;display:flex;flex-direction:column;gap:2px;background:var(--surface-2);}
  .tf-deed{display:flex;align-items:center;gap:11px;padding:8px 10px;border-radius:8px;cursor:pointer;font-size:12.5px;color:var(--text);}
  .tf-deed:hover{background:var(--row-hover);}
  .tf-deed input{accent-color:var(--gold-d);width:16px;height:16px;flex:none;}
  .tf-actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:20px;}
  .step-flow{display:flex;align-items:center;margin:6px 0 4px;flex-wrap:wrap;gap:6px 0;}
  .step{display:flex;align-items:center;gap:9px;}
  .step-dot{width:30px;height:30px;border-radius:50%;display:grid;place-items:center;font-size:12px;font-weight:700;border:2px solid var(--border-2);color:var(--text-3);background:var(--surface);flex:none;}
  .step.done .step-dot{background:var(--ink);border-color:var(--ink);color:#fff;}
  .step.active .step-dot{background:var(--gold);border-color:var(--gold);color:#fff;}
  .step.cancel .step-dot{background:#d9694f;border-color:#d9694f;color:#fff;}
  .step-lbl{font-size:12.5px;font-weight:600;color:var(--text-3);}
  .step.done .step-lbl,.step.active .step-lbl,.step.cancel .step-lbl{color:var(--heading);}
  .step-line{width:34px;height:2px;background:var(--border-2);margin:0 8px;}
  .step-line.on{background:var(--ink);}
  .letter-card{border:1px solid var(--border);border-radius:14px;overflow:hidden;background:var(--surface);margin-top:20px;}
  .letter-head{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:15px 18px;border-bottom:1px solid var(--border);background:var(--surface-2);flex-wrap:wrap;}
  .task-desc{background:var(--surface-2);border:1px solid var(--border);border-radius:12px;padding:14px 16px;font-size:13px;color:var(--text);line-height:1.7;margin-top:16px;}
  .cmt-thread{display:flex;flex-direction:column;}
  .cmt{display:flex;gap:11px;padding:14px 2px;border-top:1px solid var(--border);}
  .cmt:first-child{border-top:none;}
  .cmt-av{width:32px;height:32px;border-radius:8px;flex:none;display:grid;place-items:center;font-weight:700;font-size:12px;color:#fff;}
  .cmt-body{flex:1;min-width:0;}
  .cmt-h{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-bottom:5px;}
  .cmt-name{font-size:13px;font-weight:700;color:var(--heading);}
  .cmt-role{font-size:10.5px;font-weight:700;padding:2px 8px;border-radius:99px;}
  .cmt-time{font-size:11px;color:var(--text-3);margin-inline-start:auto;}
  .cmt-text{font-size:13px;color:var(--text);line-height:1.75;white-space:pre-wrap;}
  .cmt-files{display:flex;flex-wrap:wrap;gap:7px;margin-top:9px;}
  .file-chip{display:inline-flex;align-items:center;gap:7px;padding:6px 11px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface-2);font-size:12px;color:var(--text-2);font-weight:600;}
  .file-chip .fx{cursor:pointer;color:var(--text-3);display:grid;place-items:center;}
  .file-chip .fx:hover{color:#d9694f;}
  .cmt-composer{border-top:1px solid var(--border);padding-top:15px;margin-top:4px;}
  .cmt-composer textarea{width:100%;font-family:inherit;padding:11px 13px;border:1px solid var(--border-2);border-radius:10px;background:var(--surface-2);color:var(--text);font-size:13px;outline:none;resize:vertical;min-height:62px;}
  .cmt-composer textarea:focus{border-color:var(--gold);box-shadow:0 0 0 3px color-mix(in srgb,var(--gold) 20%,transparent);}
  .cmt-bar{display:flex;align-items:center;gap:10px;margin-top:11px;}
  .attach-btn{display:inline-flex;align-items:center;gap:7px;padding:9px 14px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-size:12.5px;font-weight:600;cursor:pointer;font-family:inherit;transition:background .13s;}
  .attach-btn:hover{background:var(--row-hover);color:var(--heading);}
  .tk-check{display:grid;place-items:center;cursor:pointer;}
  .tk-check input,#tkSelAll{width:17px;height:17px;accent-color:var(--gold-d);cursor:pointer;margin:0;flex:none;}
  .remind-mini{width:32px;height:32px;display:grid;place-items:center;border:1px solid transparent;border-radius:8px;background:none;color:var(--gold-d);cursor:pointer;transition:background .15s,border-color .15s;}
  .remind-mini:hover{background:color-mix(in srgb,var(--gold) 14%,transparent);border-color:var(--gold-2);}
  .cd-wrap{position:relative;display:inline-flex;align-items:center;}
  .cd-dot{width:9px;height:9px;border-radius:50%;flex:none;position:relative;display:inline-block;margin-inline-end:8px;}
  .cd-dot.live{animation:pulseFade 1.4s ease-in-out infinite;}
  .cd-dot.live::after{content:'';position:absolute;inset:0;border-radius:50%;background:inherit;animation:pulseRing 1.6s ease-out infinite;}
  .cd-tip{position:absolute;bottom:calc(100% + 8px);inset-inline-start:0;white-space:nowrap;background:var(--ink);color:#fff;font-size:11.5px;font-weight:600;padding:6px 10px;border-radius:8px;box-shadow:0 8px 22px -8px rgba(18,40,76,.42);z-index:25;opacity:0;visibility:hidden;transform:translateY(-4px);transition:opacity .16s,transform .16s,visibility .16s;pointer-events:none;}
  .cd-tip::before{content:'';position:absolute;top:100%;inset-inline-start:14px;border:5px solid transparent;border-top-color:var(--ink);}
  .cd-wrap:hover .cd-tip{opacity:1;visibility:visible;transform:none;}
  .td:has(.cd-wrap){overflow:visible;}
  .remind-card{display:flex;align-items:center;justify-content:space-between;gap:14px;flex-wrap:wrap;background:var(--surface);border:1px solid var(--border);border-radius:12px;box-shadow:var(--shadow);padding:14px 18px;margin-top:16px;}
  .remind-btn{display:inline-flex;align-items:center;gap:8px;padding:10px 18px;border:none;border-radius:9px;background:var(--gold-d);color:#fff;font-size:13px;font-weight:700;cursor:pointer;font-family:inherit;box-shadow:0 6px 16px -6px color-mix(in srgb,var(--gold-d) 60%,transparent);transition:background .15s,transform .12s;}
  .remind-btn:hover{background:var(--gold);transform:translateY(-1px);}
  .toast{position:fixed;bottom:24px;inset-inline-start:50%;transform:translate(-50%,20px);background:var(--ink);color:#fff;font-size:13px;font-weight:600;padding:12px 18px;border-radius:10px;box-shadow:0 14px 34px -10px rgba(16,40,76,.55);display:flex;align-items:center;gap:10px;z-index:400;opacity:0;visibility:hidden;transition:opacity .25s,transform .25s,visibility .25s;max-width:90vw;}
  .toast .tico{color:var(--gold-2);flex:none;}
  .toast.show{opacity:1;visibility:visible;transform:translate(-50%,0);}
@keyframes newDot{0%,100%{opacity:1;box-shadow:0 0 0 0 rgba(47,125,225,.55)}50%{opacity:.55;box-shadow:0 0 0 5px rgba(47,125,225,0)}}

  /* ── طي القائمة الجانبية ── */
  aside{transition:width .2s ease;}
  .app.nav-collapsed{grid-template-columns:68px 1fr;}
  .app.nav-collapsed aside .brand{padding-inline:8px;}
  .app.nav-collapsed .brand-card svg{width:46px;}
  .app.nav-collapsed nav{padding-inline:10px;}
  .app.nav-collapsed .nav-group{display:none;}
  .app.nav-collapsed .nav-item,.app.nav-collapsed .nav-toggle{justify-content:center;padding-inline:0;gap:0;position:relative;}
  .app.nav-collapsed .nav-item>span:not(.nav-badge),.app.nav-collapsed .nav-toggle>span{display:none;}
  .app.nav-collapsed .nav-chevron{display:none;}
  .app.nav-collapsed .nav-sub-wrap{display:none !important;}
  .app.nav-collapsed .nav-item.active::before{width:3px;height:22px;}
  .app.nav-collapsed .nav-badge{position:absolute;top:2px;inset-inline-end:6px;min-width:16px;height:16px;font-size:9px;}
  /* ── زر طي القائمة (الركن السفلي — الحافة الداخلية) ── */
  .nav-collapse-btn{margin-top:auto;margin-inline-start:12px;margin-bottom:14px;width:36px;height:36px;flex:none;display:grid;place-items:center;border-radius:10px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.04);color:#aeb6c4;cursor:pointer;transition:background .15s,color .15s;}
  .nav-collapse-btn:hover{background:rgba(255,255,255,.1);color:#fff;}
  .nav-collapse-btn .ncb-chev{transition:transform .2s ease;}
  .app.nav-collapsed .nav-collapse-btn{margin-inline:auto;}
  .app.nav-collapsed .nav-collapse-btn .ncb-chev{transform:rotate(180deg);}

  /* ── نموذج دراسة الحالة — التقرير المطبوع المُصغّر (EJADAH) ── */
  .csx{--navy:#0f2a4e;--gold:#a38f67;--cb:#c8c8c8;--alt:#f9fafc;--sub:#f5f6f8;--appr:#f8f9fa;--yb:#fffbeb;--ybd:#e5c84a;--yt:#7a5f00;--tx:#1c1c1c;--tm:#5a5a5a;color:var(--tx);direction:rtl;background:#fff;border:1px solid var(--cb);border-radius:6px;overflow:hidden;position:relative;font-size:13px;line-height:1.5;}
  .csx *{box-sizing:border-box;}
  .csx-header{display:flex;border-bottom:3px solid var(--gold);}
  .csx-logo{background:var(--navy);display:flex;flex-direction:column;align-items:center;justify-content:center;padding:12px 24px;gap:3px;}
  .csx-word{color:#fff;font-size:26px;font-weight:800;line-height:1;letter-spacing:.5px;}
  .csx-word span{color:var(--gold);}
  .csx-subw{color:var(--gold);font-size:9px;font-weight:700;letter-spacing:3px;}
  .csx-div{width:4px;background:var(--gold);}
  .csx-svc{display:flex;align-items:center;padding:0 20px;color:var(--gold);font-size:12px;font-weight:700;letter-spacing:2px;line-height:1.5;}
  .csx-body{padding:20px 26px;}
  .csx-title{text-align:center;margin-bottom:16px;padding-bottom:12px;border-bottom:1.5px solid var(--gold);}
  .csx-title-m{font-size:19px;font-weight:800;color:var(--navy);}
  .csx-title-s{font-size:13px;font-weight:700;color:var(--gold);margin-top:3px;}
  .csx-tbl{width:100%;border-collapse:collapse;margin-bottom:14px;table-layout:fixed;}
  .csx-tbl th,.csx-tbl td{border:1px solid var(--cb);padding:6px 10px;text-align:right;vertical-align:middle;font-size:12.5px;}
  .csx-sec td{background:var(--navy);color:#fff;font-weight:800;text-align:center;font-size:14px;border-color:var(--navy);}
  .csx-col th{background:var(--navy);color:#fff;font-weight:700;text-align:center;font-size:12px;}
  .csx-col th:first-child{text-align:right;}
  .csx-yn{text-align:center !important;width:120px;}
  .csx-lbl{font-weight:700;color:var(--navy);width:38%;background:var(--alt);}
  .csx-ltr{direction:ltr;text-align:right;}
  .csx-notes td{background:var(--appr);font-size:12px;line-height:1.6;}
  .csx-notes b{display:block;color:var(--navy);margin-bottom:2px;}
  .csx-alert{background:var(--yb);border:1px solid var(--ybd);border-radius:3px;padding:5px 11px;font-size:12px;color:var(--yt);margin-bottom:6px;}
  .csx-cb{font-size:16px;color:#8896a6;display:inline-block;}
  .csx-cb.on{color:var(--navy);}
  .csx-subrow td{background:var(--sub);font-size:11.5px;color:var(--tm);font-style:italic;}
  .csx-decl{background:var(--appr);border-inline-start:4px solid var(--gold);padding:8px 13px;font-size:12.5px;margin-bottom:10px;line-height:1.6;}
  .csx-appr th{background:var(--navy);color:#fff;text-align:center;font-size:12.5px;}
  .csx-appr td{height:74px;text-align:center;vertical-align:middle;}
  .csx-stamp{width:60px;height:60px;border:2px solid #1a6b5a;border-radius:50%;display:grid;place-items:center;color:#1a6b5a;font-size:8px;font-weight:800;text-align:center;line-height:1.25;transform:rotate(-8deg);margin:0 auto;}
  .csx-footer{display:flex;align-items:center;gap:18px;padding:11px 26px;border-top:1.5px solid var(--cb);direction:ltr;font-size:10.5px;color:var(--tm);flex-wrap:wrap;}
  .csx-footer .g{color:var(--gold);font-weight:700;}
  .csx-footer .ar{margin-inline-start:auto;direction:rtl;text-align:right;font-size:11px;font-weight:700;color:#0f2a4e;}

  /* ── مساحة عمل نموذج دراسة الحالة ── */
  .csw-mtbl{width:100%;border-collapse:collapse;}
  .csw-mtbl th{background:#eaf2fb;border-bottom:1px solid var(--border);padding:10px 14px;font-size:11px;font-weight:700;color:var(--text-2);}
  .csw-mtbl th.c{text-align:center;color:var(--ink);}
  .csw-mtbl td{border-bottom:1px solid var(--border);padding:10px 14px;font-size:12px;vertical-align:top;background:var(--surface);}
  .csw-mtbl tr.conflict td{background:#fff6f4;}
  .csw-mtbl tr.conflict td:first-child{box-shadow:inset 3px 0 0 #E74C3C;}
  .csw-mtbl tr.await td:first-child{box-shadow:inset 3px 0 0 #F39C12;}
  .csw-pick{width:24px;height:24px;border-radius:6px;border:2px solid #B8CDE0;background:#fff;color:#fff;cursor:pointer;display:inline-flex;align-items:center;justify-content:center;padding:0;transition:border-color .12s,background .12s;}
  .csw-pick:hover{border-color:#2E86C1;}
  .csw-pick.on-y{border-color:#1ABC9C;background:#1ABC9C;}
  .csw-pick.on-n{border-color:#E74C3C;background:#E74C3C;}
  .csw-adopt{margin-top:6px;display:inline-flex;align-items:center;gap:4px;border-radius:99px;border:1px solid #BCDCF2;background:#D6EAF8;color:#1A5276;padding:4px 9px;font-size:10px;font-weight:700;cursor:pointer;line-height:1.3;font-family:inherit;}
  .csw-adopt:hover{background:#C3E0F6;}
  .csw-step{display:inline-flex;align-items:center;gap:7px;padding:9px 14px;border:none;border-bottom:2px solid transparent;background:none;font-size:12px;font-weight:600;color:var(--text-3);cursor:pointer;font-family:inherit;white-space:nowrap;transition:color .15s,border-color .15s;}
  .csw-step .n{width:24px;height:24px;border-radius:99px;background:var(--surface-2);display:grid;place-items:center;font-size:12px;font-weight:700;color:var(--text-2);}
  .csw-step.on{color:var(--ink);border-bottom-color:var(--ink);}
  .csw-step.on .n{background:var(--ink);color:#fff;}
  .csw-step.done{color:#2f7a4d;}
  .csw-step.done .n{background:#2f7a4d;color:#fff;}
  /* ── استعلام بورصة ── */
  .brs-split{display:grid;gap:18px;align-items:start;grid-template-columns:1fr;}
  @media(min-width:1180px){.brs-split{grid-template-columns:1fr 1fr;}}
  /* ── تسجيل الدخول (LoginPage — apps/shell/src/app/login) ── */
  .lg-wrap{position:fixed;inset:0;z-index:400;display:flex;align-items:center;justify-content:center;background:var(--surface-2);padding:24px;overflow:auto;}
  .lg-wrap[hidden]{display:none;}
  .lg-card{width:100%;max-width:430px;background:var(--surface);border:1px solid var(--border);border-inline-start:3px solid var(--gold);border-radius:16px;box-shadow:0 18px 50px -28px rgba(18,40,76,.4);padding:38px 40px;}
  .lg-brand{display:flex;flex-direction:column;align-items:center;gap:12px;text-align:center;margin-bottom:26px;}
  .lg-logo{width:172px;height:auto;max-width:100%;}
  .lg-tag{font-size:13px;color:var(--text-3);}
  .lg-title{margin:0 0 4px;font-size:21px;font-weight:700;color:var(--heading);}
  .lg-sub{margin:0 0 22px;font-size:15px;line-height:1.7;color:var(--text-2);}
  .lg-error{margin-bottom:16px;border-radius:10px;border-inline-end:3px solid #d9694f;background:color-mix(in srgb,#d9694f 12%,transparent);padding:10px 13px;font-size:13px;line-height:1.7;color:#a32d2d;font-weight:600;}
  .lg-error[hidden]{display:none;}
  .lg-form{display:flex;flex-direction:column;gap:16px;}
  .lg-field{display:flex;flex-direction:column;gap:7px;}
  .lg-field label{font-size:13px;font-weight:600;color:var(--text-2);}
  .lg-field input{font-family:inherit;padding:11px 13px;border:1px solid var(--border-2);border-radius:9px;background:var(--surface-2);color:var(--text);font-size:14px;outline:none;transition:border-color .15s,box-shadow .15s;}
  .lg-field input:focus{border-color:var(--gold);box-shadow:0 0 0 3px color-mix(in srgb,var(--gold) 20%,transparent);}
  .lg-field input::placeholder{color:var(--text-3);}
  .lg-btn{margin-top:6px;width:100%;justify-content:center;display:flex;align-items:center;gap:8px;padding:12px;border:none;border-radius:9px;background:var(--ink);color:#fff;font-size:15px;font-weight:700;font-family:inherit;cursor:pointer;box-shadow:0 8px 20px -12px rgba(18,40,76,.6);transition:background .15s,transform .12s;}
  .lg-btn:hover{background:var(--navy-3);transform:translateY(-1px);}
  .lg-btn:disabled{opacity:.5;cursor:not-allowed;transform:none;box-shadow:none;}
</style>
</head>
<body>
<div class="app">
  <aside>
    <div class="brand">
      <div class="brand-card">
        <svg viewBox="0 0 141.7 50" xmlns="http://www.w3.org/2000/svg"><g><g><path fill="#ffffff" d="M139.9,26c0.1-0.1,0.1-0.2,0.1-0.3c0-0.1,0-0.2-0.1-0.2c0-0.1-0.1-0.1-0.2-0.2c-0.1,0-0.1-0.1-0.2-0.1c-0.1,0-0.2,0-0.3,0h-0.6V27h0.3v-0.8h0.2l0.4,0.8h0.4l-0.5-0.8C139.7,26.2,139.9,26.1,139.9,26z M139.5,26c0,0-0.1,0-0.1,0h-0.3v-0.5h0.3c0,0,0.1,0,0.1,0c0,0,0.1,0,0.1,0c0,0,0.1,0,0.1,0.1c0,0,0,0.1,0,0.1c0,0.1,0,0.1,0,0.1c0,0-0.1,0.1-0.1,0.1C139.5,25.9,139.5,25.9,139.5,26z"/><path fill="#ffffff" d="M139.3,24.4c-0.9,0-1.7,0.7-1.7,1.7c0,0.9,0.7,1.7,1.7,1.7c0.9,0,1.7-0.7,1.7-1.7C141,25.2,140.3,24.4,139.3,24.4z M139.3,27.5c-0.8,0-1.4-0.6-1.4-1.4c0-0.8,0.6-1.4,1.4-1.4s1.4,0.6,1.4,1.4C140.7,26.9,140.1,27.5,139.3,27.5z"/></g><path fill="#ffffff" d="M35.3,27.7c1.7,0,5.4,0,5.4,0l8-16.8l8,16.8H62L48.6,1C48.6,1,39.7,18.8,35.3,27.7z"/><path fill="#ffffff" d="M89.6,27.7c1.7,0,5.4,0,5.4,0l8-16.8l8,16.8h5.4L103,1C103,1,94.1,18.8,89.6,27.7z"/><polygon fill="#ffffff" points="131.3,12.4 115.5,12.4 115.5,16.8 131.4,16.8 131.4,27.7 135.7,27.7 135.7,1 131.3,1"/><path fill="#ffffff" d="M29.2,1c0,0-0.1,11.9-0.1,17.5c0,2.3-1.7,4.5-4.1,4.8c-0.4,0-1.3,0.1-1.3,0.1v4.3c0,0,1.9-0.1,2.8-0.3c2.5-0.5,4.6-1.8,5.8-4.1c0.9-1.7,1.3-3.6,1.3-5.5c0-5.4,0-16.8,0-16.8H29.2z"/><rect x="0.8" y="1" fill="#ffffff" width="17.8" height="4.3"/><rect x="0.8" y="23.4" fill="#ffffff" width="17.8" height="4.3"/><rect x="0.7" y="12.2" fill="#ffffff" width="14.9" height="4.3"/><path fill="#ffffff" d="M75.3,1L75.3,1L59.8,1v4.5h15.5v0c4.9,0,9,4,9,8.9c0,4.9-4,8.9-9,8.9h-8.9v4.3h8.9c7.4,0,13.3-5.9,13.3-13.3S82.7,1,75.3,1z"/><g><path fill="#A4906F" d="M20.4,43.5c0,1.7-1.2,2.8-3.1,2.8h-2.2v3h-1v-8.7h3.2C19.3,40.7,20.4,41.8,20.4,43.5z M19.4,43.5c0-1.2-0.7-1.8-2-1.8h-2.2v3.7h2.2C18.6,45.4,19.4,44.7,19.4,43.5z"/><path fill="#A4906F" d="M26.2,45.7h-1.8v3.6h-1v-8.7H27c1.7,0,2.7,1,2.7,2.5c0,1.4-0.9,2.3-2.4,2.5l2.5,3.7h-1.3L26.2,45.7z M24.4,44.7H27c1.1,0,1.7-0.6,1.7-1.5s-0.6-1.5-1.7-1.5h-2.6V44.7z"/><path fill="#A4906F" d="M32,45c0-2.6,1.9-4.5,4.5-4.5c2.6,0,4.5,1.8,4.5,4.5s-1.9,4.5-4.5,4.5C33.9,49.5,32,47.7,32,45z M39.9,45c0-2.1-1.4-3.5-3.4-3.5c-2,0-3.4,1.4-3.4,3.5c0,2.1,1.4,3.5,3.4,3.5C38.5,48.5,39.9,47.1,39.9,45z"/><path fill="#A4906F" d="M44.9,41.6v3.1h4.4v0.9h-4.4v3.6h-1v-8.7h5.7v1H44.9z"/><path fill="#A4906F" d="M58.6,48.4v1h-6v-8.7h5.9v1h-4.9v2.9h4.7v0.9h-4.7v3H58.6z"/><path fill="#A4906F" d="M61,48.4l0.5-0.9c0.8,0.6,1.9,1,3,1c1.3,0,2.3-0.5,2.3-1.5c0-1.1-1.2-1.3-2.3-1.5c-2.5-0.5-3.2-1.1-3.2-2.3c0-2.1,1.9-2.6,3.4-2.6c1.1,0,2.1,0.3,3,0.8l-0.5,0.9c-0.8-0.5-1.6-0.7-2.5-0.7c-1.6,0-2.4,0.6-2.4,1.6c0,0.8,0.6,1.2,2.4,1.5c1.1,0.2,3.2,0.5,3.2,2.4c0,1.5-1.3,2.5-3.3,2.5C63,49.5,61.6,48.9,61,48.4z"/><path fill="#A4906F" d="M70.3,48.4l0.5-0.9c0.8,0.6,1.9,1,3,1c1.3,0,2.3-0.5,2.3-1.5c0-1.1-1.2-1.3-2.3-1.5c-2.5-0.5-3.2-1.1-3.2-2.3c0-2.1,1.9-2.6,3.4-2.6c1.1,0,2.1,0.3,3,0.8l-0.5,0.9c-0.8-0.5-1.6-0.7-2.5-0.7c-1.6,0-2.4,0.6-2.4,1.6c0,0.8,0.6,1.2,2.4,1.5c1.1,0.2,3.2,0.5,3.2,2.4c0,1.5-1.3,2.5-3.3,2.5C72.4,49.5,71,48.9,70.3,48.4z"/><path fill="#A4906F" d="M81.2,49.3h-1v-8.7h1V49.3z"/><path fill="#A4906F" d="M84.2,45c0-2.6,1.9-4.5,4.5-4.5c2.6,0,4.5,1.8,4.5,4.5s-1.9,4.5-4.5,4.5C86.1,49.5,84.2,47.7,84.2,45z M92.1,45c0-2.1-1.4-3.5-3.4-3.5c-2,0-3.4,1.4-3.4,3.5c0,2.1,1.4,3.5,3.4,3.5C90.7,48.5,92.1,47.1,92.1,45z"/><path fill="#A4906F" d="M103,40.7v8.7h-0.9l-5-6.8v6.8h-1v-8.7h0.9l4.9,6.8v-6.8H103z"/><path fill="#A4906F" d="M112,46.9h-4.1l-1,2.4h-1.1l3.6-8.7h1.2l3.6,8.7h-1.1L112,46.9z M111.6,46l-1.7-4.2l-1.7,4.2H111.6z"/><path fill="#A4906F" d="M122.3,48.4v1h-5.6v-8.7h1v7.7H122.3z"/></g></g></svg>
      </div>

    <nav>
      <div class="nav-group">المالية</div>
      <div class="fin-subnav" style="margin:0">
        <div class="fin-leaf" data-fin-nav="inbox"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M22 12h-6l-2 3h-4l-2-3H2"/><path d="M5.45 5.11 2 12v6a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-6l-3.45-6.89A2 2 0 0 0 16.76 4H7.24a2 2 0 0 0-1.79 1.11z"/></svg><span>مهامي</span><span class="fin-count" data-fin-count="inbox"></span></div>
        <div class="fin-leaf" data-fin-nav="revenue"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-3 3"/></svg><span>الإيرادات</span><span class="fin-count" data-fin-count="revenue"></span></div>
        <div class="fin-leaf" data-fin-nav="costs"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg><span>التكاليف</span><span class="fin-count" data-fin-count="costs"></span></div>
      </div>

      <div class="nav-group">بوابات الأطراف — لتجربة الأثر</div>
      <div class="nav-item" id="navEngFees"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4v16h16"/><path d="M4 20 20 4"/><path d="M8 20v-3"/><path d="M12 20v-3"/><path d="M16 20v-3"/></svg><span>المكتب الهندسي</span></div>
      <div class="nav-item" id="navInspector"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M15 12a3 3 0 1 1-6 0 3 3 0 0 1 6 0z"/><path d="M2.46 12C3.73 7.94 7.52 5 12 5s8.27 2.94 9.54 7c-1.27 4.06-5.06 7-9.54 7s-8.27-2.94-9.54-7z"/></svg><span>المعاين</span></div>
    </nav>
    <button id="navCollapseBtn" class="nav-collapse-btn" type="button" title="طي/فتح القائمة الجانبية" aria-label="طي القائمة الجانبية"><svg class="ncb-chev" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="11 17 6 12 11 7"/><polyline points="18 17 13 12 18 7"/></svg></button>
  </aside>

  <div class="main">
    <header>
      <div style="display:flex;align-items:center;gap:14px;min-width:0">
        <div style="min-width:0">
          <h1 id="pageTitle">المالية — مهامي</h1>
          <div class="crumbs" id="crumbs"></div>
        </div>
      </div>
      <div class="hdr-right">
        <div class="user"><div class="avatar">س</div><div class="who"><b>سليمان القحطاني</b><span>موظف المالية</span></div></div>
      </div>
    </header>

    <div class="content">
      <div id="view-generic" class="view"></div>
    </div>
  </div>
</div>

<script>
(function(){
  'use strict';
  function esc(s){ return String(s==null?'':s).replace(/[&<>"']/g,function(c){ return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]; }); }
  var NOW = new Date(2026, 6, 1).getTime(); // تاريخ مرجعي 01/07/2026
  var DAY = 86400000;
  var BOOT = Date.now();
  var WEEKDAYS = ['الأحد','الإثنين','الثلاثاء','الأربعاء','الخميس','الجمعة','السبت'];
  function pad2(n){ return String(n).padStart(2,'0'); }
  function fmtSAR(n){ return Number(n||0).toLocaleString('en-US')+' ر.س'; }
  var CHEV = '<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>';
  function crumb(parts){
    return parts.map(function(p,i){
      var last = i === parts.length - 1;
      return (i ? CHEV : '') + '<span' + (last ? ' class="cur"' : '') + '>' + esc(p) + '</span>';
    }).join('');
  }
  var VIEWS = ['generic'];
  function setHeader(title, crumbHtml){
    var t = document.getElementById('pageTitle'); if (t) t.textContent = title;
    var c = document.getElementById('crumbs'); if (c) c.innerHTML = crumbHtml;
  }
  function showView(name){
    VIEWS.forEach(function(v){ var el = document.getElementById('view-' + v); if (el) el.hidden = (v !== name); });
    document.querySelector('.content').scrollTop = 0;
  }
  function navActive(label){
    document.querySelectorAll('.nav-item').forEach(function(n){
      var s = n.querySelector('span'); n.classList.toggle('active', !!s && s.textContent.trim() === label);
    });
  }
  function showToast(msg){
    var t = document.createElement('div');
    t.style.cssText = 'position:fixed;bottom:24px;left:50%;transform:translateX(-50%);z-index:300;background:var(--ink);color:#fff;padding:12px 20px;border-radius:10px;font-size:13px;font-weight:600;box-shadow:0 12px 30px -8px rgba(18,43,78,.5);display:flex;align-items:center;gap:9px;max-width:90vw';
    t.innerHTML = '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="#8fd0a5" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg><span>' + esc(msg) + '</span>';
    document.body.appendChild(t);
    setTimeout(function(){ t.style.transition = 'opacity .4s'; t.style.opacity = '0'; setTimeout(function(){ t.remove(); }, 400); }, 2600);
  }
  function pill(t, c){ return '<span class="status" style="background:color-mix(in srgb,' + c + ' 15%,transparent);color:' + c + '"><span class="sd" style="background:' + c + '"></span>' + esc(t) + '</span>'; }
  var GOLD = 'var(--gold)', NAVY = 'var(--ink)', GREEN = '#3f8f5f', AMBER = '#d9a441', RED = '#d9694f', GRAY = '#8a8d96';
  var STAT_ACC = [['gold','var(--gold)','var(--gold-d)'],['','#d9a441','#8a5e14'],['','var(--ink)','var(--ink)'],['','#3f8f5f','#2f7a4d']];
  var STAT_ICON = '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v18h18"/><path d="M18 17V9M13 17V5M8 17v-3"/></svg>';
  function taskLive(){ return NOW + (Date.now() - BOOT); }
  function isoFromTs(ts){ var d=new Date(ts); return d.getFullYear()+'-'+pad2(d.getMonth()+1)+'-'+pad2(d.getDate()); }
  var INP_STYLE='font-family:inherit;padding:9px 12px;border:1px solid var(--border-2);border-radius:9px;background:var(--surface-2);color:var(--text);font-size:13px;outline:none';
  function engInfo(txt,variant){ var c=variant==='amber'?['#fef3d7','#fad7a0','#7a5b12']:variant==='red'?['color-mix(in srgb,#d9694f 9%,transparent)','color-mix(in srgb,#d9694f 30%,transparent)','#a5432e']:['color-mix(in srgb,var(--gold) 8%,transparent)','color-mix(in srgb,var(--gold) 28%,transparent)','var(--text-2)'];
    return '<div style="background:'+c[0]+';border:1px solid '+c[1]+';color:'+c[2]+';border-radius:8px;padding:9px 12px;font-size:11.5px;line-height:1.7;margin-bottom:12px">'+txt+'</div>'; }

  /* ══ [أُعيد بناؤه بعد حذف عرضي] وحدة المالية والفوترة — الإيرادات (فوترة مركز التصفية) + التكاليف (مستحقات المستحقين) ══ */
  var COST_ST={ due:{t:'مستحق',c:NAVY}, instmt:{t:'في مستند صرف',c:GOLD}, paid:{t:'مدفوع',c:GREEN} };
  var CSTMT_ST={ draft:{t:'مسودة',c:GRAY}, held:{t:'مسير مُعد — لدى المالية',c:NAVY}, awaiting_inv:{t:'بانتظار فاتورة المورّد',c:'#6b4bb3'}, inv_received:{t:'فاتورة واردة — بانتظار المطابقة',c:'#0f766e'}, issued:{t:'أمر صرف — بانتظار الصرف',c:GOLD}, paid:{t:'مدفوع',c:GREEN}, cancelled:{t:'ملغى',c:GRAY} };
  var COST_LINES=[
    {id:'c1', deed:'20911334455', region:'جدة — البوادي', po:'PO-2026-0009', at:'2026-06-29', payee:'مكتب الديار للمساحة', list:1800, amount:1800, ded:0, dedReason:'', st:'due'},
    {id:'c2', deed:'45118822993', region:'جدة — الحمدانية', po:'PO-2026-0006', at:'2026-06-27', payee:'مكتب الديار للمساحة', list:2000, amount:1800, ded:200, dedReason:'خصم إشرافي', st:'due'},
    {id:'c3', deed:'55220148890', region:'مكة — الشوقية', po:'PO-2026-0012', at:'2026-06-28', payee:'مكتب رواسي المساحة', list:2200, amount:2200, ded:0, dedReason:'', st:'due'},
    {id:'c4', deed:'90332157781', region:'مكة — النسيم', po:'PO-2026-0002', taskRef:'TSK-2026-0184', taskTitle:'مراجعة حكومية — كتابة العدل', at:'2026-06-12', payee:'المراجع الحكومي — أحمد الحربي', list:800, amount:800, ded:0, dedReason:'', st:'due'},
    {id:'c5', deed:'6120844770', region:'مكة — العزيزية', po:'PO-2026-0001', at:'2026-06-14', payee:'م. خالد الغامدي', list:1200, amount:1200, ded:0, dedReason:'', st:'paid'},
    {id:'c6', deed:'88455112230', region:'جدة — الصفا', po:'PO-2026-0003', at:'2026-06-25', payee:'م. سارة القحطاني', list:1200, amount:1200, ded:0, dedReason:'', st:'due'},
    {id:'c11', deed:'33011927744', region:'الطائف — الشفا', po:'PO-2026-0008', at:'2026-06-05', payee:'مكتب الديار للمساحة', list:1800, amount:1800, ded:0, dedReason:'', st:'instmt'},
    {id:'c12', deed:'44190028870', region:'جدة — الروضة', po:'PO-2026-0011', at:'2026-06-10', payee:'م. سارة القحطاني', list:1200, amount:1200, ded:0, dedReason:'', st:'instmt'},
    {id:'c13', deed:'80221193355', region:'مكة — العوالي', po:'PO-2026-0015', at:'2026-06-20', payee:'مكتب الديار للمساحة', list:1800, amount:1800, ded:0, dedReason:'', st:'due'},
    {id:'c7', deed:'30119284471', region:'مكة — الشرائع', po:'PO-2026-0005', taskRef:'TSK-2026-0171', taskTitle:'زيارة محكمة التنفيذ', at:'2026-06-18', payee:'ماجد العتيبي', list:600, amount:600, ded:0, dedReason:'', st:'due'},
    {id:'c8', deed:'77410025518', region:'الطائف — النوارية', po:'PO-2026-0014', taskRef:'TSK-2026-0166', taskTitle:'زيارة محكمة — استلام ظرف', at:'2026-06-26', payee:'فهد الزهراني', list:600, amount:600, ded:0, dedReason:'', st:'paid'},
    {id:'c9', deed:'66102938844', region:'جدة — النزهة', po:'PO-2026-0022', payee:'مكتب الديار للمساحة', list:1800, amount:0, ded:1800, dedReason:'خصم كامل — لم يُعتمد الرفع', st:'excluded', exReason:'مخصومة بالكامل — سُوّيت بين المشرف والمكتب قبل الاستحقاق'},
    {id:'c10', deed:'52209911037', region:'مكة — الششة', po:'PO-2026-0024', payee:'مكتب رواسي المساحة', list:2200, amount:0, ded:2200, dedReason:'', st:'excluded', exReason:'ملغاة — أُلغيت المعاملة من منصة إنفاذ'}
  ];
  var COST_STMTS=[
    {id:'cs1', ref:'CS-COST-260710-101', party:'val', ptype:'individual', payee:'م. خالد الغامدي', status:'paid', at:'2026/07/12', inv:'PV-8801', trf:'TRF-220145', receipt:'إيصال_تحويل_2026-07-14.pdf', paidAt:'2026/07/14', audit:[{act:'تجهيز المستند (1 بند)',by:'سليمان القحطاني — المالية',at:'2026/07/10'},{act:'اعتماد وإصدار أمر الصرف',by:'سليمان القحطاني — المالية',at:'2026/07/12'},{act:'توثيق الصرف — سند PV-8801 · تحويل TRF-220145',by:'سليمان القحطاني — المالية',at:'2026/07/14'}], lines:[{id:'c5', deed:'6120844770', region:'مكة — العزيزية', amount:1200}]},
    {id:'cs2', ref:'CS-COST-260718-102', party:'visit', ptype:'individual', payee:'فهد الزهراني', status:'paid', at:'2026/07/19', inv:'PV-8830', trf:'TRF-220990', receipt:'إيصال_تحويل_2026-07-21.pdf', paidAt:'2026/07/21', audit:[{act:'تجهيز المستند (1 بند)',by:'سليمان القحطاني — المالية',at:'2026/07/18'},{act:'اعتماد وإصدار أمر الصرف',by:'سليمان القحطاني — المالية',at:'2026/07/19'},{act:'توثيق الصرف — سند PV-8830 · تحويل TRF-220990',by:'سليمان القحطاني — المالية',at:'2026/07/21'}], lines:[{id:'c8', deed:'77410025518', region:'الطائف — النوارية', taskRef:'TSK-2026-0166', taskTitle:'زيارة محكمة — استلام ظرف', amount:600}]},
    {id:'cs3', ref:'CS-COST-260726-103', party:'eng', ptype:'vendor', payee:'مكتب الديار للمساحة', status:'awaiting_inv', at:'2026/07/26', lines:[{id:'c11', deed:'33011927744', region:'الطائف — الشفا', po:'PO-2026-0008', amount:1800}]},
    {id:'cs4', ref:'CS-COST-260728-104', party:'val', ptype:'individual', payee:'م. سارة القحطاني', status:'issued', at:'2026/07/28', lines:[{id:'c12', deed:'44190028870', region:'جدة — الروضة', po:'PO-2026-0011', amount:1200}]}
  ];
  var REV_VAT=0.15;
  var REV_CEIL={ land:10000, building:20000 };
  var REV_ST={ inprogress:{t:'قيد العمل',c:'#4b6b8a'}, eligible:{t:'بانتظار المطابقة',c:NAVY}, awaiting:{t:'بانتظار رفع الفاتورة',c:'#6b4bb3'}, invoiced:{t:'بانتظار التحصيل',c:GOLD}, collected:{t:'محصّلة',c:GREEN}, excluded:{t:'متوقفة',c:GRAY} };
  var REV_OP={ 'مرفوعة على إنفاذ':'#2f7a4d', 'تحت الإجراء':NAVY, 'قيد المعاينة':'#0f766e', 'قيد الرفع المساحي':'#0f766e', 'تعذر مؤقت':'#8a5e14', 'معلقة':'#6b4bb3' };
  var REV_CH={ support:'منصة الدعم', email:'بريد', call:'اتصال', other:'أخرى' };
  var REV_TOTAL_N=412;
  var REV_TX=[
    {id:'r1', hasSurvey:true, hasKey:false, land:32036.15, bld:1275, po:'PO-2026-0009', deed:'20911334455', city:'جدة',   doneAt:'2026-06-29', valFee:12000, keyFee:0,   survFee:2500, status:'eligible'},
    {id:'r2', hasSurvey:true, hasKey:false, land:1250, bld:420, po:'PO-2026-0012', deed:'55220148890', city:'مكة',   doneAt:'2026-06-28', valFee:12000, keyFee:0,   survFee:3800, status:'eligible'},
    {id:'r3', hasSurvey:true, hasKey:true, land:880, bld:0, po:'PO-2026-0006', deed:'45118822993', city:'جدة',   doneAt:'2026-06-27', valFee:10000, keyFee:0,   survFee:2500, status:'eligible'},
    {id:'r4', hasSurvey:true, hasKey:true, land:32036.15, bld:1275, po:'PO-2026-0014', deed:'77410025518', city:'الطائف', doneAt:'2026-06-26', valFee:12000, keyFee:250, survFee:3500, status:'awaiting'},
    {id:'r5', hasSurvey:true, hasKey:false, land:1450, bld:610, po:'PO-2026-0003', deed:'88455112230', city:'جدة',   doneAt:'2026-06-25', valFee:12000, keyFee:0,   survFee:2500, status:'awaiting'},
    {id:'r6', hasSurvey:true, hasKey:false, land:2100, bld:980, po:'PO-2026-0003', deed:'61203988465', city:'الرياض', doneAt:'2026-06-24', valFee:20000, keyFee:0,   survFee:3800, status:'awaiting'},
    {id:'r7', po:'PO-2026-0004', deed:'30119284471', city:'الطائف', doneAt:'2026-06-18', valFee:12000, keyFee:250, survFee:3500, status:'invoiced', inv:{no:'C-INV-10231', date:'2026-06-27', uploadedAt:'2026-06-28'}, followups:[]},
    {id:'r8', po:'PO-2026-0002', deed:'90332157781', city:'مكة',   doneAt:'2026-06-12', valFee:12000, keyFee:0,   survFee:3800, status:'invoiced', inv:{no:'C-INV-10198', date:'2026-06-19', uploadedAt:'2026-06-20'}, followups:[{date:'2026-06-27', channel:'support', notes:'رفع تذكرة على منصة الدعم برقم المعاملة.'}]},
    {id:'r9', po:'PO-2026-0007', deed:'50228841190', city:'الرياض', doneAt:'2026-06-27', valFee:10000, keyFee:0,   survFee:2500, status:'invoiced', inv:{no:'C-INV-10165', date:'2026-06-14', uploadedAt:'2026-06-15', po:'PO-2026-0007'}, followups:[]},
    {id:'r10',po:'PO-2026-0007', deed:'77120033114', city:'جدة',   doneAt:'2026-06-09', valFee:12000, keyFee:0,   survFee:2500, status:'invoiced', inv:{no:'C-INV-10165', date:'2026-06-14', uploadedAt:'2026-06-15', po:'PO-2026-0007'}, followups:[{date:'2026-06-22', channel:'call', notes:'اتصال بمسؤول الحساب — أفاد بالإدراج ضمن دفعة قادمة.'},{date:'2026-06-29', channel:'email', notes:'تذكير عبر البريد.'}]},
    {id:'r11',po:'PO-2026-0001', deed:'6120844770',  city:'مكة',   doneAt:'2026-06-14', valFee:12000, keyFee:0,   survFee:3800, status:'collected', inv:{no:'C-INV-9987', date:'2026-06-25', uploadedAt:'2026-06-26'}, pay:{date:'2026-06-30', amount:18170, ref:'TRF-556677'}, followups:[]},
    {id:'r12',po:'PO-2026-0011', deed:'44190028870', city:'جدة',   doneAt:'2026-06-05', valFee:10000, keyFee:0,   survFee:2500, status:'collected', inv:{no:'C-INV-9954', date:'2026-06-12', uploadedAt:'2026-06-13'}, pay:{date:'2026-06-24', amount:14375, ref:'TRF-551200'}, followups:[]},
    {id:'r13',po:'PO-2026-0008', deed:'33011927744', city:'الطائف', doneAt:'2026-05-30', valFee:12000, keyFee:250, survFee:3500, status:'collected', inv:{no:'C-INV-9931', date:'2026-06-04', uploadedAt:'2026-06-05'}, pay:{date:'2026-06-18', amount:18075, ref:'TRF-548890'}, followups:[]},
    {id:'r14',po:'PO-2026-0015', deed:'80221193355', city:'مكة',   doneAt:'2026-06-20', status:'excluded', exReason:'متعذّرة — تعذّر إتمام المعاينة الميدانية'},
    {id:'r15',po:'PO-2026-0019', deed:'19023384410', city:'الرياض', doneAt:'2026-06-23', status:'excluded', exReason:'ملغاة — سُحب الطلب من منصة إنفاذ'},
    {id:'r16',po:'PO-2026-0025', deed:'71029384412', city:'جدة',   regAt:'2026-06-30', doneAt:'2026-06-30', hasSurvey:true, hasKey:false, land:1120, bld:340, valFee:10000, keyFee:0, survFee:2500, status:'inprogress', opState:'مرفوعة على إنفاذ'},
    {id:'r17',po:'PO-2026-0026', deed:'88301245567', city:'مكة',   regAt:'2026-06-29', status:'inprogress', opState:'قيد المعاينة'},
    {id:'r18',po:'PO-2026-0027', deed:'50912837744', city:'الرياض', regAt:'2026-06-28', status:'inprogress', opState:'قيد الرفع المساحي'},
    {id:'r19',po:'PO-2026-0028', deed:'33810029915', city:'الطائف', regAt:'2026-06-25', status:'inprogress', opState:'تعذر مؤقت'},
    {id:'r20',po:'PO-2026-0029', deed:'19928374410', city:'جدة',   regAt:'2026-06-21', status:'inprogress', opState:'معلقة'}
  ];
  var FINX={ tab:'revenue', party:null, dq:'', q:'', st:'', csel:{}, cstmt:null, cclose:null, cvinv:null, cauditOpen:false, rsel:{}, rcol:{}, ctab:'open', rtab:'eligible', rq:'', rcity:'', rperiod:'' };
  var REVDRAFT=null;
  function revBase(t){ return (t.valFee||0)+(t.survFee||0); }
  function revVat(t){ return Math.round(revBase(t)*REV_VAT*100)/100; }
  function revKey(t){ return (t.keyFee||0); }
  function revSub(t){ return revBase(t); }
  function revGross(t){ return revBase(t)+revVat(t)+revKey(t); }
  function revDate(iso){ if(!iso) return '—'; var p=String(iso).split('-'); return p[2]+'/'+p[1]+'/'+p[0]; }
  function revAge(iso){ if(!iso) return 0; return Math.max(0, Math.floor((taskLive()-new Date(iso+'T00:00:00').getTime())/DAY)); }
  function revLastFollow(t){ if(!t.followups||!t.followups.length) return null; return t.followups[t.followups.length-1].date; }
  function revInvExists(no,exceptId){ no=(no||'').trim(); if(!no) return false; return REV_TX.some(function(t){ return t.id!==exceptId && t.inv && t.inv.no===no; }); }
  function revPeriodDate(t){ return t.status==='collected'&&t.pay?t.pay.date:(t.doneAt||t.regAt); }
  function revInPeriod(iso,per){ if(!per) return true; var days=per==='30'?30:90; var d=(taskLive()-new Date(iso+'T00:00:00').getTime())/DAY; return d>=0 && d<=days; }
  function revFlags(t){
    var ic=function(on,title,path,col){ var c=on?col:'var(--border-2)';
      return '<span title="'+title+(on?'':' — غير مفعّل')+'" style="display:inline-grid;place-items:center;flex:none;color:'+c+'"><svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">'+path+'</svg></span>'; };
    return '<span style="display:inline-flex;gap:5px;margin-inline-start:6px">'+
      ic(!!t.hasKey,'أتعاب استلام مفتاح','<circle cx="7.5" cy="15.5" r="3.5"/><path d="M10 13 20 3"/><path d="M16.5 6.5 19 9"/><path d="M14.5 8.5 17 11"/>','#b45309')+
      ic(!!t.hasSurvey,'أتعاب مكتب هندسي — رفع مساحي','<path d="M4 4v16h16"/><path d="M4 20 20 4"/><path d="M8 20v-3"/><path d="M12 20v-3"/><path d="M16 20v-3"/>','#1f3a5f')+
    '</span>';
  }
  function revPromote(){
    REV_TX.forEach(function(t){
      if(t.status==='inprogress' && (t.opState==='مرفوعة على إنفاذ' || t.uploadedInfath)){
        t.status='eligible'; t.doneAt=t.doneAt||t.regAt; delete t.opState;
      }
    });
  }
  function revGroups(status){
    var m={}, out=[];
    revList(status).forEach(function(t){ var k=(status==='invoiced'&&t.inv)?t.inv.no:t.po; if(!m[k]){ m[k]={po:k, inv:t.inv||null, txs:[], total:0}; out.push(m[k]); } m[k].key=k; m[k].txs.push(t); m[k].total+=revGross(t); });
    return out;
  }
  function revList(status){ var q=(FINX.rq||'').trim(), city=FINX.rcity, per=FINX.rperiod;
    return REV_TX.filter(function(t){ if(t.status!==status) return false; if(city&&t.city!==city) return false;
      if(q){ var hit=t.po.indexOf(q)>-1||t.deed.indexOf(q)>-1||(t.inv&&t.inv.no.indexOf(q)>-1); if(!hit) return false; }
      if(per&&!revInPeriod(revPeriodDate(t),per)) return false; return true; });
  }
  function finChk(on){ return '<span style="flex:none;width:17px;height:17px;border-radius:5px;border:2px solid '+(on?'var(--ink)':'var(--border-2)')+';background:'+(on?'var(--ink)':'var(--surface)')+';display:grid;place-items:center">'+(on?'<svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="3.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>':'')+'</span>'; }
  function finInfo(html){ return '<div style="display:flex;gap:10px;background:var(--surface-2);border:1px solid var(--border);border-radius:10px;padding:11px 14px;font-size:12px;line-height:1.7;color:var(--text-2);margin-bottom:14px"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" style="flex:none;margin-top:1px;color:var(--ink)"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg><div>'+html+'</div></div>'; }
  function finEmpty(t){ return '<div class="card"><div class="empty"><svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg><div class="t">'+t+'</div></div></div>'; }
  var FIN_SEL_STYLE='appearance:none;border:1px solid var(--border-2);background:var(--surface);border-radius:9px;padding:9px 12px;font-size:12.5px;font-family:inherit;color:var(--text);cursor:pointer';
  var FIN_ROLE={ eng:{t:'المكتب الهندسي',c:'#1f3a5f'}, gov:{t:'المراجع الحكومي',c:'#6b4bb3'}, val:{t:'المقيّم',c:'#0f766e'}, visit:{t:'منفّذ مهمة',c:'#b45309'}, insp:{t:'المعاين',c:'#7C3AED'}, client:{t:'عميل — إيرادات',c:'#2f7a4d'} };
  var FIN_PARTIES=[
    {id:'p0', code:'CLI-001', kind:'client', role:'client', name:'مركز الإسناد والتصفية', iban:'SA44 8000 0000 6080 1000 7519', phone:'011 200 4000'},
    {id:'p1', code:'ENG-001', kind:'cost', role:'eng', ptype:'vendor', name:'مكتب الديار للمساحة', iban:'SA03 8000 0000 6080 1016 4471', phone:'0555 100 200'},
    {id:'p2', code:'ENG-002', kind:'cost', role:'eng', ptype:'vendor', name:'مكتب رواسي المساحة', iban:'SA21 8000 0000 6080 1122 8890', phone:'0555 330 410'},
    {id:'p3', code:'GOV-001', kind:'cost', role:'gov', ptype:'individual', name:'المراجع الحكومي — أحمد الحربي', iban:'SA55 8000 0000 6080 1447 1122', phone:'0500 771 900'},
    {id:'p4', code:'APR-001', kind:'cost', role:'val', ptype:'individual', name:'م. خالد الغامدي', iban:'SA18 8000 0000 6080 1990 3321', phone:'0503 220 118'},
    {id:'p5', code:'APR-002', kind:'cost', role:'val', ptype:'individual', name:'م. سارة القحطاني', iban:'SA72 8000 0000 6080 1552 0148', phone:'0568 440 210'},
    {id:'p6', code:'VIS-001', kind:'cost', role:'visit', ptype:'individual', name:'ماجد العتيبي', iban:'SA09 8000 0000 6080 1884 5511', phone:'0533 900 771'},
    {id:'p7', code:'VIS-002', kind:'cost', role:'visit', ptype:'individual', name:'فهد الزهراني', iban:'SA61 8000 0000 6080 1301 1928', phone:'0541 700 553'},
    {id:'p8', code:'INS-001', kind:'cost', role:'insp', ptype:'individual', trackDues:false, name:'عبدالله الشهري — معاين', iban:'SA37 8000 0000 6080 1774 2200', phone:'0555 640 118'}
  ];
  var FIN_PTYPE={ vendor:{t:'مورّد',c:'#1f3a5f'}, individual:{t:'فرد',c:'#0f766e'} };
  function finPtype(p){ return p.ptype||'vendor'; }
  function finDocName(p){ return finPtype(p)==='vendor'?'مسير صرف':'أمر صرف'; }
  function finDocNameS(s){ return (s.ptype||'vendor')==='vendor'?'مسير الصرف':'أمر الصرف'; }
  function finBlankVinv(){ return { no:'', date:isoFromTs(taskLive()), matched:false, file:false, manual:false, reason:'' }; }
  var COST_VAT=0.15;
  var FIN_ACTOR='سليمان القحطاني — المالية';
  function costLog(s,act,by){ s.audit=s.audit||[]; s.audit.push({act:act, by:by||FIN_ACTOR, at:isoFromTs(taskLive()).split('-').reverse().join('/')}); }
  function stmtIsVendor(s){ return (s.ptype||'vendor')==='vendor'; }
  function stmtNet(s){ return (s.lines||[]).reduce(function(a,l){ return a+(l.amount!=null?l.amount:(l.net||0)); },0); }
  function stmtVat(s){ return stmtIsVendor(s)?Math.round(stmtNet(s)*COST_VAT*100)/100:0; }
  function costTotal(s){ return stmtNet(s)+stmtVat(s); }
  function costFactor(p){ return finPtype(p)==='vendor'?(1+COST_VAT):1; }
  function costVoucherExists(no,exceptId){ no=(no||'').trim(); if(!no) return false; return COST_STMTS.some(function(s){ return s.id!==exceptId && s.inv && s.inv===no; }); }
  function costTotalsBox(s){
    var net=stmtNet(s), vat=stmtVat(s);
    var r=function(l,v,b){ return '<div style="display:flex;justify-content:space-between;padding:8px 13px;'+(b?'border-top:1px solid var(--border);background:var(--surface-2)':'')+'"><span style="font-size:'+(b?'12.5':'12')+'px;font-weight:'+(b?'800':'500')+';color:'+(b?'var(--heading)':'var(--text-2)')+'">'+l+'</span><span style="font-size:'+(b?'13.5':'12.5')+'px;font-weight:700;color:var(--heading)">'+fmtSAR(v)+'</span></div>'; };
    return '<div style="border:1px solid var(--border);border-radius:10px;overflow:hidden;margin-top:12px">'+r('صافي الأتعاب',net)+r('ضريبة القيمة المضافة (15%)',vat)+r('الإجمالي — تُطابقه فاتورة المورّد',net+vat,true)+'</div>';
  }
  function revByDeed(d){ return REV_TX.find(function(t){ return t.deed===d; }); }
  function costGate(l){
    if(!(l.amount>0)) return {ok:false, kind:'zero', t:'صافي صفر — يُقفل بتسوية'};
    return {ok:true};
  }
  function costRefMain(l){ return l.taskRef||l.deed||'—'; }
  function costRefSub(l){ return l.taskRef?(l.taskTitle||'مهمة'):((l.region||'')+(l.po?' · '+l.po:'')); }
  function finCur(){ return FIN_PARTIES.find(function(x){ return x.id===FINX.party; }); }
  function finPartyBal(p){
    if(p.kind==='client'){ var b=REV_TX.filter(function(t){ return t.status==='eligible'||t.status==='awaiting'||t.status==='invoiced'; }).reduce(function(a,t){ return a+revGross(t); },0); var pn=REV_TX.filter(function(t){ return t.status==='eligible'||t.status==='awaiting'; }).length; return {bal:b,pending:pn}; }
    if(p.trackDues===false) return {bal:null,pending:null};
    var ls=COST_LINES.filter(function(l){ return l.payee===p.name; }); var b=Math.round(ls.filter(function(l){ return l.st==='due'||l.st==='instmt'; }).reduce(function(a,l){ return a+l.amount; },0)*costFactor(p)*100)/100; var pn=ls.filter(function(l){ return l.st==='due'; }).length; return {bal:b,pending:pn};
  }
  function finCostList(p){ var q=(FINX.q||'').trim(), st=FINX.st; return COST_LINES.filter(function(l){ if(l.payee!==p.name) return false; if(st){ if(l.st!==st) return false; } else if(l.st==='paid') return false; if(q&&l.deed.indexOf(q)<0&&l.region.indexOf(q)<0&&l.po.indexOf(q)<0) return false; return true; }); }
  function finAcctHeader(p, balLabel, balVal, minis){
    var role=FIN_ROLE[p.role]; var pt=(p.kind==='cost')?FIN_PTYPE[finPtype(p)]:null;
    var mini=function(v,l){ return '<div style="display:flex;flex-direction:column;gap:1px"><span style="font-size:13px;font-weight:800;color:var(--heading);line-height:1.2">'+fmtSAR(v)+'</span><span style="font-size:9.5px;color:var(--text-3)">'+l+'</span></div>'; };
    return '<div class="card" style="padding:10px 14px;margin-bottom:10px;display:flex;align-items:center;gap:14px;flex-wrap:wrap">'+
      '<div style="display:flex;align-items:center;gap:8px;min-width:0;flex-wrap:wrap"><span style="font-size:14.5px;font-weight:800;color:var(--heading)">'+esc(p.name)+'</span>'+pill(role.t,role.c)+(pt?pill(pt.t,pt.c):'')+'<span style="font-size:10px;color:var(--text-3)" dir="ltr">'+esc(p.code)+' · '+esc(p.iban)+'</span></div>'+
      ((minis&&minis.length)||balVal!=null?'<div style="display:flex;align-items:center;gap:16px;margin-inline-start:auto;flex-wrap:wrap">'+(minis||[]).map(function(m){ return mini(m[0],m[1]); }).join('')+(balVal!=null?'<div style="display:flex;flex-direction:column;gap:1px;padding-inline-start:16px;border-inline-start:1px solid var(--border)"><span style="font-size:16px;font-weight:800;color:var(--heading);line-height:1.2">'+fmtSAR(balVal)+'</span><span style="font-size:9.5px;color:var(--text-3)">'+balLabel+'</span></div>':'')+'</div>':'')+
    '</div>';
  }
  function finAcctFilters(){
    return '<div style="display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-bottom:12px">'+
      '<input data-fx-q value="'+esc(FINX.q||'')+'" placeholder="بحث في بنود هذا المورّد" style="'+INP_STYLE+';flex:1;min-width:200px">'+
      '<select data-fx-st style="'+FIN_SEL_STYLE+';min-width:150px"><option value="">غير المكتمل</option><option value="due"'+(FINX.st==='due'?' selected':'')+'>مستحق</option><option value="instmt"'+(FINX.st==='instmt'?' selected':'')+'>في كشف</option><option value="paid"'+(FINX.st==='paid'?' selected':'')+'>مدفوع</option></select>'+
    '</div>';
  }
  function finInboxItems(){
    var it=[];
    revList('eligible').forEach(function(t){ it.push({g:'الإيرادات',act:'مطابقة الأتعاب',ref:t.po,sub:t.deed,amt:revGross(t),go:'rev:eligible'}); });
    revList('awaiting').forEach(function(t){ it.push({g:'الإيرادات',act:'تسجيل الفاتورة ورفعها على إنفاذ',ref:t.po,sub:t.deed,amt:revGross(t),go:'rev:awaiting'}); });
    revGroups('invoiced').forEach(function(g){ var iv=g.txs[0].inv||{}; it.push({g:'الإيرادات',act:'تسجيل التحويل عند وروده ('+g.txs.length+' معاملة)',ref:iv.no||g.po,sub:g.po,amt:g.total,go:'rev:invoiced',age:revAge(iv.uploadedAt)}); });
    COST_STMTS.forEach(function(s){
      var pp=FIN_PARTIES.find(function(x){ return x.name===s.payee; }); if(!pp) return;
      var a=null;
      if(s.status==='held') a='تحويل المسير للمكتب لإصدار الفاتورة';
      else if(s.status==='inv_received') a='مطابقة فاتورة المورّد وقيدها';
      else if(s.status==='issued') a='توثيق الصرف ورفع إيصال التحويل';
      if(a) it.push({g:'التكاليف',act:a,ref:s.ref,sub:s.payee,amt:costTotal(s),go:'cost:'+pp.id+':'+s.id});
    });
    var byPayee={};
    COST_LINES.forEach(function(l){ if(l.st!=='due'||!costGate(l).ok) return;
      var pp=FIN_PARTIES.find(function(x){ return x.name===l.payee; }); if(!pp||pp.trackDues===false||finPtype(pp)==='vendor') return;
      (byPayee[pp.id]=byPayee[pp.id]||{p:pp,n:0,sum:0}); byPayee[pp.id].n++; byPayee[pp.id].sum+=l.amount; });
    Object.keys(byPayee).forEach(function(k){ var b=byPayee[k];
      it.push({g:'التكاليف',act:'إصدار أمر صرف ('+b.n+' بند)',ref:b.p.code,sub:b.p.name,amt:b.sum,go:'cost:'+b.p.id+':open'}); });
    return it;
  }
  function finInbox(){
    var items=finInboxItems();
    var cols='minmax(200px,1.7fr) minmax(120px,1fr) minmax(150px,1.2fr) minmax(105px,.85fr) 92px';
    var rows=items.map(function(i){
      var c=i.g==='الإيرادات'?'#2f7a4d':'#1f3a5f';
      return '<div class="row" data-fx-go="'+i.go+'" role="button" style="grid-template-columns:'+cols+';min-height:48px;cursor:pointer">'+
        '<div class="td"><div style="display:flex;flex-direction:column;gap:2px"><span style="font-size:12.5px;font-weight:700;color:var(--heading)">'+esc(i.act)+'</span><span style="font-size:10px;color:'+c+';font-weight:700">'+i.g+(i.age!=null?' · منذ '+i.age+' يوماً':'')+'</span></div></div>'+
        '<div class="td" dir="ltr" style="font-size:12px;font-weight:700;color:var(--gold-d)">'+esc(i.ref)+'</div>'+
        '<div class="td" style="font-size:11.5px;color:var(--text-2)">'+esc(i.sub)+'</div>'+
        '<div class="td" style="font-weight:700;font-size:12.5px;color:var(--heading)">'+fmtSAR(i.amt)+'</div>'+
        '<div class="td"><span style="font-size:11px;font-weight:700;color:var(--ink)">فتح ›</span></div>'+
      '</div>'; }).join('');
    return finInfo('كل ما يتطلب إجراءً من المالية في مكان واحد — إيرادات وتكاليف. اضغط الصف للانتقال إلى موضعه.')+
      '<div class="card"><div class="scroll" style="max-height:calc(100vh - 250px);overflow:auto"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+cols+';position:sticky;top:0;z-index:3"><div class="th">الإجراء المطلوب</div><div class="th">المرجع</div><div class="th">الجهة / المعاملة</div><div class="th">المبلغ</div><div class="th"></div></div><div>'+(rows||'')+'</div></div></div></div>'+
      (items.length?'':finEmpty('لا إجراءات معلّقة — كل شيء مُحدَّث'));
  }
  function finAutoRuns(){
    var curM=isoFromTs(taskLive()).slice(0,7);
    FIN_PARTIES.forEach(function(p){
      if(p.kind!=='cost'||p.trackDues===false||finPtype(p)!=='vendor') return;
      var byMonth={};
      COST_LINES.forEach(function(l){
        if(l.payee!==p.name||l.st!=='due'||!(l.amount>0)||l.taskRef) return;
        var m=(l.at||'').slice(0,7); if(!m||m>=curM) return;
        (byMonth[m]=byMonth[m]||[]).push(l);
      });
      Object.keys(byMonth).sort().forEach(function(m){
        if(COST_STMTS.some(function(s){ return s.payee===p.name&&s.period===m; })) return;
        var picked=byMonth[m], at=m+'-30';
        var ref='CS-COST-'+m.slice(2).replace('-','')+'-A'+String(101+COST_STMTS.length).slice(1);
        var st={ id:'cs'+Date.now()+Math.floor(Math.random()*900), ref:ref, party:p.role, ptype:'vendor', payee:p.name,
          status:'held', period:m, auto:true, at:at.split('-').reverse().join('/'),
          lines:picked.map(function(l){ return {id:l.id,deed:l.deed,region:l.region,po:l.po,taskRef:l.taskRef,taskTitle:l.taskTitle,amount:l.amount,list:(l.list!=null?l.list:l.amount),ded:l.ded||0,dedReason:l.dedReason||''}; }),
          audit:[{act:'إنشاء آلي لمسير نهاية الشهر '+m+' ('+picked.length+' معاملة)',by:'النظام',at:at.split('-').reverse().join('/')}] };
        COST_STMTS.unshift(st);
        picked.forEach(function(l){ l.st='instmt'; });
      });
    });
  }
  function finCostAccount(p){
    if(!FINX.ctab) FINX.ctab='open';
    if(p.trackDues===false) return finAcctHeader(p,null,null,[])+finInfo('متابعة مستحقات هذا المستحق <b>معطّلة على هذا النظام</b> من ملف المستخدم — تُدار أتعابه في برنامج التقييم العقاري. لا تُسجَّل له مستحقات أو مستندات صرف هنا.');
    var all=COST_LINES.filter(function(l){ return l.payee===p.name; });
    var dueItems=all.filter(function(l){ return l.st==='due'; });
    var due=dueItems.reduce(function(a,l){ return a+l.amount; },0);
    var inSt=all.filter(function(l){ return l.st==='instmt'; }).reduce(function(a,l){ return a+l.amount; },0);
    var mine=COST_STMTS.filter(function(s){ return s.payee===p.name; });
    var orders=mine.filter(function(s){ return s.status!=='paid'&&s.status!=='cancelled'; });
    var paidStmts=mine.filter(function(s){ return s.status==='paid'; });
    var paid=paidStmts.reduce(function(a,s){ return a+s.lines.reduce(function(x,l){ return x+l.amount; },0); },0);
    var header=finAcctHeader(p,'مستحق له الآن',Math.round((due+inSt)*costFactor(p)*100)/100,[[Math.round(due*costFactor(p)*100)/100,'مستحق — لم يُسجَّل'],[Math.round(inSt*costFactor(p)*100)/100,(finPtype(p)==='vendor'?'في مسيرات صرف':'في أوامر صرف')],[Math.round(paid*costFactor(p)*100)/100,'مدفوع سابقاً']]);
    var readyN=dueItems.filter(function(l){ return l.amount>0&&costGate(l).ok; }).length;
    var tabs=[['open','المستحقات',readyN],['orders',(finPtype(p)==='vendor'?'مسيرات وأوامر صرف':'أوامر الصرف'),orders.length],['paid','مدفوعة',paidStmts.length],['excluded','مستبعدة',all.filter(function(l){ return l.st==='excluded'; }).length]];
    var strip='<div style="display:flex;flex-wrap:wrap;gap:6px;margin-bottom:10px">'+tabs.map(function(tb){ var on=FINX.ctab===tb[0];
      return '<button data-cctab="'+tb[0]+'" style="display:inline-flex;align-items:center;gap:6px;padding:7px 13px;border-radius:99px;border:1px solid '+(on?'var(--ink)':'var(--border-2)')+';background:'+(on?'var(--ink)':'var(--surface)')+';color:'+(on?'#fff':'var(--text-2)')+';font-family:inherit;font-size:12px;font-weight:700;cursor:pointer">'+tb[1]+'<span style="background:'+(on?'rgba(255,255,255,.22)':'var(--surface-2)')+';color:'+(on?'#fff':'var(--text-3)')+';border-radius:99px;min-width:17px;height:17px;display:inline-grid;place-items:center;font-size:10px;padding:0 5px">'+tb[2]+'</span></button>'; }).join('')+'</div>';
    var body;
    if(FINX.ctab==='open'){
      var q=(FINX.q||'').trim();
      var list=dueItems.filter(function(l){ return !q||l.deed.indexOf(q)>-1||l.region.indexOf(q)>-1||(l.po||'').indexOf(q)>-1; });
      list.sort(function(a,b){ var ga=costGate(a).ok?0:1, gb=costGate(b).ok?0:1; if(ga!==gb) return ga-gb; return (a.at||'')<(b.at||'')?-1:1; });
      var sel=dueItems.filter(function(l){ return FINX.csel[l.id]&&costGate(l).ok; }); var selTotal=sel.reduce(function(a,l){ return a+l.amount; },0);
      var cols='50px minmax(150px,1.5fr) minmax(98px,.75fr) minmax(125px,1fr) minmax(85px,.72fr)';
      var Cc='justify-content:center;text-align:center';
      var rows=list.map(function(l){ var on=!!FINX.csel[l.id]; var listP=(l.list!=null?l.list:l.amount); var ded=l.ded||0;
        var g=costGate(l);
        return '<div class="row"'+(g.ok?' data-fx-crow="'+l.id+'" role="button"':'')+' style="grid-template-columns:'+cols+';min-height:46px;'+(g.ok?'cursor:pointer':'opacity:.7')+(on?';background:color-mix(in srgb,var(--ink) 5%,transparent)':'')+'">'+
          '<div class="td">'+(g.ok?finChk(on):'<span style="width:17px;height:17px;border-radius:5px;border:2px dashed var(--border-2);display:inline-block" title="'+esc(g.t)+'"></span>')+'</div>'+
          '<div class="td"><div style="display:flex;flex-direction:column;gap:2px"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px;text-align:end">'+esc(costRefMain(l))+'</span><span style="color:var(--text-3);font-size:11px">'+esc(costRefSub(l))+(g.ok?(l.at?' · <span style="color:'+(revAge(l.at)>30?'#a5432e':'var(--text-3)')+';font-weight:600">منذ '+revAge(l.at)+' يوماً</span>':''):' · <span style="color:'+(g.kind==='excluded'?'#a5432e':'#8a5e14')+';font-weight:700">'+esc(g.t)+'</span>'+(g.kind==='excluded'?' <button data-fx-gateex="'+l.id+'" style="border:none;background:none;padding:0;font-family:inherit;font-size:10.5px;color:var(--ink);text-decoration:underline;cursor:pointer">استثناء المشرف</button>':''))+(g.ex?' · <span style="color:#2f7a4d;font-weight:700">أُفرِج باستثناء المشرف</span>':'')+'</span></div></div>'+
          '<div class="td" style="'+Cc+';font-size:12.5px;color:var(--text-2)">'+fmtSAR(listP)+'</div>'+
          '<div class="td" style="'+Cc+'">'+(ded>0?'<div style="display:flex;flex-direction:column;gap:2px;align-items:center"><span style="font-size:12px;font-weight:700;color:#c0553d">−'+fmtSAR(ded)+'</span><span style="font-size:9.5px;color:var(--text-3);line-height:1.3">'+esc(l.dedReason||'')+'</span></div>':'<span style="color:var(--text-3);font-size:12px">—</span>')+'</div>'+
          '<div class="td" style="'+Cc+';font-weight:700;font-size:13px;color:var(--heading)">'+fmtSAR(l.amount)+'</div>'+
        '</div>';
      }).join('');
      var table='<div class="card"><div class="scroll" style="max-height:calc(100vh - 290px);overflow:auto"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+cols+';position:sticky;top:0;z-index:3"><div class="th"></div><div class="th">المعاملة</div><div class="th">سعر الجدول</div><div class="th">تعديل التسعير</div><div class="th">الصافي</div></div><div>'+(rows||'')+'</div></div></div></div>'+(list.length?'':finEmpty(q?'لا بنود مطابقة للبحث':'لا مستحقات قائمة — كل البنود مُدرجة في مستندات صرف'));
      var selectable=list.filter(function(l){ return l.amount>0&&costGate(l).ok; });
      var allOn=selectable.length&&selectable.every(function(l){ return FINX.csel[l.id]; });
      var toolbar='<div style="display:flex;flex-wrap:wrap;align-items:center;gap:9px;margin-bottom:10px">'+
        '<input data-fx-q value="'+esc(FINX.q||'')+'" placeholder="بحث: رقم الصك · المنطقة · رقم الطلب" style="'+INP_STYLE+';flex:1;min-width:200px">'+
        '<button data-fx-call="1"'+(selectable.length?'':' style="opacity:.5;pointer-events:none"')+' style="padding:9px 14px;font-size:12px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-weight:700;cursor:pointer;font-family:inherit;white-space:nowrap">'+(allOn?'إلغاء التحديد':'تحديد الكل ('+selectable.length+')')+'</button>'+
        '<button class="primary" data-fx-ccreate="1"'+(sel.length?'':' style="opacity:.5;pointer-events:none"')+' style="padding:9px 16px;font-size:12.5px;white-space:nowrap">تجهيز '+finDocName(p)+(sel.length?' ('+sel.length+' — '+fmtSAR(Math.round(selTotal*costFactor(p)*100)/100)+')':'')+'</button>'+
        '<span style="font-size:11px;color:var(--text-3);white-space:nowrap">'+list.length+' مستحق · <b style="color:'+(selectable.length?'var(--heading)':'#8a5e14')+'">'+selectable.length+'</b> جاهز للصرف</span>'+
      '</div>';
      body=toolbar+table;
    } else if(FINX.ctab==='orders'){
      body=finInfo(finPtype(p)==='vendor'?'مسيرات الصرف قيد الإجراء: «بانتظار فاتورة المورّد» أُرسل المسير للمورّد ليرفع فاتورة مطابقة · «فاتورة واردة» تحتاج إقرار مطابقتكم · «أمر صرف» يحتاج توثيق الدفع وإيصال التحويل. اضغط المستند لعرض معاملاته.':'أوامر صرف قيد الإجراء للأفراد — لا فاتورة ولا مسير. تحتاج توثيق الدفع ورفع إيصال التحويل. اضغط أمر الصرف لعرض معاملاته.')+
        finCostStmts(p,['draft','held','awaiting_inv','inv_received','issued','cancelled'],(finPtype(p)==='vendor'?'لا مسيرات أو أوامر صرف قيد الإجراء':'لا أوامر صرف قيد الإجراء'));
    } else if(FINX.ctab==='paid'){
      body=finInfo(finPtype(p)==='vendor'?'مسيرات الصرف المدفوعة — للمطابقة فقط: المعاملات وفاتورة المورّد وسند الصرف وإيصال التحويل.':'أوامر الصرف المدفوعة للأفراد — للمطابقة فقط: المعاملات وسند الصرف وإيصال التحويل.')+
        finCostStmts(p,['paid'],(finPtype(p)==='vendor'?'لا مسيرات صرف مدفوعة بعد':'لا أوامر صرف مدفوعة بعد'));
    } else {
      var exList=all.filter(function(l){ return l.st==='excluded'; });
      var xcols='minmax(150px,1.5fr) minmax(102px,.8fr) minmax(90px,.8fr) minmax(244px,1.8fr)';
      var xrows=exList.map(function(l){
        return '<div class="row" style="grid-template-columns:'+xcols+';min-height:48px">'+
          '<div class="td"><div style="display:flex;flex-direction:column;gap:2px"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px;text-align:end">'+esc(costRefMain(l))+'</span><span style="color:var(--text-3);font-size:11px">'+esc(costRefSub(l))+'</span></div></div>'+
          '<div class="td" style="font-size:12.5px;color:var(--text-2)">'+fmtSAR(l.list!=null?l.list:0)+'</div>'+
          '<div class="td" style="font-weight:700;font-size:12.5px;color:#a5432e">'+fmtSAR(l.amount)+'</div>'+
          '<div class="td" style="font-size:11.5px;color:var(--text-2);line-height:1.6">'+esc(l.exReason||'')+(l.settledAt?'<span style="color:var(--text-3)"> · '+esc(l.settledAt)+'</span>':'')+'</div>'+
        '</div>'; }).join('');
      body=finInfo('بنود خرجت من دورة الصرف ولن تُدفع — ملغاة أو مخسومة بالكامل، سُوّيت بين المشرف والمكتب الهندسي قبل الاستحقاق. لا تصل المالية إلا الأتعاب المتفق عليها، فهذا التبويب للمطابقة والسجل فقط.')+
        '<div class="card"><div class="scroll"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+xcols+'"><div class="th">المعاملة</div><div class="th">سعر الجدول</div><div class="th">الصافي</div><div class="th">سبب الاستبعاد</div></div><div>'+(xrows||'')+'</div></div></div></div>'+(exList.length?'':finEmpty('لا بنود مستبعدة'));
    }
    return header+strip+body;
  }
  function finVFile(v){ var on=v.file;
    return '<button type="button" data-fx-vfile="1" style="display:flex;align-items:center;gap:8px;width:100%;margin-top:11px;padding:10px 12px;border:1px dashed '+(on?'#3f8f5f':'var(--border-2)')+';border-radius:9px;background:'+(on?'color-mix(in srgb,#3f8f5f 8%,transparent)':'var(--surface)')+';cursor:pointer;font-family:inherit;font-size:11.5px;font-weight:600;color:'+(on?'#2f7a4d':'var(--text-2)')+'"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">'+(on?'<path d="M20 6 9 17l-5-5"/>':'<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8l-5-5-5 5"/><path d="M12 3v12"/>')+'</svg><span>'+(on?'نسخة الفاتورة الضريبية مُرفقة':'إرفاق نسخة الفاتورة الضريبية (PDF)')+'</span></button>'; }
  function finVMatch(v){
    return '<button type="button" data-fx-vmatch="1" style="display:flex;align-items:center;gap:9px;width:100%;margin-top:9px;padding:11px 12px;border:1px solid '+(v.matched?'#3f8f5f':'var(--border-2)')+';border-radius:9px;background:'+(v.matched?'color-mix(in srgb,#3f8f5f 8%,transparent)':'var(--surface)')+';cursor:pointer;font-family:inherit;text-align:start">'+finChk(v.matched)+'<span style="font-size:11.5px;font-weight:600;color:'+(v.matched?'#2f7a4d':'var(--text-2)')+';line-height:1.6">طابقتُ مستند الفاتورة مع مسير الصرف — القيمة والبنود مطابقة</span></button>'; }
  function costStepper(s){
    if(s.status==='cancelled') return '';
    var vendor=stmtIsVendor(s);
    var steps=vendor?['مسير مُعد','أُرسل للمكتب','مطابقة وقيد','توثيق الصرف']:['أمر صرف صادر','توثيق الصرف','مدفوع'];
    var map=vendor?{draft:0,held:0,awaiting_inv:1,inv_received:2,issued:3,paid:4}:{draft:0,issued:1,paid:3};
    var cur=map[s.status]; if(cur==null) cur=0;
    var dots='<div style="display:flex;align-items:center;gap:0">';
    var labs='<div style="display:flex;align-items:flex-start;gap:0;margin-top:5px">';
    steps.forEach(function(lbl,i){
      var done=i<cur, active=i===cur;
      var bg=done?'#3f8f5f':(active?'var(--ink)':'var(--surface)');
      var fg=(done||active)?'#fff':'var(--text-3)';
      var bd=done?'#3f8f5f':(active?'var(--ink)':'var(--border-2)');
      if(i) dots+='<span style="flex:1;height:2px;background:'+(i<=cur?'#3f8f5f':'var(--border-2)')+'"></span>';
      dots+='<span style="flex:none;width:22px;height:22px;border-radius:99px;border:1.5px solid '+bd+';background:'+bg+';color:'+fg+';display:grid;place-items:center;font-size:10.5px;font-weight:800">'+(done?'✓':(i+1))+'</span>';
      labs+='<span style="flex:1;text-align:center;font-size:9.5px;font-weight:'+(active?'800':'600')+';color:'+(active?'var(--heading)':(done?'#2f7a4d':'var(--text-3)'))+';line-height:1.35">'+lbl+'</span>';
    });
    return '<div style="margin-bottom:14px;padding:11px 14px;background:var(--surface-2);border:1px solid var(--border);border-radius:10px">'+dots+'</div>'+labs+'</div></div>';
  }
  function finStmtDetail(s){
    var stm=CSTMT_ST[s.status], tot=costTotal(s);
    var head='<div style="display:flex;align-items:flex-start;justify-content:space-between;gap:10px;margin-bottom:14px"><div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap"><button data-fx-cclose="1" style="display:inline-flex;align-items:center;gap:5px;border:1px solid var(--border-2);background:var(--surface);color:var(--text-2);border-radius:8px;padding:6px 11px;font-size:11.5px;font-weight:700;cursor:pointer;font-family:inherit"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 15l-6-6-6 6"/></svg>إغلاق</button><div><div style="font-size:15px;font-weight:800;color:var(--heading)" dir="ltr">'+esc(s.ref)+'</div><div style="font-size:12px;color:var(--text-2);margin-top:3px">'+fmtSAR(tot)+' · '+s.lines.length+' معاملة</div></div></div>'+pill(stm.t,stm.c)+'</div>';
    head+=costStepper(s);
    if(s.note) head+='<div style="margin-bottom:12px;padding:9px 12px;background:var(--surface-2);border:1px solid var(--border);border-radius:9px;font-size:11.5px;color:var(--text-2);line-height:1.7"><b style="color:var(--heading)">ملاحظة:</b> '+esc(s.note)+'</div>';
    var lcols='minmax(150px,1.5fr) minmax(120px,1fr) 122px';
    var lt=(stmtVat(s)>0?costTotalsBox(s).replace('margin-top:12px','margin-bottom:12px'):'')+'<div class="card" style="padding:0;overflow:hidden"><div style="padding:10px 14px;border-bottom:1px solid var(--border);font-size:12px;font-weight:700;color:var(--heading);background:var(--surface-2)">معاملات '+finDocNameS(s)+'</div><div class="scroll" style="max-height:320px;overflow:auto"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+lcols+';position:sticky;top:0;z-index:2"><div class="th">المرجع</div><div class="th">البيان</div><div class="th">المبلغ</div></div><div>'+s.lines.map(function(l){ return '<div class="row" style="grid-template-columns:'+lcols+';min-height:38px"><div class="td"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px">'+esc(costRefMain(l))+'</span></div><div class="td" style="font-size:11.5px;color:var(--text-2)">'+esc(costRefSub(l))+'</div><div class="td" style="font-weight:700;font-size:12.5px;color:var(--heading)">'+fmtSAR(l.amount)+'</div></div>'; }).join('')+'</div></div></div></div>';
    if(s.rejected&&s.rejected.length) lt+='<div style="margin-top:12px;border:1px solid #e8c4bb;border-radius:10px;overflow:hidden"><div style="padding:8px 12px;background:color-mix(in srgb,#c0553d 7%,transparent);font-size:11.5px;font-weight:800;color:#a5432e">فواتير أُعيدت للتصحيح ('+s.rejected.length+')</div>'+s.rejected.map(function(r){ return '<div style="padding:8px 12px;border-top:1px solid var(--border);font-size:11px;color:var(--text-2);line-height:1.7"><b dir="ltr" style="color:var(--heading)">'+esc(r.no)+'</b> · '+esc(r.date||'')+' · '+fmtSAR(r.amount||0)+'<div style="color:#a5432e">سبب الإعادة: '+esc(r.reason)+' <span style="color:var(--text-3)">('+esc(r.at)+')</span></div></div>'; }).join('')+'</div>';
    if(s.audit&&s.audit.length){ var aOpen=FINX.cauditOpen;
      lt+='<button type="button" data-fx-audit="1" style="display:flex;align-items:center;gap:6px;margin-top:12px;padding:7px 11px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-family:inherit;font-size:11px;font-weight:700;cursor:pointer"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="transform:rotate('+(aOpen?'90':'0')+'deg)"><path d="M9 18l6-6-6-6"/></svg>سجل الإجراءات ('+s.audit.length+')</button>'+
        (aOpen?'<div style="margin-top:8px;border:1px solid var(--border);border-radius:10px;overflow:hidden">'+s.audit.map(function(e,i){ return '<div style="display:flex;gap:9px;padding:7px 12px;'+(i?'border-top:1px solid var(--border);':'')+'font-size:11px"><span style="color:var(--text-3);flex:none" dir="ltr">'+esc(e.at)+'</span><span style="color:var(--text);flex:1">'+esc(e.act)+'</span><span style="color:var(--text-2);flex:none">'+esc(e.by)+'</span></div>'; }).join('')+'</div>':''); }
    var action='';
    var isVendor=(s.ptype||'vendor')==='vendor';
    if(s.status==='draft'||s.status==='held'){
      action='<button class="primary" data-fx-capprove="'+s.id+'" style="width:100%;justify-content:center;padding:12px;margin-top:14px"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 2 11 13"/><path d="M22 2 15 22l-4-9-9-4z"/></svg><span>'+(isVendor?'تحويل المسير للمكتب لإصدار الفاتورة':'اعتماد وإصدار أمر الصرف')+'</span></button>'+
        (isVendor?'<div style="font-size:10.5px;color:var(--text-3);margin-top:8px;line-height:1.7;text-align:center">يُرسل مسير الصرف للمورّد ليُصدر فاتورة ضريبية مطابقة لقيمته وبنوده.</div>':'');
    }
    else if(s.status==='awaiting_inv'){
      action='<div style="margin-top:14px;border:1px solid var(--border);border-radius:11px;background:var(--surface-2);padding:14px 15px">'+
        '<div style="display:flex;gap:9px;align-items:flex-start"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#6b4bb3" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" style="flex:none;margin-top:1px"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg><div><div style="font-size:12.5px;font-weight:700;color:var(--heading)">بانتظار رفع المورّد لفاتورته</div><div style="font-size:11px;color:var(--text-2);margin-top:3px;line-height:1.7">أُرسل مسير الصرف للمورّد؛ يُصدر فاتورته من برنامجه المحاسبي ويرفعها على المسير من بوابته، فتصلكم هنا للمطابقة.</div></div></div>'+
        '<div style="margin-top:11px;padding:8px 11px;background:var(--surface);border:1px solid var(--border);border-radius:8px;font-size:10.5px;color:var(--text-3);line-height:1.7">الإجراء لدى المكتب الهندسي — شاشة «فوترة الأتعاب» › تبويب «مسيرات بانتظار فاتورتكم». لا إجراء مطلوب منكم الآن.</div>'+
      '</div>'; }
    else if(s.status==='inv_received'){ if(!FINX.cvinv) FINX.cvinv=finBlankVinv(); var v=FINX.cvinv; var iv=s.vinv||{};
      var kvr=function(k,val){ return '<div style="display:flex;justify-content:space-between;padding:8px 12px;border-bottom:1px solid var(--border)"><span style="font-size:11.5px;color:var(--text-2)">'+k+'</span><span style="font-size:12px;font-weight:700;color:var(--heading)" dir="ltr">'+val+'</span></div>'; };
      action='<div style="margin-top:14px;border:1px solid var(--border);border-radius:11px;background:var(--surface-2);padding:14px 15px">'+
        '<div style="font-size:12.5px;font-weight:700;color:var(--heading);margin-bottom:2px">مطابقة فاتورة المورّد <span style="font-weight:500;color:var(--text-3);font-size:11px">— وردت من بوابة المورّد</span></div>'+
        '<div style="border:1px solid var(--border);border-radius:9px;overflow:hidden;background:var(--surface);margin-top:10px">'+kvr('رقم الفاتورة',esc(iv.no||'—'))+kvr('تاريخ الفاتورة',esc(iv.date||'—'))+kvr('نسخة الفاتورة','<span style="color:#2f7a4d">✓ '+esc(iv.file||'مرفوعة من المورّد')+'</span>')+'<div style="display:flex;justify-content:space-between;padding:9px 12px;background:var(--surface-2)"><span style="font-size:11.5px;font-weight:700;color:var(--heading)">القيمة — مطابقة للمسير</span><span style="font-size:13.5px;font-weight:800;color:var(--heading)">'+fmtSAR(tot)+'</span></div></div>'+
        finVMatch(v)+
        '<button class="primary" data-fx-cvinv="'+s.id+'" style="width:100%;justify-content:center;padding:11px;margin-top:12px"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg><span>قيد الفاتورة وإصدار أمر الصرف</span></button>'+
        '<div style="margin-top:12px;padding-top:12px;border-top:1px dashed var(--border-2)"><label class="tf-lbl">سبب الإعادة للمورّد</label><textarea data-fx-vf="reason" rows="2" placeholder="خلل في المستند أو بياناته" style="'+INP_STYLE+';width:100%;margin-top:4px;resize:vertical">'+esc(v.reason||'')+'</textarea>'+
          '<button data-fx-vreturn="'+s.id+'" style="width:100%;padding:10px;margin-top:9px;font-size:11.5px;border:1px solid #c0553d;border-radius:9px;background:var(--surface);color:#a5432e;font-weight:700;cursor:pointer;font-family:inherit">إعادة للمورّد للتصحيح</button></div>'+
      '</div>'; }
    else if(s.status==='issued'){ if(!FINX.cclose) FINX.cclose=finBlankClose(); var c=FINX.cclose;
      action='<div style="margin-top:14px;border:1px solid var(--border);border-radius:11px;background:var(--surface-2);padding:14px 15px">'+
        '<div style="font-size:12.5px;font-weight:700;color:var(--heading);margin-bottom:2px">توثيق الصرف <span style="font-weight:500;color:var(--text-3);font-size:11px">— من البرنامج المحاسبي الخارجي</span></div>'+
        '<div style="display:grid;grid-template-columns:1fr 1fr;gap:11px;margin-top:11px">'+
          '<div><label class="tf-lbl">رقم سند الصرف / الفاتورة <span style="color:#c0553d">*</span></label><input data-fx-cf="inv" value="'+esc(c.inv)+'" dir="ltr" placeholder="من البرنامج المحاسبي" style="'+INP_STYLE+';width:100%;margin-top:4px"></div>'+
          '<div><label class="tf-lbl">مرجع التحويل <span style="color:#c0553d">*</span></label><input data-fx-cf="trf" value="'+esc(c.trf)+'" dir="ltr" placeholder="رقم التحويل البنكي" style="'+INP_STYLE+';width:100%;margin-top:4px"></div>'+
        '</div>'+
        '<div style="margin-top:11px"><label class="tf-lbl">تاريخ الصرف</label><input data-fx-cf="paidAt" type="date" value="'+esc(c.paidAt)+'" style="'+INP_STYLE+';width:100%;margin-top:4px"></div>'+
        '<div style="margin-top:11px"><label class="tf-lbl">إيصال التحويل <span style="color:#c0553d">*</span></label>'+
          '<button type="button" data-fx-creceipt="1" style="display:flex;align-items:center;gap:8px;width:100%;margin-top:4px;padding:10px 12px;border:1px dashed '+(c.receipt?'#3f8f5f':'var(--border-2)')+';border-radius:9px;background:'+(c.receipt?'color-mix(in srgb,#3f8f5f 8%,transparent)':'var(--surface)')+';cursor:pointer;font-family:inherit;font-size:11.5px;font-weight:600;color:'+(c.receipt?'#2f7a4d':'var(--text-2)')+'"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">'+(c.receipt?'<path d="M20 6 9 17l-5-5"/>':'<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8l-5-5-5 5"/><path d="M12 3v12"/>')+'</svg><span>'+(c.receipt?'إيصال التحويل مرفوع — '+esc(c.receipt):'رفع إيصال التحويل (إثبات الدفع)')+'</span></button></div>'+
        '<button class="primary" data-fx-cpay="'+s.id+'" style="width:100%;justify-content:center;padding:11px;margin-top:14px"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg><span>إقفال أمر الصرف كمدفوع</span></button>'+
      '</div>'; }
    else if(s.status==='cancelled'){ action='<div style="margin-top:14px;background:var(--surface-2);border:1px solid var(--border-2);border-radius:11px;padding:13px 15px;font-size:12.5px;line-height:1.9;color:var(--text-2)"><div style="font-weight:800;margin-bottom:4px;color:var(--heading)">مستند ملغى</div>أُلغي هذا المستند قبل الصرف وأُرجعت بنوده إلى «مستحقات مفتوحة» — لم يُدفع أي مبلغ. يبقى ظاهراً للسجل والتدقيق.</div>'; }
    else if(s.status==='paid'){ action='<div style="margin-top:14px;background:color-mix(in srgb,#3f8f5f 10%,transparent);border:1px solid #a9dfbf;border-radius:11px;padding:13px 15px;font-size:12.5px;line-height:1.9;color:#2f7a4d"><div style="font-weight:800;margin-bottom:4px">✓ مدفوع</div>سند صرف <b dir="ltr">'+esc(s.inv||'')+'</b> · تاريخ '+esc(s.paidAt||'')+(s.trf?' · مرجع تحويل <b dir="ltr">'+esc(s.trf)+'</b>':'')+(s.receipt?'<div style="margin-top:5px">إيصال التحويل <b>✓ '+esc(s.receipt)+'</b></div>':'')+(s.vinv?'<div style="margin-top:5px">فاتورة المورّد <b dir="ltr">'+esc(s.vinv.no)+'</b> · '+esc(s.vinv.date||'')+'</div>':'')+'</div>'; }
    if(s.status!=='paid'&&s.status!=='cancelled') action+='<div style="margin-top:12px;padding-top:12px;border-top:1px dashed var(--border-2)"><button data-fx-ccancel="'+s.id+'" style="width:100%;padding:9px;font-size:11.5px;border:1px solid var(--border-2);border-radius:9px;background:var(--surface);color:#a5432e;font-weight:700;cursor:pointer;font-family:inherit">إلغاء المستند وإرجاع بنوده مفتوحة</button></div>';
    return '<div class="card" style="padding:16px 17px">'+head+lt+action+'</div>';
  }
  function finCostStmts(p, statuses, emptyMsg){
    var mine=COST_STMTS.filter(function(s){ return s.payee===p.name && statuses.indexOf(s.status)>-1; });
    var sel=FINX.cstmt?mine.find(function(x){ return x.id===FINX.cstmt; }):null;
    if(sel) return finStmtDetail(sel);
    var cols='minmax(150px,1.5fr) minmax(95px,.85fr) minmax(92px,.7fr) minmax(100px,.9fr) minmax(190px,1fr)';
    var C='justify-content:center;text-align:center';
    var rows=mine.map(function(s){ var stm=CSTMT_ST[s.status]; var tot=costTotal(s);
      return '<div class="row" data-fx-cstmt="'+s.id+'" role="button" style="grid-template-columns:'+cols+';min-height:46px;cursor:pointer">'+
        '<div class="td"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px">'+esc(s.ref)+'</span></div>'+
        '<div class="td" style="'+C+';font-size:11.5px;color:var(--text-2)" dir="ltr">'+esc(s.at||s.paidAt||'—')+'</div>'+
        '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)">'+s.lines.length+' معاملة</div>'+
        '<div class="td" style="'+C+';font-weight:700;font-size:12.5px;color:var(--heading)">'+fmtSAR(tot)+'</div>'+
        '<div class="td" style="'+C+'">'+pill(stm.t,stm.c)+'</div>'+
      '</div>';
    }).join('');
    return '<div class="card"><div class="scroll"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+cols+'"><div class="th">المرجع</div><div class="th" style="text-align:center">التاريخ</div><div class="th" style="text-align:center">المعاملات</div><div class="th" style="text-align:center">الإجمالي</div><div class="th" style="text-align:center">الحالة</div></div><div>'+(rows||'')+'</div></div></div></div>'+(mine.length?'':finEmpty(emptyMsg||'لا مسيرات في هذا التبويب'));
  }
  function openGateEx(id){
    var l=COST_LINES.find(function(x){ return x.id===id; }); if(!l) return;
    REVDRAFT={ kind:'gateex', lineId:id, reason:'' };
    var body=finInfo('الأصل ألّا تُصرف الأتعاب قبل تحصيل إيراد المعاملة. وهذه المعاملة <b>إيرادها مستبعد</b> فلن يُحصَّل أبداً — فالإفراج عنها استثناء يحتاج موافقة المشرف ويُسجَّل باسمه.')+
      '<div style="border:1px solid var(--border);border-radius:10px;overflow:hidden;margin-bottom:12px">'+
        '<div style="display:flex;justify-content:space-between;padding:8px 13px;border-bottom:1px solid var(--border)"><span style="font-size:11.5px;color:var(--text-2)">المرجع</span><span style="font-size:12px;font-weight:700;color:var(--heading)" dir="ltr">'+esc(costRefMain(l))+'</span></div>'+
        '<div style="display:flex;justify-content:space-between;padding:9px 13px;background:var(--surface-2)"><span style="font-size:12px;font-weight:800;color:var(--heading)">الأتعاب</span><span style="font-size:13.5px;font-weight:800;color:var(--heading)">'+fmtSAR(l.amount)+'</span></div>'+
      '</div>'+
      '<div><label class="tf-lbl">مبرر الاستثناء <span style="color:#c0553d">*</span></label><textarea data-gx-reason rows="3" placeholder="سبب صرف الأتعاب مع استبعاد إيراد المعاملة" style="'+INP_STYLE+';width:100%;margin-top:4px;resize:vertical"></textarea></div>';
    var foot='<button class="btn-ghost" data-rv-cancel type="button">إلغاء</button><button class="primary" data-gx-save type="button">إفراج باستثناء المشرف</button>';
    revModalShell('استثناء المشرف','بند إيراد معاملته مستبعد · '+esc(l.payee),body,foot);
    var m=document.getElementById('revModal');
    m.querySelector('[data-gx-reason]').addEventListener('input', function(e){ REVDRAFT.reason=e.target.value; });
    m.querySelector('[data-gx-save]').addEventListener('click', function(){
      if(!(REVDRAFT.reason||'').trim()){ revErr('مبرر الاستثناء مطلوب.'); return; }
      l.gateOverride={ by:'مشرف الإسناد', reason:REVDRAFT.reason.trim(), at:isoFromTs(taskLive()).split('-').reverse().join('/') };
      revClose(); renderFinance(); showToast('أُفرِج عن أتعاب '+costRefMain(l)+' باستثناء المشرف — سُجّل المبرر.');
    });
  }
  function openRunReview(){
    var p=finCur(); if(!p) return;
    var picked=COST_LINES.filter(function(l){ return FINX.csel[l.id]&&l.payee===p.name&&l.st==='due'&&costGate(l).ok; });
    if(!picked.length){ showToast('اختر بنوداً لتجهيز مسير الصرف.'); return; }
    var tot=picked.reduce(function(a,l){ return a+l.amount; },0);
    var pt=FIN_PTYPE[finPtype(p)];
    REVDRAFT={ kind:'run', note:'' };
    var lcols='minmax(140px,1.4fr) minmax(150px,1.2fr) 110px';
    var lines='<div class="scroll" style="max-height:260px;overflow:auto;border:1px solid var(--border);border-radius:9px"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+lcols+'"><div class="th">المرجع</div><div class="th">البيان</div><div class="th">المبلغ</div></div><div>'+picked.map(function(l){ return '<div class="row" style="grid-template-columns:'+lcols+';min-height:38px"><div class="td"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px">'+esc(costRefMain(l))+'</span></div><div class="td" style="font-size:11.5px;color:var(--text-2)">'+esc(costRefSub(l))+'</div><div class="td" style="font-weight:700;font-size:12.5px;color:var(--heading)">'+fmtSAR(l.amount)+'</div></div>'; }).join('')+'</div></div></div>';
    var kv=function(k,v){ return '<div style="display:flex;justify-content:space-between;padding:8px 13px;border-bottom:1px solid var(--border)"><span style="font-size:11.5px;color:var(--text-2)">'+k+'</span><span style="font-size:12px;font-weight:700;color:var(--heading)">'+v+'</span></div>'; };
    var body='<div style="border:1px solid var(--border);border-radius:10px;overflow:hidden;margin-bottom:14px">'+
        kv('المستحق', esc(p.name)+' <span style="font-weight:500;color:var(--text-3)">· '+pt.t+'</span>')+
        kv('الآيبان','<span dir="ltr">'+esc(p.iban)+'</span>')+
        kv('عدد المعاملات', picked.length+' معاملة')+
        (finPtype(p)==='vendor'?kv('صافي الأتعاب',fmtSAR(tot))+kv('ضريبة القيمة المضافة (15%)',fmtSAR(Math.round(tot*COST_VAT*100)/100)):'')+'<div style="display:flex;justify-content:space-between;padding:10px 13px;background:var(--surface-2)"><span style="font-size:12.5px;font-weight:800;color:var(--heading)">'+(finPtype(p)==='vendor'?'الإجمالي — تُطابقه فاتورة المورّد':'الإجمالي')+'</span><span style="font-size:14px;font-weight:800;color:var(--heading)">'+fmtSAR(Math.round(tot*costFactor(p)*100)/100)+'</span></div>'+
      '</div>'+lines+
      '<div style="margin-top:12px"><label class="tf-lbl">ملاحظة</label><textarea data-run-note rows="2" placeholder="اختيارية" style="'+INP_STYLE+';width:100%;margin-top:4px;resize:vertical"></textarea></div>'+
      '<div style="font-size:10.5px;color:var(--text-3);margin-top:10px;line-height:1.7">'+(finPtype(p)==='vendor'?'بالتأكيد يُعَدّ المسير و<b>يبقى لدى المالية</b> — لا يُرسل للمكتب الآن. وعند قرار الصرف يُحوَّل إليه ليُصدر فاتورة ضريبية مطابقة لقيمته، فلا يسبق تاريخ الفاتورة تاريخ الصرف.':'بالتأكيد يصدر أمر الصرف مباشرة — الأفراد لا يُصدرون فواتير ولا يلزمهم مسير صرف.')+'</div>';
    var foot='<button class="btn-ghost" data-rv-cancel type="button">إلغاء</button><button class="primary" data-run-confirm type="button"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg><span>'+(finPtype(p)==='vendor'?'تأكيد — يبقى لدى المالية':'تأكيد وإصدار أمر الصرف')+'</span></button>';
    revModalShell('مراجعة '+finDocName(p),'راجِع البنود قبل التأكيد',body,foot);
    var m=document.getElementById('revModal');
    var nt=m.querySelector('[data-run-note]'); if(nt) nt.addEventListener('input', function(e){ REVDRAFT.note=e.target.value; });
    m.querySelector('[data-run-confirm]').addEventListener('click', function(){ var note=(REVDRAFT&&REVDRAFT.note)||''; revClose(); finCostCreate(note,true); });
  }
  function finCostCreate(note, autoSend){
    var p=finCur(); var picked=COST_LINES.filter(function(l){ return FINX.csel[l.id]&&l.payee===p.name&&l.st==='due'&&costGate(l).ok; });
    if(!picked.length){ showToast('اختر بنوداً لتجهيز مسير الصرف.'); return; }
    var ref='CS-COST-'+isoFromTs(taskLive()).slice(2).replace(/-/g,'')+'-'+String(101+COST_STMTS.length).slice(1);
    COST_STMTS.unshift({ id:'cs'+Date.now(), ref:ref, party:p.role, ptype:finPtype(p), payee:p.name, status:'draft', at:'', note:(note||''), lines:picked.map(function(l){ return {id:l.id,deed:l.deed,region:l.region,po:l.po,taskRef:l.taskRef,taskTitle:l.taskTitle,amount:l.amount,list:(l.list!=null?l.list:l.amount),ded:l.ded||0,dedReason:l.dedReason||''}; }) });
    picked.forEach(function(l){ l.st='instmt'; });
    FINX.csel={}; FINX.ctab='orders'; FINX.cstmt=COST_STMTS[0].id; FINX.cclose=null;
    costLog(COST_STMTS[0],'تجهيز المستند ('+picked.length+' بند)');
    if(autoSend){ var ns=COST_STMTS[0];
      if(stmtIsVendor(ns)){ ns.status='held'; ns.at=isoFromTs(taskLive()).split('-').reverse().join('/'); costLog(ns,'إعداد مسير الصرف — محتجز لدى المالية'); renderFinance(); showToast('أُعِدّ مسير الصرف '+ns.ref+' ويبقى لدى المالية حتى قرار الصرف.'); return; }
      finStmtApprove(ns.id); return; }
    renderFinance();
    showToast('جُهّز «'+finDocName(p)+'» '+ref+' كمسودة ('+picked.length+' بند).');
  }
  function finStmtApprove(id){ var s=COST_STMTS.find(function(x){ return x.id===id; }); if(!s) return; var isVendor=(s.ptype||'vendor')==='vendor';
    if(!s.at) s.at=isoFromTs(taskLive()).split('-').reverse().join('/');
    costLog(s,isVendor?'اعتماد وإرسال للمورّد':'اعتماد وإصدار أمر الصرف');
    if(isVendor){ s.status='awaiting_inv'; FINX.cvinv=finBlankVinv(); renderFinance(); showToast('اعتُمد مسير الصرف '+s.ref+' وأُرسل للمورّد لإصدار فاتورة مطابقة.'); }
    else { s.status='issued'; FINX.cclose=finBlankClose(); renderFinance(); showToast('اعتُمد مسير الصرف '+s.ref+' وصدر أمر الصرف.'); }
  }
  function finStmtCancel(id){ var s=COST_STMTS.find(function(x){ return x.id===id; }); if(!s||s.status==='paid') return;
    s.lines.forEach(function(sl){ var t=COST_LINES.find(function(l){ return sl.id?l.id===sl.id:(l.deed===sl.deed&&l.payee===s.payee); }); if(t&&t.st==='instmt') t.st='due'; });
    costLog(s,'إلغاء المستند وإرجاع بنوده مفتوحة'); s.status='cancelled'; s.vinv=null;
    FINX.cstmt=null; FINX.cclose=null; FINX.cvinv=null; renderFinance(); showToast('أُلغي المستند '+s.ref+' وعادت بنوده إلى المستحقات المفتوحة.');
  }
  function finStmtReturn(id){ var s=COST_STMTS.find(function(x){ return x.id===id; }); if(!s) return; var r=((FINX.cvinv&&FINX.cvinv.reason)||'').trim();
    if(!r){ showToast('اذكر سبب الإعادة للمورّد.'); var el=document.querySelector('[data-fx-vf="reason"]'); if(el){ el.style.borderColor='#c0553d'; el.focus(); } return; }
    s.rejected=s.rejected||[]; if(s.vinv) s.rejected.push({ no:s.vinv.no, date:s.vinv.date, amount:s.vinv.amount, file:s.vinv.file||'', reason:r, at:isoFromTs(taskLive()).split('-').reverse().join('/') });
    costLog(s,'إعادة الفاتورة للتصحيح — '+r);
    s.returned={ reason:r, at:isoFromTs(taskLive()).split('-').reverse().join('/') }; s.vinv=null; s.status='awaiting_inv';
    FINX.cvinv=finBlankVinv(); renderFinance(); showToast('أُعيد مسير الصرف '+s.ref+' للمورّد لتصحيح الفاتورة.');
  }
  function finStmtVinv(id){ var s=COST_STMTS.find(function(x){ return x.id===id; }); if(!s||!FINX.cvinv) return; var v=FINX.cvinv;
    var tot=s.lines.reduce(function(a,l){ return a+l.amount; },0);
    var fromOffice=(s.status==='inv_received'&&s.vinv);
    var no=fromOffice?s.vinv.no:(v.no||'').trim();
    var date=fromOffice?s.vinv.date:((v.date||'').split('-').reverse().join('/'));
    if(!no){ showToast('رقم فاتورة المورّد مطلوب.'); return; }
    if(!date){ showToast('تاريخ الفاتورة مطلوب.'); return; }
    if(!v.matched){ showToast('أكّد مطابقة مستند الفاتورة مع مسير الصرف.'); return; }
    s.vinv={ no:no, date:date, amount:tot };
    costLog(s,'مطابقة فاتورة المورّد '+no+' وقيدها');
    s.status='issued'; FINX.cvinv=null; FINX.cclose=finBlankClose(); renderFinance();
    showToast('قُيّدت فاتورة المورّد '+s.vinv.no+' وصدر أمر الصرف.');
  }
  function finCostIssue(id){ return finStmtApprove(id); }
  function finCostPay(id){ var s=COST_STMTS.find(function(x){ return x.id===id; }); if(!s||!FINX.cclose) return; var c=FINX.cclose; if(!c.inv.trim()){ showToast('رقم سند الصرف مطلوب.'); var el=document.querySelector('[data-fx-cf="inv"]'); if(el){ el.style.borderColor='#c0553d'; el.focus(); } return; } if(!(c.trf||'').trim()){ showToast('مرجع التحويل البنكي مطلوب للمطابقة البنكية.'); var el3=document.querySelector('[data-fx-cf="trf"]'); if(el3){ el3.style.borderColor='#c0553d'; el3.focus(); } return; } if(costVoucherExists(c.inv,s.id)){ showToast('رقم سند الصرف مستخدم مسبقاً على مستند آخر — لا يمكن تكراره.'); var el2=document.querySelector('[data-fx-cf="inv"]'); if(el2){ el2.style.borderColor='#c0553d'; el2.focus(); } return; } if(!c.receipt){ showToast('ارفع إيصال التحويل لإثبات الدفع.'); return; } s.status='paid'; s.inv=c.inv.trim(); s.trf=(c.trf||'').trim(); s.receipt=c.receipt; s.paidAt=(c.paidAt||'').split('-').reverse().join('/'); costLog(s,'توثيق الصرف — سند '+s.inv+' · تحويل '+s.trf); s.lines.forEach(function(sl){ var t=COST_LINES.find(function(l){ return sl.id?l.id===sl.id:(l.deed===sl.deed&&l.payee===s.payee); }); if(t) t.st='paid'; }); FINX.cclose=null; FINX.ctab='paid'; renderFinance(); showToast('أُقفل أمر الصرف '+s.ref+' كمدفوع.'); }
  function finPartiesList(){
    var q=(FINX.dq||'').trim();
    var parties=FIN_PARTIES.filter(function(p){ return p.kind==='cost' && (!q||p.name.indexOf(q)>-1||p.code.indexOf(q)>-1); });
    var cols='minmax(68px,.5fr) minmax(135px,1.5fr) minmax(90px,.6fr) minmax(148px,1.1fr) minmax(108px,.85fr) minmax(92px,.72fr) minmax(120px,.75fr) 26px';
    var C='justify-content:center;text-align:center';
    var rows=parties.map(function(p){ var role=FIN_ROLE[p.role]; var pt=FIN_PTYPE[finPtype(p)]; var b=finPartyBal(p);
      return '<div class="row" data-fx-party="'+p.id+'" role="button" style="grid-template-columns:'+cols+';min-height:52px;cursor:pointer">'+
        '<div class="td"><span dir="ltr" style="font-size:11px;font-weight:700;color:var(--text-2)">'+esc(p.code)+'</span></div>'+
        '<div class="td" style="'+C+'"><span style="font-size:13px;font-weight:700;color:var(--heading)">'+esc(p.name)+'</span></div>'+
        '<div class="td" style="'+C+'">'+pill(pt.t,pt.c)+'</div>'+
        '<div class="td" style="'+C+'">'+pill(role.t,role.c)+'</div>'+
        '<div class="td" dir="ltr" style="'+C+';font-size:12px;color:var(--text-2)">'+esc(p.phone)+'</div>'+
        '<div class="td" style="'+C+';font-weight:700;font-size:13px;color:'+(b.bal==null?'var(--text-3)':'var(--heading)')+'">'+(b.bal==null?'<span style="font-size:11px;font-weight:600">متابعة معطّلة</span>':fmtSAR(b.bal))+'</div>'+
        '<div class="td" style="'+C+'">'+(b.bal==null?'<span style="font-size:11px;color:var(--text-3)">تُدار خارج النظام</span>':(b.pending?'<span style="display:inline-flex;align-items:center;gap:5px;font-size:11.5px;color:#8a5e14;font-weight:700"><span style="width:7px;height:7px;border-radius:99px;background:#d9a441"></span>'+b.pending+' بند</span>':'<span style="font-size:11.5px;color:var(--text-3)">مسوّى</span>'))+'</div>'+
        '<div class="td" style="'+C+';color:var(--text-3)"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 18l-6-6 6-6"/></svg></div>'+
      '</div>';
    }).join('');
    return finInfo('المستحقون المسجّلون لدى المالية — موردون خارجيون وموظفون لهم أتعاب على المعاملات والمهام. اضغط الاسم للدخول على تفاصيله المالية.')+
      '<div class="toolbar" style="margin-bottom:12px"><div class="filters" style="flex:1;display:flex"><input data-fx-dq value="'+esc(FINX.dq||'')+'" placeholder="بحث باسم المستحق أو رقمه" style="'+INP_STYLE+';flex:1;min-width:240px"></div><span style="font-size:11.5px;color:var(--text-3);align-self:center">'+parties.length+' مستحق مسجّل</span></div>'+
      '<div class="card"><div class="scroll"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+cols+'"><div class="th">الرقم</div><div class="th" style="text-align:center">الاسم</div><div class="th" style="text-align:center">الصفة</div><div class="th" style="text-align:center">المهمة</div><div class="th" style="text-align:center">الجوال</div><div class="th" style="text-align:center">مستحق له</div><div class="th" style="text-align:center">مستحقات قائمة</div><div class="th"></div></div><div>'+(rows||'')+'</div></div></div></div>'+(parties.length?'':finEmpty('لا مستحقين مطابقين'));
  }
  function finRevRows(status){
    var list=revList(status);
    if(status==='eligible'||status==='awaiting') list.sort(function(a,b){ return a.doneAt<b.doneAt?1:-1; });
    else if(status==='inprogress') list.sort(function(a,b){ return a.regAt<b.regAt?1:-1; });
    else if(status==='invoiced') list.sort(function(a,b){ return (a.inv.uploadedAt<b.inv.uploadedAt?-1:1); });
    else if(status==='collected') list.sort(function(a,b){ return (a.pay.date<b.pay.date?1:-1); });
    else list.sort(function(a,b){ return a.doneAt<b.doneAt?1:-1; });
    var C='justify-content:center;text-align:center';
    var po=function(t){ return '<div class="td" style="font-size:12px;color:var(--ink);font-weight:600"><span dir="ltr">'+esc(t.po)+'</span></div>'; };
    var deed=function(t){ return '<div class="td" style="'+C+'"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px">'+esc(t.deed)+'</span></div>'; };
    var city=function(t){ return '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)">'+esc(t.city)+'</div>'; };
    var done=function(t){ return '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)">'+revDate(t.doneAt)+'</div>'; };
    var gross=function(t){ return '<div class="td" style="'+C+'"><span style="display:inline-flex;align-items:center;gap:10px">'+revFlags(t)+'<span style="font-weight:700;font-size:13px;color:var(--heading)">'+fmtSAR(revGross(t))+'</span></span></div>'; };
    var invc=function(t){ return '<div class="td" style="'+C+'"><div style="display:flex;flex-direction:column;gap:2px;align-items:center"><span dir="ltr" style="font-weight:700;color:var(--text);font-size:12px">'+esc(t.inv.no)+'</span><span style="color:var(--text-3);font-size:10.5px" dir="ltr">'+revDate(t.inv.date)+'</span></div></div>'; };
    var cols, heads, rows;
    if(status==='inprogress'){
      cols='minmax(105px,.9fr) minmax(125px,1.05fr) minmax(72px,.6fr) minmax(110px,.9fr) minmax(150px,1.2fr)';
      heads=['رقم الطلب','رقم الصك','المدينة','تاريخ التسجيل','الحالة التشغيلية'];
      rows=list.map(function(t){ var oc=REV_OP[t.opState]||'#4b6b8a';
        return '<div class="row" style="grid-template-columns:'+cols+';min-height:48px">'+po(t)+deed(t)+city(t)+
        '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)" dir="ltr">'+revDate(t.regAt)+'</div>'+
        '<div class="td" style="'+C+'">'+pill(t.opState||'قيد العمل',oc)+'</div></div>'; }).join('');
    } else if(status==='awaiting'){
      cols='50px minmax(142px,1.1fr) minmax(132px,.8fr) minmax(132px,.8fr) minmax(110px,.8fr) minmax(100px,.85fr) minmax(90px,.75fr) minmax(112px,.95fr) minmax(162px,.95fr)';
      heads=['','رقم الصك','مساحة الأرض (م٢)','مسطح البناء (م٢)','أتعاب المفاتيح','الأتعاب','الضريبة','شاملة الضريبة','الإجمالي المستحق'];
      var num=function(v,d){ return Number(v||0).toLocaleString('en-US',{minimumFractionDigits:d||0,maximumFractionDigits:d||0}); };
      var groups={}; list.forEach(function(t){ (groups[t.po]=groups[t.po]||[]).push(t); });
      rows=Object.keys(groups).map(function(po){
        var gl=groups[po];
        var sBase=0,sVat=0,sKey=0,sGross=0;
        var body=gl.map(function(t){ var base=revBase(t), vat=revVat(t), key=revKey(t), gross=base+vat+key;
          sBase+=base; sVat+=vat; sKey+=key; sGross+=gross;
          var on=!!FINX.rsel[t.id];
          return '<div class="row" data-rv-sel="'+t.id+'" role="button" style="grid-template-columns:'+cols+';min-height:44px;cursor:pointer'+(on?';background:color-mix(in srgb,var(--ink) 5%,transparent)':'')+'">'+
            '<div class="td">'+finChk(on)+'</div>'+
            '<div class="td"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px">'+esc(t.deed)+'</span></div>'+
            '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)" dir="ltr">'+num(t.land,2)+'</div>'+
            '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)" dir="ltr">'+num(t.bld,2)+'</div>'+
            '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)" dir="ltr">'+num(key,2)+'</div>'+
            '<div class="td" style="'+C+';font-size:12px;color:var(--text)" dir="ltr">'+num(base,2)+'</div>'+
            '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)" dir="ltr">'+num(vat,2)+'</div>'+
            '<div class="td" style="'+C+';font-size:12px;color:var(--text)" dir="ltr">'+num(base+vat,2)+'</div>'+
            '<div class="td" style="'+C+'"><span style="display:inline-flex;align-items:center;gap:10px">'+revFlags(t)+'<span style="font-weight:700;font-size:12.5px;color:var(--heading)" dir="ltr">'+num(gross,2)+'</span></span></div>'+
          '</div>'; }).join('');
        var col=!!FINX.rcol[po];
        var head='<div class="row" data-rv-col="'+esc(po)+'" role="button" style="grid-template-columns:1fr;min-height:46px;background:var(--surface-2);cursor:pointer"><div class="td"><div style="display:flex;width:100%;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap">'+
          '<span style="display:inline-flex;align-items:center;gap:9px;flex-wrap:wrap"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="flex:none;color:var(--text-3);transform:rotate('+(col?'0':'-90')+'deg)"><path d="M9 18l6-6-6-6"/></svg><span style="font-size:12.5px;font-weight:800;color:var(--heading)">أمر العمل <span dir="ltr">'+esc(po)+'</span></span><span style="font-size:11px;color:var(--text-3)">'+gl.length+' معاملة · الإجمالي المستحق <b dir="ltr" style="color:var(--heading)">'+num(sGross,2)+'</b> ر.س</span></span>'+
        '</div></div></div>';
        var tot='<div class="row" style="grid-template-columns:'+cols+';min-height:38px;background:color-mix(in srgb,var(--gold) 7%,transparent)">'+
          '<div class="td"></div>'+
          '<div class="td" style="font-size:11px;font-weight:800;color:var(--heading)">إجمالي أمر العمل</div>'+
          '<div class="td"></div><div class="td"></div>'+
          '<div class="td" style="'+C+';font-size:11.5px;font-weight:700;color:var(--text-2)" dir="ltr">'+num(sKey,2)+'</div>'+
          '<div class="td" style="'+C+';font-size:11.5px;font-weight:700;color:var(--text)" dir="ltr">'+num(sBase,2)+'</div>'+
          '<div class="td" style="'+C+';font-size:11.5px;font-weight:700;color:var(--text-2)" dir="ltr">'+num(sVat,2)+'</div>'+
          '<div class="td" style="'+C+';font-size:11.5px;font-weight:700;color:var(--text)" dir="ltr">'+num(sBase+sVat,2)+'</div>'+
          '<div class="td" style="'+C+';font-size:12.5px;font-weight:800;color:var(--heading)" dir="ltr">'+num(sGross,2)+'</div>'+
        '</div>';
        return head+(col?'':body+tot);
      }).join('');
    } else if(status==='eligible'){
      cols='minmax(105px,.9fr) minmax(142px,1fr) minmax(70px,.55fr) minmax(104px,.82fr) minmax(146px,.9fr) minmax(238px,1.3fr)';
      heads=['رقم الطلب','رقم الصك','المدينة','تاريخ الاكتمال','إجمالي الأتعاب','الإجراء'];
      rows=list.map(function(t){ return '<div class="row" style="grid-template-columns:'+cols+';min-height:52px">'+po(t)+deed(t)+city(t)+done(t)+gross(t)+
        '<div class="td" style="'+C+'"><div style="display:flex;gap:6px;justify-content:center">'+(revCanMatch(t)?'<button class="primary" data-rv-match="'+t.id+'" style="padding:8px 12px;font-size:11.5px;white-space:nowrap">مطابقة الأتعاب</button>':'')+'<button data-rv-state="'+t.id+'" title="تحديث حالة المعاملة كما هي في منصة إنفاذ" style="padding:8px 11px;font-size:11.5px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-weight:700;cursor:pointer;font-family:inherit;white-space:nowrap">تحديث حالة إنفاذ</button></div></div></div>'; }).join('');
    } else if(status==='invoiced'){
      cols='minmax(130px,1.2fr) minmax(105px,.9fr) minmax(112px,.95fr) minmax(96px,.8fr) minmax(132px,1fr)';
      heads=['رقم الصك','تاريخ الاكتمال','الأتعاب','الضريبة','الإجمالي المستحق'];
      var num2=function(v){ return Number(v||0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}); };
      rows=revGroups('invoiced').map(function(g){
        var iv=g.txs[0].inv||{}, age=revAge(iv.uploadedAt);
        var ckey=g.key||g.po, col=!!FINX.rcol[ckey];
        var fu=g.txs[0].followups||[], lf=fu.length?fu[fu.length-1].date:null;
        var sB=0,sV=0;
        var body=g.txs.map(function(t){ var b=revBase(t), v=revVat(t);
          sB+=b; sV+=v;
          return '<div class="row" style="grid-template-columns:'+cols+';min-height:42px">'+
            '<div class="td"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px">'+esc(t.deed)+'</span></div>'+
            '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)" dir="ltr">'+revDate(t.doneAt)+'</div>'+
            '<div class="td" style="'+C+';font-size:12px;color:var(--text)" dir="ltr">'+num2(b)+'</div>'+
            '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)" dir="ltr">'+num2(v)+'</div>'+
            '<div class="td" style="'+C+';font-weight:700;font-size:12.5px;color:var(--heading)" dir="ltr">'+num2(b+v+revKey(t))+'</div>'+
          '</div>'; }).join('');
        var head='<div class="row" data-rv-col="'+esc(ckey)+'" role="button" style="grid-template-columns:1fr;min-height:48px;background:var(--surface-2);cursor:pointer"><div class="td"><div style="display:flex;width:100%;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap">'+
          '<span style="display:inline-flex;align-items:center;gap:9px;flex-wrap:wrap"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" style="flex:none;color:var(--text-3);transform:rotate('+(col?'0':'-90')+'deg)"><path d="M9 18l6-6-6-6"/></svg>'+
            '<span style="font-size:12.5px;font-weight:800;color:var(--heading)">فاتورة <span dir="ltr">'+esc(iv.no||'—')+'</span></span>'+
            '<span style="font-size:11px;color:var(--text-3)">'+g.txs.length+' معاملة · الإجمالي <b dir="ltr" style="color:var(--heading)">'+num2(g.total)+'</b> ر.س · عمر المستحق '+age+' يوماً'+(lf?' · آخر متابعة '+revDate(lf):'')+'</span>'+
          '</span>'+
          '<span style="display:inline-flex;gap:6px;margin-inline-start:auto"><button class="primary" data-rv-pay="'+esc(iv.no||g.po)+'" style="padding:7px 12px;font-size:11.5px;white-space:nowrap">تسجيل التحويل</button>'+
          '<button data-rv-follow="'+esc(iv.no||g.po)+'" style="padding:7px 11px;font-size:11.5px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-weight:700;cursor:pointer;font-family:inherit;white-space:nowrap">متابعة'+(fu.length?' ('+fu.length+')':'')+'</button></span>'+
        '</div></div></div>';
        var tot='<div class="row" style="grid-template-columns:'+cols+';min-height:36px;background:color-mix(in srgb,var(--gold) 7%,transparent)">'+
          '<div class="td" style="font-size:11px;font-weight:800;color:var(--heading)">إجمالي الفاتورة</div><div class="td"></div>'+
          '<div class="td" style="'+C+';font-size:11.5px;font-weight:700;color:var(--text)" dir="ltr">'+num2(sB)+'</div>'+
          '<div class="td" style="'+C+';font-size:11.5px;font-weight:700;color:var(--text-2)" dir="ltr">'+num2(sV)+'</div>'+
          '<div class="td" style="'+C+';font-size:12.5px;font-weight:800;color:var(--heading)" dir="ltr">'+num2(g.total)+'</div>'+
        '</div>';
        return head+(col?'':body+tot);
      }).join('');
    } else if(status==='collected'){
      cols='minmax(100px,.85fr) minmax(134px,1fr) minmax(70px,.55fr) minmax(104px,.82fr) minmax(112px,.9fr) minmax(120px,.95fr) minmax(105px,.85fr) minmax(92px,.7fr)';
      heads=['رقم الطلب','رقم الصك','المدينة','تاريخ الاكتمال','إجمالي الأتعاب','الفاتورة','تاريخ التحويل','الحالة'];
      rows=list.map(function(t){ return '<div class="row" style="grid-template-columns:'+cols+';min-height:52px">'+po(t)+deed(t)+city(t)+done(t)+gross(t)+invc(t)+
        '<div class="td" style="'+C+';font-size:12px;color:var(--text-2)" dir="ltr">'+revDate(t.pay.date)+'</div>'+
        '<div class="td" style="'+C+'">'+pill(REV_ST.collected.t,REV_ST.collected.c)+'</div></div>'; }).join('');
    } else {
      cols='minmax(105px,.9fr) minmax(125px,1fr) minmax(72px,.58fr) minmax(105px,.82fr) minmax(210px,1.5fr) minmax(150px,1fr)';
      heads=['رقم الطلب','رقم الصك','المدينة','تاريخ الاكتمال','سبب التوقف','الإجراء'];
      rows=list.map(function(t){ return '<div class="row" style="grid-template-columns:'+cols+';min-height:48px">'+po(t)+deed(t)+city(t)+done(t)+
        '<div class="td" style="font-size:12px;color:var(--text-2)">'+esc(t.exReason||'')+'</div>'+
        '<div class="td" style="'+C+'"><button data-rv-state="'+t.id+'" style="padding:7px 12px;font-size:11.5px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-weight:700;cursor:pointer;font-family:inherit;white-space:nowrap">استدعاء — تحديث الحالة</button></div></div>'; }).join('');
    }
    var thead='<div class="thead" style="grid-template-columns:'+cols+'">'+heads.map(function(h,i){ return '<div class="th"'+(i===0&&status==='excluded'?'':' style="text-align:center"')+'>'+h+'</div>'; }).join('')+'</div>';
    return '<div class="card"><div class="scroll"><div class="grid" style="min-width:100%">'+thead+'<div>'+(rows||'')+'</div></div></div></div>'+(list.length?'':finEmpty('لا معاملات مطابقة في هذا التبويب'));
  }
  function finRevenue(){
    var counts={ inprogress:revList('inprogress').length, eligible:revList('eligible').length, awaiting:revList('awaiting').length, invoiced:revList('invoiced').length, collected:revList('collected').length, excluded:revList('excluded').length };
    var tabs=[['inprogress','تحت الدراسة'],['eligible','مؤهلة للفوترة'],['awaiting','مساعد الفوترة'],['invoiced','بانتظار التحصيل'],['collected','محصّلة'],['excluded','متوقفة']];
    var subs='<div style="display:flex;flex-wrap:wrap;gap:7px;margin-bottom:14px">'+tabs.map(function(tb){ var on=FINX.rtab===tb[0]; var n=counts[tb[0]];
      return '<button data-rvtab="'+tb[0]+'" style="display:inline-flex;align-items:center;gap:7px;padding:8px 15px;border-radius:99px;border:1px solid '+(on?'var(--ink)':'var(--border-2)')+';background:'+(on?'var(--ink)':'var(--surface)')+';color:'+(on?'#fff':'var(--text-2)')+';font-family:inherit;font-size:12.5px;font-weight:700;cursor:pointer">'+tb[1]+'<span style="background:'+(on?'rgba(255,255,255,.22)':'var(--surface-2)')+';color:'+(on?'#fff':'var(--text-3)')+';border-radius:99px;min-width:18px;height:18px;display:inline-grid;place-items:center;font-size:10.5px;padding:0 5px">'+n+'</span></button>'; }).join('')+'</div>';
    var cities=[]; REV_TX.forEach(function(t){ if(cities.indexOf(t.city)<0) cities.push(t.city); });
    var cityOpts='<option value="">كل المدن</option>'+cities.map(function(c){ return '<option value="'+esc(c)+'"'+(FINX.rcity===c?' selected':'')+'>'+esc(c)+'</option>'; }).join('');
    var perOpts=[['','كل الفترات'],['30','آخر ٣٠ يوماً'],['90','آخر ٩٠ يوماً']].map(function(o){ return '<option value="'+o[0]+'"'+(FINX.rperiod===o[0]?' selected':'')+'>'+o[1]+'</option>'; }).join('');
    var filters='<div style="display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-bottom:14px">'+
      '<input data-rvq value="'+esc(FINX.rq||'')+'" placeholder="بحث: رقم الطلب · رقم الصك · رقم الفاتورة" style="'+INP_STYLE+';flex:1;min-width:240px">'+
      '<select data-rvcity style="'+FIN_SEL_STYLE+';min-width:130px">'+cityOpts+'</select>'+
      '<select data-rvperiod style="'+FIN_SEL_STYLE+';min-width:130px">'+perOpts+'</select>'+
    '</div>';
    var extra='';
    if(FINX.rtab==='awaiting'){
      var av=revList('awaiting'), sel=av.filter(function(t){ return FINX.rsel[t.id]; });
      var selTot=sel.reduce(function(a,t){ return a+revGross(t); },0);
      var allOn=av.length&&av.every(function(t){ return FINX.rsel[t.id]; });
      extra='<div style="display:flex;flex-wrap:wrap;align-items:center;gap:9px;margin-bottom:10px">'+
        '<button data-rv-selall="1"'+(av.length?'':' style="opacity:.5;pointer-events:none"')+' style="padding:9px 14px;font-size:12px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-weight:700;cursor:pointer;font-family:inherit;white-space:nowrap">'+(allOn?'إلغاء التحديد':'تحديد الكل ('+av.length+')')+'</button>'+
        '<button class="primary" data-rv-invsel="1"'+(sel.length?'':' style="opacity:.5;pointer-events:none"')+' style="padding:9px 16px;font-size:12.5px;white-space:nowrap">تسجيل فاتورة للمحدد'+(sel.length?' ('+sel.length+' — '+Number(selTot).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})+' ر.س)':'')+'</button>'+
        '<span style="font-size:11px;color:var(--text-3)">اختر المعاملات الجاهزة وأضفها لفاتورة واحدة — التجميع تحت أمر العمل للعرض فقط.</span>'+
      '</div>';
    }
    return subs+filters+extra+finRevRows(FINX.rtab);
  }
  function revClose(){ var m=document.getElementById('revModal'); if(m) m.remove(); REVDRAFT=null; }
  function revErr(msg){ var e=document.getElementById('revModalErr'); if(e){ e.textContent=msg||''; e.hidden=!msg; if(msg) e.scrollIntoView({block:'nearest'}); } }
  function revModalShell(title,sub,body,foot){
    var ex=document.getElementById('revModal'); if(ex) ex.remove(); var ov=document.createElement('div'); ov.className='modal-overlay'; ov.id='revModal';
    ov.innerHTML='<div class="modal" role="dialog" aria-modal="true" style="max-width:560px">'+
      '<div class="modal-head"><div><h2>'+title+'</h2><div style="font-size:11.5px;color:var(--text-3);margin-top:3px">'+sub+'</div></div><button class="modal-x" data-rv-x type="button">✕</button></div>'+
      '<div class="modal-body" style="max-height:76vh;overflow-y:auto"><div class="modal-error" id="revModalErr" hidden></div>'+body+'</div>'+
      '<div class="modal-foot">'+foot+'</div></div>';
    document.body.appendChild(ov);
    ov.addEventListener('click', function(e){ if(e.target.id==='revModal'||e.target.closest('[data-rv-x]')||e.target.closest('[data-rv-cancel]')) revClose(); });
    return ov;
  }
  function revFld(label,req,inner){ return '<div style="margin-top:12px"><label class="tf-lbl">'+label+(req?' <span style="color:#c0553d">*</span>':'')+'</label>'+inner+'</div>'; }
  function revFileBtn(key,accept){ var on=REVDRAFT&&REVDRAFT.files&&REVDRAFT.files[key];
    var ic=on?'<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>':'<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8l-5-5-5 5"/><path d="M12 3v12"/></svg>';
    return '<button type="button" data-rv-file="'+key+'" style="display:flex;align-items:center;gap:8px;width:100%;margin-top:4px;padding:10px 12px;border:1px dashed '+(on?'#3f8f5f':'var(--border-2)')+';border-radius:9px;background:'+(on?'color-mix(in srgb,#3f8f5f 8%,transparent)':'var(--surface-2)')+';cursor:pointer;font-family:inherit;font-size:12px;font-weight:600;color:'+(on?'#2f7a4d':'var(--text-2)')+'">'+ic+'<span>'+(on?'مُرفق — '+accept:'إرفاق '+accept)+'</span></button>'; }
  function revToday(){ return isoFromTs(taskLive()); }
  function revBindFiles(){ var m=document.getElementById('revModal'); if(!m) return; m.querySelectorAll('[data-rv-file]').forEach(function(b){ b.addEventListener('click', function(){ var k=b.getAttribute('data-rv-file'); REVDRAFT.files[k]=REVDRAFT.files[k]?false:('ملف.pdf'); var accept=b.querySelector('span').textContent.replace(/^مُرفق — |^إرفاق /,''); var wrap=document.createElement('div'); wrap.innerHTML=revFileBtn(k,accept); var nb=wrap.firstChild; b.parentNode.replaceChild(nb,b); nb.addEventListener('click', arguments.callee); }); }); }
  function revInvTotalsHtml(){
    var base=(REVDRAFT.valFee||0)+(REVDRAFT.survFee||0); var vat=Math.round(base*REV_VAT*100)/100; var key=(REVDRAFT.keyFee||0); var tot=base+vat+key;
    var r=function(l,v,b){ return '<div style="display:flex;justify-content:space-between;padding:8px 13px;'+(b?'border-top:1px solid var(--border);background:var(--surface-2)':'')+'"><span style="font-size:'+(b?'12.5':'12')+'px;font-weight:'+(b?'800':'500')+';color:'+(b?'var(--heading)':'var(--text-2)')+'">'+l+'</span><span style="font-size:'+(b?'13.5':'12.5')+'px;font-weight:700;color:var(--heading)">'+fmtSAR(v)+'</span></div>'; };
    return r('المجموع الخاضع للضريبة (تقييم + رفع مساحي)',base)+r('ضريبة القيمة المضافة (15%)',vat)+(key>0?r('أتعاب المفاتيح (شاملة الضريبة)',key):'')+r('الإجمالي المستحق',tot,true);
  }
  var REV_INFATH=['مرفوعة على إنفاذ','بانتظار الفاتورة','ملغاة من إنفاذ','متعذّرة'];
  function revCanMatch(t){ return t.infath==='بانتظار الفاتورة'; }
  function openRevState(id){
    var t=REV_TX.find(function(x){ return x.id===id; }); if(!t) return;
    REVDRAFT={ kind:'state', txId:id, st:t.infath||'مرفوعة على إنفاذ', note:'' };
    var opts=REV_INFATH.map(function(s){ return '<option value="'+esc(s)+'"'+(REVDRAFT.st===s?' selected':'')+'>'+esc(s)+'</option>'; }).join('');
    var body=finInfo('يدخل موظف المالية منصة إنفاذ كل فترة ويحدّث الحالة هنا. عند اختيار «بانتظار الفاتورة» تُفتح <b>مطابقة الأتعاب</b> مباشرة. و«ملغاة» أو «متعذّرة» تُخرج المعاملة إلى المستبعدة.')+
      '<div style="display:flex;justify-content:space-between;padding:9px 12px;background:var(--surface-2);border:1px solid var(--border);border-radius:9px"><span style="font-size:11.5px;color:var(--text-2)">المعاملة</span><span style="font-size:12px;font-weight:700;color:var(--heading)" dir="ltr">'+esc(t.po)+' · '+esc(t.deed)+'</span></div>'+
      revFld('الحالة في منصة إنفاذ',true,'<select data-rv-st style="'+FIN_SEL_STYLE+';width:100%;margin-top:4px">'+opts+'</select>')+
      revFld('ملاحظة',false,'<textarea data-rv-stnote rows="2" placeholder="اختيارية" style="'+INP_STYLE+';width:100%;margin-top:4px;resize:vertical"></textarea>');
    var foot='<button class="btn-ghost" data-rv-cancel type="button">إلغاء</button><button class="primary" data-rv-stsave type="button">تحديث حالة إنفاذ</button>';
    revModalShell('تحديث حالة المعاملة في إنفاذ',esc(t.po)+' · '+esc(t.deed),body,foot);
    var m=document.getElementById('revModal');
    m.querySelector('[data-rv-st]').addEventListener('change', function(e){ REVDRAFT.st=e.target.value; });
    m.querySelector('[data-rv-stnote]').addEventListener('input', function(e){ REVDRAFT.note=e.target.value; });
    m.querySelector('[data-rv-stsave]').addEventListener('click', function(){
      var s=REVDRAFT.st, nt=(REVDRAFT.note||'').trim();
      t.infath=s;
      if(s==='ملغاة من إنفاذ'||s==='متعذّرة'){ t.status='excluded'; t.exReason=(s==='متعذّرة'?'متعذّرة من المركز':'جاهزة ولم تُرفع على نظام مركز التصفية')+(nt?' · '+nt:''); }
      else if(t.status==='excluded'){ t.status='eligible'; t.doneAt=t.doneAt||t.regAt; delete t.exReason; }
      revClose(); renderFinance(); showToast('حُدّثت حالة إنفاذ للمعاملة '+t.deed+' إلى «'+s+'».');
      if(revCanMatch(t)&&t.status==='eligible') setTimeout(function(){ openRevMatch(t.id); },220);
    });
  }
  function openRevMatch(id){
    var t=REV_TX.find(function(x){ return x.id===id; }); if(!t) return;
    REVDRAFT={ kind:'match', txId:id, valFee:t.valFee||0, keyFee:t.keyFee||0, survFee:t.survFee||0 };
    var numInp=function(key,val){ return '<input data-rv-num="'+key+'" type="number" min="0" value="'+val+'" dir="ltr" style="'+INP_STYLE+';width:100%;margin-top:4px;text-align:start">'; };
    var body=finInfo('أدخل <b>الأرقام المالية</b> من منصة إنفاذ. المساحات تأتي من دراسة الحالة ولا تُدخل هنا. حساب إنفاذ تقريبي (سقوف: تقييم الأرض '+fmtSAR(REV_CEIL.land)+'، تقييم البناء '+fmtSAR(REV_CEIL.building)+') — المرجع الفعلي الفاتورة الضريبية.')+
      '<div style="font-size:12.5px;font-weight:800;color:var(--heading);margin:4px 0 2px">مطابقة الأتعاب</div>'+
      '<div style="display:flex;gap:16px;flex-wrap:wrap;margin-top:12px;padding:9px 12px;background:var(--surface-2);border:1px solid var(--border);border-radius:9px">'+
        '<span style="font-size:11px;color:var(--text-3)">من دراسة الحالة:</span>'+
        '<span style="font-size:11.5px;color:var(--text-2)">مساحة الأرض <b dir="ltr" style="color:var(--heading)">'+Number(t.land||0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})+'</b> م٢</span>'+
        '<span style="font-size:11.5px;color:var(--text-2)">مسطح البناء <b dir="ltr" style="color:var(--heading)">'+Number(t.bld||0).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})+'</b> م٢</span>'+
      '</div>'+
      revFld('أتعاب التقييم',true,numInp('valFee',REVDRAFT.valFee))+
      revFld('أتعاب استلام المفاتيح — شاملة الضريبة'+(t.hasKey?' <span style="color:#c0553d">*</span>':''),false,numInp('keyFee',REVDRAFT.keyFee))+
      '<div style="font-size:10px;color:var(--text-3);margin-top:3px;line-height:1.6">مبلغ ثابت شامل الضريبة (عادةً 250 ر.س)؛ اتركه صفراً إن لم توجد أتعاب مفاتيح.</div>'+
      revFld('أتعاب الرفع المساحي (المكتب الهندسي)'+(t.hasSurvey?' <span style="color:#c0553d">*</span>':''),false,numInp('survFee',REVDRAFT.survFee))+
      '<div id="rvTot" style="border:1px solid var(--border);border-radius:10px;overflow:hidden;margin-top:13px">'+revInvTotalsHtml()+'</div>'+
      '<div style="font-size:10.5px;color:var(--text-3);margin-top:10px;line-height:1.7">باعتماد المطابقة تنتقل المعاملة إلى تبويب «مساعد الفوترة» بحالة «بانتظار رفع الفاتورة» — حيث تُفوتر لاحقاً على البرنامج المحاسبي وتُرفع على إنفاذ.</div>';
    var foot='<button class="btn-ghost" data-rv-cancel type="button">إلغاء</button><button class="primary" data-rv-matchsave type="button"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg><span>اعتماد المطابقة ← مساعد الفوترة</span></button>';
    revModalShell('مطابقة الأتعاب','معاملة '+t.po+' · صك '+t.deed+' · '+t.city,body,foot);
    var m=document.getElementById('revModal');
    m.querySelectorAll('[data-rv-num]').forEach(function(inp){ inp.addEventListener('input', function(){ REVDRAFT[inp.getAttribute('data-rv-num')]=+inp.value||0; var el=document.getElementById('rvTot'); if(el) el.innerHTML=revInvTotalsHtml(); }); });
    m.querySelector('[data-rv-matchsave]').addEventListener('click', function(){ saveRevMatch(id); });
  }
  function saveRevMatch(id){
    var t=REV_TX.find(function(x){ return x.id===id; }); if(!t||!REVDRAFT) return; var d=REVDRAFT;
    if(!(d.valFee>0)) return revErr('أتعاب التقييم مطلوبة.');
    if(t.hasSurvey&&!(d.survFee>0)) return revErr('هذه المعاملة تستحق أتعاب مكتب هندسي من إنفاذ — أدخل أتعاب الرفع المساحي.');
    if(t.hasKey&&!(d.keyFee>0)) return revErr('هذه المعاملة تستحق أتعاب استلام مفتاح من إنفاذ — أدخل أتعاب استلام المفاتيح.');
    t.valFee=d.valFee; t.keyFee=d.keyFee; t.survFee=d.survFee; t.status='awaiting';
    revClose(); FINX.rtab='awaiting'; renderFinance(); showToast('طُوبِقت أتعاب معاملة '+t.po+' ونُقلت إلى مساعد الفوترة.');
  }
  function openRevInvoicePO(po){
    var gl=po?REV_TX.filter(function(t){ return t.status==='awaiting'&&t.po===po; }):REV_TX.filter(function(t){ return t.status==='awaiting'&&FINX.rsel[t.id]; });
    if(!gl.length) return;
    var tot=gl.reduce(function(a,t){ return a+revGross(t); },0);
    REVDRAFT={ kind:'invpo', po:(po||null), no:'', date:revToday(), uploadedAt:revToday(), files:{ inv:false, shot:false } };
    var body=finInfo('الفاتورة تشمل المعاملات التي اخترتها. أنشئها في البرنامج المحاسبي بقيمة الإجمالي المستحق، ثم ارفعها على إنفاذ وسجّل بياناتها هنا.')+
      '<div style="display:flex;justify-content:space-between;align-items:center;background:var(--surface-2);border:1px solid var(--border);border-radius:10px;padding:11px 14px"><span style="font-size:12px;color:var(--text-2)">'+gl.length+' معاملة · الإجمالي المستحق</span><span style="font-size:15px;font-weight:800;color:var(--heading)" dir="ltr">'+Number(tot).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})+' ر.س</span></div>'+
      revFld('رقم الفاتورة الضريبية',true,'<input data-rv-no value="" dir="ltr" placeholder="رقم فريد من المحاسبي" style="'+INP_STYLE+';width:100%;margin-top:4px">')+
      '<div id="rvNoWarn"></div>'+
      revFld('تاريخ الفاتورة',true,'<input data-rv-date type="date" value="'+REVDRAFT.date+'" style="'+INP_STYLE+';width:100%;margin-top:4px">')+
      '<div id="rvYearWarn"></div>'+
      revFld('تاريخ الرفع على إنفاذ',true,'<input data-rv-up type="date" value="'+REVDRAFT.uploadedAt+'" style="'+INP_STYLE+';width:100%;margin-top:4px">')+
      revFld('مرفق الفاتورة الضريبية (PDF)',true,revFileBtn('inv','فاتورة PDF'))+
      revFld('لقطة شاشة إنفاذ',false,revFileBtn('shot','صورة'));
    var foot='<button class="btn-ghost" data-rv-cancel type="button">إلغاء</button><button class="primary" data-rv-posave type="button"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg><span>تسجيل الفاتورة ← بانتظار التحصيل</span></button>';
    revModalShell('تسجيل الفاتورة',gl.length+' معاملة مختارة',body,foot);
    var m=document.getElementById('revModal');
    var no=m.querySelector('[data-rv-no]'); no.addEventListener('input', function(){ REVDRAFT.no=no.value; var w=document.getElementById('rvNoWarn'); w.innerHTML=revInvExists(no.value,null)?'<div style="margin-top:6px;font-size:11.5px;color:#c0553d;font-weight:700">⚠ رقم الفاتورة مستخدم مسبقاً.</div>':''; });
    var dt=m.querySelector('[data-rv-date]'); dt.addEventListener('change', function(){ REVDRAFT.date=dt.value; var w=document.getElementById('rvYearWarn'); var y=(dt.value||'').slice(0,4); w.innerHTML=(y&&+y<new Date(taskLive()).getFullYear())?'<div style="margin-top:6px;font-size:11.5px;color:#8a5e14;font-weight:700">⚠ إنفاذ تشترط فاتورة مصدَرة في السنة الجارية.</div>':''; });
    m.querySelector('[data-rv-up]').addEventListener('change', function(e){ REVDRAFT.uploadedAt=e.target.value; });
    revBindFiles();
    m.querySelector('[data-rv-posave]').addEventListener('click', function(){
      var d=REVDRAFT;
      if(!(d.no||'').trim()) return revErr('رقم الفاتورة مطلوب.');
      if(revInvExists(d.no,null)) return revErr('رقم الفاتورة مستخدم مسبقاً — لا يمكن تكراره.');
      if(!d.date) return revErr('تاريخ الفاتورة مطلوب.');
      if(!d.uploadedAt) return revErr('تاريخ الرفع على إنفاذ مطلوب.');
      if(!d.files.inv) return revErr('أرفق نسخة الفاتورة الضريبية (PDF).');
      gl.forEach(function(t){ t.inv={ no:d.no.trim(), date:d.date, uploadedAt:d.uploadedAt }; t.status='invoiced'; t.followups=t.followups||[]; delete FINX.rsel[t.id]; });
      revClose(); FINX.rtab='invoiced'; renderFinance(); showToast('سُجّلت الفاتورة '+d.no.trim()+' ('+gl.length+' معاملة) ونُقلت لبانتظار التحصيل.');
    });
  }
  function openRevInvoice(id){
    var t=REV_TX.find(function(x){ return x.id===id; }); if(!t) return;
    REVDRAFT={ kind:'inv', txId:id, no:'', date:revToday(), uploadedAt:revToday(), files:{ inv:false, shot:false } };
    var body=finInfo('بعد إنشاء الفاتورة على البرنامج المحاسبي ورفعها على منصة إنفاذ، حدِّث الإجراء هنا بتدوين بياناتها. تنتقل المعاملة إلى «بانتظار التحصيل».')+
      '<div style="display:flex;justify-content:space-between;align-items:center;background:var(--surface-2);border:1px solid var(--border);border-radius:10px;padding:11px 14px;margin-bottom:2px"><span style="font-size:12px;color:var(--text-2)">إجمالي الأتعاب المطابَق (شامل الضريبة)</span><span style="font-size:15px;font-weight:800;color:var(--heading)">'+fmtSAR(revGross(t))+'</span></div>'+
      '<div style="font-size:12.5px;font-weight:800;color:var(--heading);margin:16px 0 2px">بيانات الفاتورة</div>'+
      revFld('رقم الفاتورة',true,'<input data-rv-no value="" dir="ltr" placeholder="رقم فريد من المحاسبي" style="'+INP_STYLE+';width:100%;margin-top:4px">')+
      '<div id="rvNoWarn"></div>'+
      revFld('تاريخ الفاتورة',true,'<input data-rv-date type="date" value="'+REVDRAFT.date+'" style="'+INP_STYLE+';width:100%;margin-top:4px">')+
      '<div id="rvYearWarn"></div>'+
      revFld('تاريخ الرفع على إنفاذ',true,'<input data-rv-up type="date" value="'+REVDRAFT.uploadedAt+'" style="'+INP_STYLE+';width:100%;margin-top:4px">')+
      revFld('نسخة الفاتورة الضريبية (PDF)',true,revFileBtn('inv','فاتورة PDF'))+
      revFld('لقطة شاشة إنفاذ',false,revFileBtn('shot','صورة'))+
      '<div style="font-size:10.5px;color:var(--text-3);margin-top:8px;line-height:1.7">لقطة الشاشة اختيارية — الفاتورة الضريبية تحمل نفس الأرقام.</div>';
    var foot='<button class="btn-ghost" data-rv-cancel type="button">إلغاء</button><button class="primary" data-rv-invsave type="button"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg><span>تحديث الإجراء ← بانتظار التحصيل</span></button>';
    revModalShell('تسجيل الفاتورة ورفعها','معاملة '+t.po+' · صك '+t.deed+' · '+t.city,body,foot);
    var m=document.getElementById('revModal');
    var no=m.querySelector('[data-rv-no]'); no.addEventListener('input', function(){ REVDRAFT.no=no.value; var w=document.getElementById('rvNoWarn'); w.innerHTML=revInvExists(no.value,id)?'<div style="margin-top:6px;font-size:11.5px;color:#c0553d;font-weight:700">⚠ رقم الفاتورة مستخدم مسبقاً على معاملة أخرى.</div>':''; });
    var dt=m.querySelector('[data-rv-date]'); dt.addEventListener('change', function(){ REVDRAFT.date=dt.value; var w=document.getElementById('rvYearWarn'); var y=(dt.value||'').slice(0,4); w.innerHTML=(y&&+y<new Date(taskLive()).getFullYear())?'<div style="margin-top:6px;font-size:11.5px;color:#8a5e14;font-weight:700">⚠ الفاتورة مؤرخة بسنة سابقة — إنفاذ تشترط السنة الجارية (تحذير فقط).</div>':''; });
    var up=m.querySelector('[data-rv-up]'); up.addEventListener('change', function(){ REVDRAFT.uploadedAt=up.value; });
    revBindFiles();
    m.querySelector('[data-rv-invsave]').addEventListener('click', function(){ saveRevInvoice(id); });
  }
  function saveRevInvoice(id){
    var t=REV_TX.find(function(x){ return x.id===id; }); if(!t||!REVDRAFT) return; var d=REVDRAFT;
    if(!(d.no||'').trim()) return revErr('رقم الفاتورة مطلوب.');
    if(revInvExists(d.no,id)) return revErr('رقم الفاتورة مستخدم مسبقاً — لا يمكن تكراره على معاملتين.');
    if(!d.date) return revErr('تاريخ الفاتورة مطلوب.');
    if(!d.uploadedAt) return revErr('تاريخ الرفع على إنفاذ مطلوب.');
    if(!d.files.inv) return revErr('أرفق نسخة الفاتورة الضريبية (PDF).');
    t.inv={ no:d.no.trim(), date:d.date, uploadedAt:d.uploadedAt }; t.status='invoiced'; t.followups=t.followups||[];
    revClose(); FINX.rtab='invoiced'; renderFinance(); showToast('سُجّلت الفاتورة '+t.inv.no+' ونُقلت المعاملة لبانتظار التحصيل.');
  }
  function openRevPay(key){
    var gl=REV_TX.filter(function(x){ return x.status==='invoiced'&&((x.inv&&x.inv.no===key)||x.po===key); }); if(!gl.length) return;
    var t=gl[0]; var due=gl.reduce(function(a,x){ return a+revGross(x); },0);
    REVDRAFT={ kind:'pay', key:key, date:revToday(), amount:'', ref:'', files:{ receipt:false } };
    var body=finInfo('تحويل واحد لكل فاتورة. المبلغ يُنقل يدوياً من الإيصال؛ النظام يقارنه بإجمالي الفاتورة وينبّه عند الاختلاف — دون منع الحفظ.')+
      '<div style="display:flex;justify-content:space-between;align-items:center;background:var(--surface-2);border:1px solid var(--border);border-radius:10px;padding:11px 14px;margin-bottom:2px"><span style="font-size:12px;color:var(--text-2)">إجمالي الفاتورة المستحق</span><span style="font-size:15px;font-weight:800;color:var(--heading)">'+fmtSAR(due)+'</span></div>'+
      revFld('تاريخ التحويل',true,'<input data-rv-pdate type="date" value="'+REVDRAFT.date+'" style="'+INP_STYLE+';width:100%;margin-top:4px">')+
      revFld('المبلغ المحوَّل',true,'<input data-rv-amt type="number" min="0" value="" dir="ltr" placeholder="من الإيصال" style="'+INP_STYLE+';width:100%;margin-top:4px;text-align:start">')+
      '<div id="rvMis"></div>'+
      revFld('إيصال التحويل',true,revFileBtn('receipt','إيصال'))+
      revFld('رقم مرجع التحويل',false,'<input data-rv-ref value="" dir="ltr" placeholder="اختياري" style="'+INP_STYLE+';width:100%;margin-top:4px">');
    var foot='<button class="btn-ghost" data-rv-cancel type="button">إلغاء</button><button class="primary" data-rv-paysave type="button"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg><span>تسجيل التحصيل</span></button>';
    revModalShell('تسجيل التحويل','فاتورة '+(t.inv?t.inv.no:key)+' · '+gl.length+' معاملة',body,foot);
    var m=document.getElementById('revModal');
    var amt=m.querySelector('[data-rv-amt]'); amt.addEventListener('input', function(){ REVDRAFT.amount=amt.value; var w=document.getElementById('rvMis'); var v=+amt.value; w.innerHTML=(amt.value!==''&&v!==due)?'<div style="margin-top:6px;font-size:11.5px;color:#8a5e14;font-weight:700">⚠ المبلغ يختلف عن إجمالي الفاتورة ('+fmtSAR(due)+') — تحقق قبل الحفظ.</div>':''; });
    m.querySelector('[data-rv-pdate]').addEventListener('change', function(e){ REVDRAFT.date=e.target.value; });
    m.querySelector('[data-rv-ref]').addEventListener('input', function(e){ REVDRAFT.ref=e.target.value; });
    revBindFiles();
    m.querySelector('[data-rv-paysave]').addEventListener('click', function(){ saveRevPay(key); });
  }
  function saveRevPay(key){
    var gl=REV_TX.filter(function(x){ return x.status==='invoiced'&&((x.inv&&x.inv.no===key)||x.po===key); }); if(!gl.length||!REVDRAFT) return; var d=REVDRAFT;
    if(!d.date) return revErr('تاريخ التحويل مطلوب.');
    if(!(+d.amount>0)) return revErr('مبلغ التحويل مطلوب.');
    if(!d.files.receipt) return revErr('أرفق إيصال التحويل.');
    gl.forEach(function(t){ t.pay={ date:d.date, amount:+d.amount, ref:(d.ref||'').trim() }; t.status='collected'; });
    revClose(); FINX.rtab='collected'; renderFinance(); showToast('وُثّق تحصيل الفاتورة '+key+' ('+gl.length+' معاملة).');
  }
  function openRevFollow(key){
    var t=REV_TX.find(function(x){ return x.status==='invoiced'&&((x.inv&&x.inv.no===key)||x.po===key); }); if(!t) return;
    REVDRAFT={ kind:'follow', txId:t.id, date:revToday(), channel:'support', notes:'' };
    var hist=(t.followups||[]).length?'<div style="border:1px solid var(--border);border-radius:10px;overflow:hidden;margin-bottom:6px">'+t.followups.map(function(f){ return '<div style="display:flex;gap:10px;padding:9px 13px;border-bottom:1px solid var(--border);font-size:11.5px"><span dir="ltr" style="color:var(--text-3);flex:none">'+revDate(f.date)+'</span><span style="color:var(--ink);font-weight:700;flex:none">'+esc(REV_CH[f.channel]||f.channel)+'</span><span style="color:var(--text-2)">'+esc(f.notes||'')+'</span></div>'; }).join('')+'</div>':'<div style="font-size:12px;color:var(--text-3);padding:8px 0">لا محاولات متابعة مسجّلة بعد.</div>';
    var chOpts=Object.keys(REV_CH).map(function(k){ return '<option value="'+k+'"'+(k==='support'?' selected':'')+'>'+REV_CH[k]+'</option>'; }).join('');
    var body=finInfo('سجل المتابعة يوثّق محاولات المطالبة بالتحصيل — لا تنبيهات آلية ولا عتبة تأخير. الغرض معرفة ما لم يُتابَع بعد.')+
      '<div style="font-size:12px;font-weight:700;color:var(--heading);margin-bottom:6px">المتابعات السابقة</div>'+hist+
      revFld('تاريخ المتابعة',true,'<input data-rv-fdate type="date" value="'+REVDRAFT.date+'" style="'+INP_STYLE+';width:100%;margin-top:4px">')+
      revFld('القناة',true,'<select data-rv-fch style="'+FIN_SEL_STYLE+';width:100%;margin-top:4px">'+chOpts+'</select>')+
      revFld('ملاحظات',false,'<textarea data-rv-fnotes rows="3" placeholder="تفاصيل المحاولة" style="'+INP_STYLE+';width:100%;margin-top:4px;resize:vertical"></textarea>');
    var foot='<button class="btn-ghost" data-rv-cancel type="button">إلغاء</button><button class="primary" data-rv-followsave type="button">تسجيل المتابعة</button>';
    revModalShell('تسجيل متابعة تحصيل','فاتورة '+(t.inv?t.inv.no:key),body,foot);
    var m=document.getElementById('revModal');
    m.querySelector('[data-rv-fdate]').addEventListener('change', function(e){ REVDRAFT.date=e.target.value; });
    m.querySelector('[data-rv-fch]').addEventListener('change', function(e){ REVDRAFT.channel=e.target.value; });
    m.querySelector('[data-rv-fnotes]').addEventListener('input', function(e){ REVDRAFT.notes=e.target.value; });
    m.querySelector('[data-rv-followsave]').addEventListener('click', function(){ saveRevFollow(id); });
  }
  function saveRevFollow(id){
    var t=REV_TX.find(function(x){ return x.id===id; }); if(!t||!REVDRAFT) return; var d=REVDRAFT;
    if(!d.date) return revErr('تاريخ المتابعة مطلوب.');
    t.followups=t.followups||[]; t.followups.push({ date:d.date, channel:d.channel, notes:(d.notes||'').trim() });
    revClose(); renderFinance(); showToast('سُجّلت محاولة متابعة لمعاملة '+t.po+'.');
  }
  function finAccount(){ var p=finCur(); if(!p) return '<div class="card"><div class="empty"><div class="t">اختر مورّداً من القائمة</div></div></div>'; return p.kind==='client'?finRevenue():finCostAccount(p); }
  function finxClick(e){
    var ftab=e.target.closest('[data-fintab]'); if(ftab){ FINX.tab=ftab.getAttribute('data-fintab'); FINX.party=null; FINX.q=''; FINX.st=''; FINX.dq=''; renderFinance(); return; }
    if(e.target.closest('[data-fx-back]')){ FINX.party=null; FINX.csel={}; FINX.cstmt=null; FINX.cclose=null; FINX.ctab='open'; FINX.q=''; FINX.st=''; renderFinance(); return; }
    var pb=e.target.closest('[data-fx-party]'); if(pb){ FINX.party=pb.getAttribute('data-fx-party'); FINX.csel={}; FINX.cstmt=null; FINX.cclose=null; FINX.ctab='open'; FINX.q=''; FINX.st=''; renderFinance(); return; }
    var cct=e.target.closest('[data-cctab]'); if(cct){ FINX.ctab=cct.getAttribute('data-cctab'); FINX.cstmt=null; FINX.cclose=null; FINX.cvinv=null; FINX.q=''; renderFinance(); return; }
    if(e.target.closest('[data-fx-cclose]')){ FINX.cstmt=null; FINX.cclose=null; FINX.cvinv=null; renderFinance(); return; }
    var cr=e.target.closest('[data-fx-crow]'); if(cr){ var id=cr.getAttribute('data-fx-crow'); FINX.csel[id]=!FINX.csel[id]; renderFinance(); return; }
    if(e.target.closest('[data-fx-call]')){ var p=finCur(); var ls=finCostList(p).filter(function(l){ return l.st==='due'&&l.amount>0&&costGate(l).ok; }); var allOn=ls.length&&ls.every(function(l){ return FINX.csel[l.id]; }); ls.forEach(function(l){ FINX.csel[l.id]=!allOn; }); renderFinance(); return; }
    var gex=e.target.closest('[data-fx-gateex]'); if(gex){ openGateEx(gex.getAttribute('data-fx-gateex')); return; }
    if(e.target.closest('[data-fx-ccreate]')){ openRunReview(); return; }
    var cs=e.target.closest('[data-fx-cstmt]'); if(cs){ FINX.cstmt=cs.getAttribute('data-fx-cstmt'); FINX.cclose=null; FINX.cvinv=null; renderFinance(); return; }
    var ci=e.target.closest('[data-fx-capprove]'); if(ci){ finStmtApprove(ci.getAttribute('data-fx-capprove')); return; }
    if(e.target.closest('[data-fx-audit]')){ FINX.cauditOpen=!FINX.cauditOpen; renderFinance(); return; }
    if(e.target.closest('[data-fx-creceipt]')){ if(FINX.cclose){ FINX.cclose.receipt=FINX.cclose.receipt?'':('إيصال_تحويل_'+isoFromTs(taskLive())+'.pdf'); renderFinance(); } return; }
    if(e.target.closest('[data-fx-vmatch]')){ if(FINX.cvinv){ FINX.cvinv.matched=!FINX.cvinv.matched; renderFinance(); } return; }
    if(e.target.closest('[data-fx-vfile]')){ if(FINX.cvinv){ FINX.cvinv.file=!FINX.cvinv.file; renderFinance(); } return; }
    if(e.target.closest('[data-fx-vmanual]')){ if(FINX.cvinv){ FINX.cvinv.manual=!FINX.cvinv.manual; renderFinance(); } return; }
    var ccl=e.target.closest('[data-fx-ccancel]'); if(ccl){ finStmtCancel(ccl.getAttribute('data-fx-ccancel')); return; }
    var vrt=e.target.closest('[data-fx-vreturn]'); if(vrt){ finStmtReturn(vrt.getAttribute('data-fx-vreturn')); return; }
    var cvi=e.target.closest('[data-fx-cvinv]'); if(cvi){ finStmtVinv(cvi.getAttribute('data-fx-cvinv')); return; }
    var cp=e.target.closest('[data-fx-cpay]'); if(cp){ finCostPay(cp.getAttribute('data-fx-cpay')); return; }
    var go=e.target.closest('[data-fx-go]');
    if(go){ var q=go.getAttribute('data-fx-go').split(':');
      if(q[0]==='rev'){ FINX.tab='revenue'; FINX.rtab=q[1]; FINX.rq=''; FINX.rcity=''; FINX.rperiod=''; }
      else { FINX.tab='costs'; FINX.party=q[1]; FINX.csel={}; FINX.cclose=null; FINX.cvinv=null;
        if(q[2]==='open'){ FINX.ctab='open'; FINX.cstmt=null; } else { FINX.ctab='orders'; FINX.cstmt=q[2]; } }
      renderFinance(); return; }
    var rtab=e.target.closest('[data-rvtab]'); if(rtab){ FINX.rtab=rtab.getAttribute('data-rvtab'); renderFinance(); return; }
    var rvs=e.target.closest('[data-rv-state]'); if(rvs){ openRevState(rvs.getAttribute('data-rv-state')); return; }
    var rvm=e.target.closest('[data-rv-match]'); if(rvm){ openRevMatch(rvm.getAttribute('data-rv-match')); return; }
    var rcl=e.target.closest('[data-rv-col]'); if(rcl){ var pk=rcl.getAttribute('data-rv-col'); FINX.rcol[pk]=!FINX.rcol[pk]; renderFinance(); return; }
    var rsl=e.target.closest('[data-rv-sel]'); if(rsl){ var sid=rsl.getAttribute('data-rv-sel'); FINX.rsel[sid]=!FINX.rsel[sid]; renderFinance(); return; }
    if(e.target.closest('[data-rv-selall]')){ var av=revList('awaiting'); var on=av.length&&av.every(function(t){ return FINX.rsel[t.id]; }); av.forEach(function(t){ FINX.rsel[t.id]=!on; }); renderFinance(); return; }
    if(e.target.closest('[data-rv-invsel]')){ openRevInvoicePO(null); return; }
    var rvi=e.target.closest('[data-rv-inv]'); if(rvi){ openRevInvoice(rvi.getAttribute('data-rv-inv')); return; }
    var rvp=e.target.closest('[data-rv-pay]'); if(rvp){ openRevPay(rvp.getAttribute('data-rv-pay')); return; }
    var rvf=e.target.closest('[data-rv-follow]'); if(rvf){ openRevFollow(rvf.getAttribute('data-rv-follow')); return; }
  }
  function renderFinance(){
    if(!FINX.tab) FINX.tab='revenue';
    revPromote();
    finAutoRuns();
    var billN=REV_TX.filter(function(t){ return t.status==='eligible'||t.status==='awaiting'; }).length;
    var costN=FIN_PARTIES.filter(function(p){ return p.kind==='cost'; }).reduce(function(a,p){ return a+(finPartyBal(p).pending||0); },0);
    var secName=FINX.tab==='inbox'?'المالية — مهامي':FINX.tab==='revenue'?'الإيرادات — فوترة مركز التصفية':'التكاليف — صرف المستحقات';
    var body;
    if(FINX.tab==='inbox'){ body=finInbox(); }
    else if(FINX.tab==='revenue'){ body=finRevenue(); }
    else { var sp=FIN_PARTIES.find(function(p){ return p.id===FINX.party && p.kind==='cost'; }); if(sp){ body='<button data-fx-back="1" style="display:inline-flex;align-items:center;gap:6px;border:1px solid var(--border-2);background:var(--surface);color:var(--text-2);border-radius:8px;padding:7px 13px;font-size:12px;font-weight:700;cursor:pointer;font-family:inherit;margin-bottom:14px"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18l6-6-6-6"/></svg>رجوع لقائمة المستحقين</button>'+finCostAccount(sp); } else { body=finPartiesList(); } }
    document.getElementById('view-generic').innerHTML='<div id="finWrap"><div id="finBody" style="margin-top:6px">'+body+'</div></div>';
    setHeader(secName, crumb(['عام','المالية والفوترة',FINX.tab==='inbox'?'مهامي':(FINX.tab==='revenue'?'الإيرادات':'التكاليف')]));
    navActive('المالية والفوترة');
    document.querySelectorAll('[data-fin-nav]').forEach(function(n){ n.classList.toggle('active', n.getAttribute('data-fin-nav')===FINX.tab); });
    var rc=document.querySelector('[data-fin-count="revenue"]'); if(rc){ rc.textContent=billN||''; rc.style.display=billN?'':'none'; }
    var ib=document.querySelector('[data-fin-count="inbox"]'); if(ib){ var n=finInboxItems().length; ib.textContent=n||''; ib.style.display=n?'':'none'; }
    var cc=document.querySelector('[data-fin-count="costs"]'); if(cc){ cc.textContent=costN||''; cc.style.display=costN?'':'none'; }
    showView('generic');
    var w=document.getElementById('finWrap');
    w.addEventListener('click', finxClick);
    w.querySelectorAll('[data-fx-cf]').forEach(function(inp){ var h=function(e){ if(FINX.cclose) FINX.cclose[inp.getAttribute('data-fx-cf')]=e.target.value; }; inp.addEventListener('input',h); inp.addEventListener('change',h); });
    w.querySelectorAll('[data-fx-vf]').forEach(function(inp){ var h=function(e){ if(FINX.cvinv) FINX.cvinv[inp.getAttribute('data-fx-vf')]=e.target.value; }; inp.addEventListener('input',h); inp.addEventListener('change',h); });
    var rvq=w.querySelector('[data-rvq]'); if(rvq) rvq.addEventListener('input', function(e){ FINX.rq=e.target.value; renderFinance(); var n=document.querySelector('[data-rvq]'); if(n){ n.focus(); n.setSelectionRange(n.value.length,n.value.length); } });
    var rvc=w.querySelector('[data-rvcity]'); if(rvc) rvc.addEventListener('change', function(e){ FINX.rcity=e.target.value; renderFinance(); });
    var rvp=w.querySelector('[data-rvperiod]'); if(rvp) rvp.addEventListener('change', function(e){ FINX.rperiod=e.target.value; renderFinance(); });
    var dq=w.querySelector('[data-fx-dq]'); if(dq) dq.addEventListener('input', function(e){ FINX.dq=e.target.value; renderFinance(); var n=document.querySelector('[data-fx-dq]'); if(n){ n.focus(); n.setSelectionRange(n.value.length,n.value.length); } });
    var q=w.querySelector('[data-fx-q]'); if(q) q.addEventListener('input', function(e){ FINX.q=e.target.value; renderFinance(); var n=document.querySelector('[data-fx-q]'); if(n){ n.focus(); n.setSelectionRange(n.value.length,n.value.length); } });
    var st=w.querySelector('[data-fx-st]'); if(st) st.addEventListener('change', function(e){ FINX.st=e.target.value; renderFinance(); });
  }

  var FIN={ sel:{}, stmt:null, close:null, deferUnsel:false };
  var FIN_ST={ draft:{t:'مسودة',c:GRAY}, issued:{t:'صادر — بانتظار الصرف',c:GOLD}, closed:{t:'مدفوع',c:GREEN} };
  var FIN_READY=[
    {id:'fr1', deed:'88120044991', region:'جدة — الشرفية', po:'PO-2026-0005', office:'مكتب جدة للمساحة', net:1800, carried:false},
    {id:'fr2', deed:'10203040506', region:'مكة — العزيزية', po:'PO-2026-0011', office:'مكتب جدة للمساحة', net:1500, carried:false},
    {id:'fr3', deed:'55220148890', region:'مكة — الشوقية', po:'PO-2026-0012', office:'مكتب مكة للرفع', net:2200, carried:true}
  ];
  var FIN_STMTS=[
    {id:'fs1', ref:'FN-CS-260720-001', office:'مكتب جدة للمساحة', status:'issued', issued:'2026/07/22', lines:[{deed:'20911334455', region:'جدة — البوادي', po:'PO-2026-0009', net:1800}]},
    {id:'fs2', ref:'FN-CS-260705-002', office:'مكتب مكة للرفع', status:'closed', issued:'2026/07/06', inv:'ENG-INV-2201', trf:'TRF-330120', paidAt:'2026/07/12', lines:[{deed:'6120844770', region:'مكة — العزيزية', po:'PO-2026-0001', net:2200}]}
  ];
  function finTotal(s){ return (s.lines||[]).reduce(function(a,l){ return a+(l.amount!=null?l.amount:(l.net||0)); },0); }
  function finSelIds(){ return Object.keys(FIN.sel).filter(function(k){ return FIN.sel[k]; }); }
  function finBlankClose(){ return { inv:'', receipt:'', trf:'', paidAt:isoFromTs(taskLive()) }; }
  function finNextRef(){ return 'FN-CS-'+isoFromTs(taskLive()).slice(2).replace(/-/g,'')+'-'+String(101+FIN_STMTS.length).slice(1); }
  /* ══ [أُعيد بناؤه] شاشة فوترة الأتعاب (المكتب الهندسي) — تبويبات: تتطلب إجراءكم · جاهزة للفوترة · كشوف صادرة ══ */
  var FEE_ST={ pending_office:{t:'بانتظار إفادتكم',c:GOLD}, dispute:{t:'تحفّظ — لدى المشرف',c:RED}, final:{t:'تعديل نافذ',c:'#1f3a5f'}, ready:{t:'مستحق',c:NAVY}, carried:{t:'مرحَّل — دورة قادمة',c:'#8a5e14'}, invoiced:{t:'مفوتر',c:GREEN} };
  var ENG_OFFICE='مكتب الديار للمساحة';
  var ENGV={ id:null, no:'', date:'', file:false };
  var FEE_STATE={ tab:'runs', openFn:'' };
  var ENG_FEES=[
    {ref:'PO-2026-0005', deed:'88120044991', region:'جدة — الشرفية', office:'مكتب جدة للمساحة', list:2000, net:1800, adj:-200, adjReason:'خصم إشرافي — تعديل حدود', st:'pending_office'},
    {ref:'PO-2026-0011', deed:'10203040506', region:'مكة — العزيزية', office:'مكتب جدة للمساحة', list:1500, net:1500, adj:0, adjReason:'', st:'ready'},
    {ref:'PO-2026-0012', deed:'55220148890', region:'مكة — الشوقية', office:'مكتب مكة للرفع', list:2200, net:2200, adj:0, adjReason:'', st:'ready', objection:''}
  ];
  function renderEngFees(){
    var S=function(p){ return '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">'+p+'</svg>'; };
    var ico=function(bg,fg,svg){ return '<span class="kpi-ico" style="background:color-mix(in srgb,'+bg+' 14%,transparent);color:'+fg+'">'+svg+'</span>'; };
    var actionN=ENG_FEES.filter(function(f){ return f.st==='pending_office'||f.st==='dispute'; }).length;
    var readyN=ENG_FEES.filter(function(f){ return f.st==='ready'||f.st==='final'; }).length;
    var readyTotal=ENG_FEES.filter(function(f){ return f.st==='ready'||f.st==='final'; }).reduce(function(a,f){ return a+f.net; },0);
    var runsTotal=COST_STMTS.filter(function(s){ return s.payee===ENG_OFFICE && s.status==='awaiting_inv'; }).reduce(function(a,s){ return a+costTotal(s); },0);
    var runsN=COST_STMTS.filter(function(s){ return s.payee===ENG_OFFICE && s.status==='awaiting_inv'; }).length;
    var kpi='<div class="kpi">'+
      '<div class="first"><div class="kpi-head"><span class="kpi-ico gold">'+S('<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/>')+'</span><span>بانتظار فاتورتكم</span></div><div class="kpi-num" style="font-size:20px">'+fmtSAR(runsTotal)+'</div><div class="kpi-sub"><span class="g"></span>'+runsN+' مسير صرف</div></div>'+
      '<div><div class="kpi-head">'+ico(NAVY,NAVY,S('<path d="M20 6 9 17l-5-5"/>'))+'<span>أتعاب مستحقة</span></div><div class="kpi-num" style="font-size:20px">'+fmtSAR(readyTotal)+'</div><div class="kpi-sub">'+readyN+' بند بانتظار التجميع</div></div>'+
      '<div><div class="kpi-head">'+ico('#3f8f5f','#2f7a4d',S('<rect x="3" y="3" width="18" height="18" rx="2"/><path d="M7 8h10M7 12h6"/>'))+'<span>فواتير مُصدرة</span></div><div class="kpi-num" style="font-size:20px">'+FIN_STMTS.length+'</div><div class="kpi-sub">من برنامجكم المحاسبي</div></div>'+
      (actionN?'<div><div class="kpi-head">'+ico('#c0553d','#a5432e',S('<path d="M12 9v4"/><path d="M12 17h.01"/><path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>'))+'<span>تعديلات تسعير</span></div><div class="kpi-num" style="font-size:20px">'+actionN+'</div><div class="kpi-sub">تتطلب ردّكم</div></div>':'')+
    '</div>';
    var runs=COST_STMTS.filter(function(s){ return s.payee===ENG_OFFICE && s.status==='awaiting_inv'; });
    if(FEE_STATE.tab==='action' && !actionN) FEE_STATE.tab='runs';
    var tabs=[['runs','مسيرات بانتظار فاتورتكم',runs.length],['ready','أتعاب مستحقة',readyN],['issued','فواتير مُصدرة',FIN_STMTS.length]];
    if(actionN) tabs.push(['action','تعديلات التسعير',actionN]);
    var strip='<div style="display:flex;flex-wrap:wrap;gap:7px;margin:18px 0 14px">'+tabs.map(function(tb){ var on=FEE_STATE.tab===tb[0];
      return '<button data-fee-tab="'+tb[0]+'" style="display:inline-flex;align-items:center;gap:7px;padding:8px 15px;border-radius:99px;border:1px solid '+(on?'var(--ink)':'var(--border-2)')+';background:'+(on?'var(--ink)':'var(--surface)')+';color:'+(on?'#fff':'var(--text-2)')+';font-family:inherit;font-size:12.5px;font-weight:700;cursor:pointer">'+tb[1]+'<span style="background:'+(on?'rgba(255,255,255,.22)':'var(--surface-2)')+';color:'+(on?'#fff':'var(--text-3)')+';border-radius:99px;min-width:18px;height:18px;display:inline-grid;place-items:center;font-size:10.5px;padding:0 5px">'+tb[2]+'</span></button>'; }).join('')+'</div>';
    var body;
    if(FEE_STATE.tab==='runs'){
      body=engInfo('مسيرات صرف اعتمدتها المالية وأُرسلت إليكم. أصدروا الفاتورة الضريبية من برنامجكم المحاسبي <b>مطابقة</b> لقيمة المسير وبنوده، ثم ارفعوها هنا مع رقمها وتاريخها — القيمة مقفلة على المسير ولا تُقبل مخالفتها.')+
        runs.map(function(s){ var tot=costTotal(s); var open=ENGV.id===s.id;
          var lines='<div class="scroll" style="max-height:200px;overflow:auto;border:1px solid var(--border);border-radius:9px;margin-top:12px"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:minmax(120px,1.3fr) minmax(110px,1fr) 100px"><div class="th">رقم الصك</div><div class="th">المنطقة</div><div class="th" style="text-align:center">المبلغ</div></div><div>'+s.lines.map(function(l){ return '<div class="row" style="grid-template-columns:minmax(120px,1.3fr) minmax(110px,1fr) 100px;min-height:38px"><div class="td"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px">'+esc(l.deed)+'</span></div><div class="td" style="font-size:11.5px;color:var(--text-2)">'+esc(l.region)+'</div><div class="td" style="justify-content:center;font-weight:700;font-size:12.5px;color:var(--heading)">'+fmtSAR(l.amount)+'</div></div>'; }).join('')+'</div></div></div>';
          var ret=s.returned?'<div style="margin-top:12px;padding:10px 13px;background:color-mix(in srgb,#c0553d 6%,transparent);border:1px solid #e8c4bb;border-radius:9px;font-size:11.5px;color:#a5432e;line-height:1.7"><b>أُعيدت للتصحيح ('+esc(s.returned.at)+'):</b> '+esc(s.returned.reason)+'</div>':'';
          var form=open?'<div style="margin-top:12px;border:1px solid var(--border);border-radius:11px;background:var(--surface-2);padding:14px 15px">'+
              '<div style="display:flex;justify-content:space-between;align-items:center;background:var(--surface);border:1px solid var(--border);border-radius:9px;padding:9px 12px"><span style="font-size:11.5px;color:var(--text-2)">قيمة الفاتورة المطلوبة (مطابقة للمسير)</span><span style="font-size:14px;font-weight:800;color:var(--heading)">'+fmtSAR(tot)+'</span></div>'+
              '<div style="display:grid;grid-template-columns:1fr 1fr;gap:11px;margin-top:11px">'+
                '<div><label class="tf-lbl">رقم الفاتورة الضريبية <span style="color:#c0553d">*</span></label><input data-eng-vf="no" value="'+esc(ENGV.no)+'" dir="ltr" placeholder="من برنامجكم المحاسبي" style="'+INP_STYLE+';width:100%;margin-top:4px"></div>'+
                '<div><label class="tf-lbl">تاريخ الفاتورة <span style="color:#c0553d">*</span></label><input data-eng-vf="date" type="date" value="'+esc(ENGV.date)+'" style="'+INP_STYLE+';width:100%;margin-top:4px"></div>'+
              '</div>'+
              '<button type="button" data-eng-vfile="1" style="display:flex;align-items:center;gap:8px;width:100%;margin-top:11px;padding:10px 12px;border:1px dashed '+(ENGV.file?'#3f8f5f':'var(--border-2)')+';border-radius:9px;background:'+(ENGV.file?'color-mix(in srgb,#3f8f5f 8%,transparent)':'var(--surface)')+';cursor:pointer;font-family:inherit;font-size:11.5px;font-weight:600;color:'+(ENGV.file?'#2f7a4d':'var(--text-2)')+'"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">'+(ENGV.file?'<path d="M20 6 9 17l-5-5"/>':'<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M17 8l-5-5-5 5"/><path d="M12 3v12"/>')+'</svg><span>'+(ENGV.file?'نسخة الفاتورة مرفوعة':'رفع نسخة الفاتورة الضريبية (PDF) *')+'</span></button>'+
              '<button class="primary" data-eng-vsave="'+s.id+'" style="width:100%;justify-content:center;padding:11px;margin-top:13px">رفع الفاتورة على مسير الصرف</button>'+
            '</div>':'<button class="primary" data-eng-vopen="'+s.id+'" style="margin-top:12px;padding:9px 16px;font-size:12px">تسجيل فاتورتكم لهذا المسير</button>';
          return '<div class="card" style="padding:15px 16px;margin-bottom:12px"><div style="display:flex;align-items:flex-start;justify-content:space-between;gap:10px;flex-wrap:wrap"><div><div style="font-size:14.5px;font-weight:800;color:var(--heading)" dir="ltr">'+esc(s.ref)+'</div><div style="font-size:11.5px;color:var(--text-2);margin-top:3px">'+fmtSAR(tot)+' · '+s.lines.length+' معاملة'+(s.at?' · اعتُمد '+esc(s.at):'')+'</div></div>'+pill(CSTMT_ST.awaiting_inv.t,CSTMT_ST.awaiting_inv.c)+'</div>'+ret+lines+form+'</div>'; }).join('')+
        (runs.length?'':'<div class="card"><div class="empty"><div class="t">لا مسيرات بانتظار فاتورتكم</div></div></div>');
    } else if(FEE_STATE.tab==='issued'){
      var cols='minmax(150px,1.4fr) minmax(130px,1.1fr) minmax(95px,.8fr) minmax(168px,1fr)';
      var rows=FIN_STMTS.map(function(s){ var st=FIN_ST[s.status];
        return '<div class="row" style="grid-template-columns:'+cols+';min-height:44px"><div class="td"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px">'+esc(s.ref)+'</span></div><div class="td" style="font-size:12px;color:var(--text)">'+esc(s.office)+'</div><div class="td" style="font-weight:700;font-size:12.5px;color:var(--heading)">'+fmtSAR(finTotal(s))+'</div><div class="td">'+pill(st.t,st.c)+'</div></div>'; }).join('');
      body='<div class="card"><div class="scroll"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+cols+'"><div class="th">المرجع</div><div class="th">المكتب</div><div class="th">الإجمالي</div><div class="th">الحالة</div></div><div>'+rows+'</div></div></div></div>';
    } else {
      var list=ENG_FEES.filter(function(f){ return FEE_STATE.tab==='ready'?(f.st==='ready'||f.st==='final'):(f.st==='pending_office'||f.st==='dispute'); });
      var cols='minmax(170px,1.7fr) minmax(150px,1.2fr) minmax(150px,1.2fr)';
      var rows=list.map(function(f){ var idx=ENG_FEES.indexOf(f); var st=FEE_ST[f.st];
        var act=f.st==='pending_office'?'<div style="display:flex;gap:6px;justify-content:center"><button class="primary" data-fee-approve="'+idx+'" style="padding:7px 11px;font-size:11px">قبول التعديل</button><button data-fee-object="'+idx+'" style="padding:7px 10px;font-size:11px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-weight:700;cursor:pointer;font-family:inherit">تحفّظ</button></div>':(f.st==='dispute'?'<span style="font-size:11px;color:#a5432e;font-weight:700">بانتظار بتّ المشرف</span>':pill(st.t,st.c));
        var objBox=f.st==='pending_office'?'<div id="feeObj'+idx+'" hidden style="grid-column:1/-1;padding:10px 13px;background:var(--surface-2);border-top:1px solid var(--border)"><label class="tf-lbl">سبب التحفّظ على الخصم</label><textarea id="feeObjTxt'+idx+'" rows="2" placeholder="وضّح تحفّظكم" style="'+INP_STYLE+';width:100%;margin-top:4px;resize:vertical"></textarea><button class="primary" data-fee-objconfirm="'+idx+'" style="margin-top:8px;padding:7px 14px;font-size:11.5px">تسجيل التحفّظ</button></div>':'';
        var disBox=f.st==='dispute'?'<div style="grid-column:1/-1;padding:11px 13px;background:color-mix(in srgb,#c0553d 6%,transparent);border-top:1px solid var(--border)">'+
            '<div style="font-size:11.5px;color:var(--text-2);line-height:1.7"><b style="color:#a5432e">تحفّظ المكتب:</b> '+esc(f.objection||'')+'</div>'+
            '<div style="margin-top:10px;padding-top:10px;border-top:1px dashed var(--border-2)"><div style="font-size:11px;font-weight:800;color:var(--heading);margin-bottom:7px">قرار المشرف بعد المناقشة</div>'+
              '<div style="display:flex;gap:7px;flex-wrap:wrap"><button data-fee-final="'+idx+'" style="padding:7px 12px;font-size:11px;border:1px solid #1f3a5f;border-radius:8px;background:#1f3a5f;color:#fff;font-weight:700;cursor:pointer;font-family:inherit">تثبيت الخصم — إعادة نافذة</button>'+
              '<button data-fee-cancel="'+idx+'" style="padding:7px 12px;font-size:11px;border:1px solid var(--border-2);border-radius:8px;background:var(--surface);color:var(--text-2);font-weight:700;cursor:pointer;font-family:inherit">إلغاء الخصم</button></div></div>'+
          '</div>':'';
        var finBox=f.st==='final'?'<div style="grid-column:1/-1;padding:9px 13px;background:color-mix(in srgb,#1f3a5f 5%,transparent);border-top:1px solid var(--border);font-size:11px;color:var(--text-2);line-height:1.7"><b style="color:#1f3a5f">نافذ:</b> نوقِش الخصم مع المشرف وأُعيد مثبّتاً — لا يقبل تحفّظاً جديداً.</div>':'';
        var fee=(f.adj||f.st==='final')?'<div style="display:flex;flex-direction:column;gap:2px;align-items:center"><span style="font-weight:700;font-size:13px;color:var(--heading)">'+fmtSAR(f.net)+'</span>'+(f.adj?'<span style="font-size:9.5px;color:'+(f.st==='final'?'#1f3a5f':'#c0553d')+';font-weight:700">خصم من '+fmtSAR(f.list)+' · '+esc(f.adjReason||'')+'</span>':'')+'</div>':'<span style="font-weight:700;font-size:13px;color:var(--heading)">'+fmtSAR(f.net)+'</span>';
        return '<div class="row" style="grid-template-columns:'+cols+';min-height:48px"><div class="td"><div style="display:flex;flex-direction:column;gap:2px"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px;text-align:end">'+esc(f.deed)+'</span><span style="color:var(--text-3);font-size:11px">'+esc(f.region)+' · '+esc(f.ref)+'</span></div></div><div class="td">'+fee+'</div><div class="td">'+act+'</div>'+objBox+disBox+finBox+'</div>'; }).join('');
      body=engInfo(FEE_STATE.tab==='ready'?'أتعاب مستحقة لكم وفق التسعير المتفق عليه مسبقاً. تُجمّع لاحقاً في مسير صرف تُصدرون فاتورة مطابقة له.':'حالات استثنائية عُدّل فيها التسعير عن المتفق عليه مسبقاً — اقبلوا التعديل أو سجّلوا تحفّظاً يبتّه المشرف.')+
        '<div class="card"><div class="scroll"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+cols+'"><div class="th">المعاملة</div><div class="th">الأتعاب</div><div class="th">'+(FEE_STATE.tab==='ready'?'الحالة':'الإجراء')+'</div></div><div>'+(rows||'')+'</div></div></div></div>'+(list.length?'':'<div class="card"><div class="empty"><div class="t">لا بنود في هذا التبويب</div></div></div>');
    }
    document.getElementById('view-generic').innerHTML='<div id="feeWrap">'+kpi+strip+body+'</div>';
    setHeader('فوترة الأتعاب — المكتب الهندسي', crumb(['لوحة التحكم','فوترة الأتعاب']));
    navActive('فوترة الأتعاب'); showView('generic');
    var w=document.getElementById('feeWrap');
    w.querySelectorAll('[data-fee-tab]').forEach(function(b){ b.addEventListener('click', function(){ FEE_STATE.tab=b.getAttribute('data-fee-tab'); ENGV={id:null,no:'',date:'',file:false}; renderEngFees(); }); });
    w.querySelectorAll('[data-eng-vopen]').forEach(function(b){ b.addEventListener('click', function(){ ENGV={ id:b.getAttribute('data-eng-vopen'), no:'', date:isoFromTs(taskLive()), file:false }; renderEngFees(); }); });
    w.querySelectorAll('[data-eng-vf]').forEach(function(inp){ var h=function(e){ ENGV[inp.getAttribute('data-eng-vf')]=e.target.value; }; inp.addEventListener('input',h); inp.addEventListener('change',h); });
    w.querySelectorAll('[data-eng-vfile]').forEach(function(b){ b.addEventListener('click', function(){ ENGV.file=!ENGV.file; renderEngFees(); }); });
    w.querySelectorAll('[data-eng-vsave]').forEach(function(b){ b.addEventListener('click', function(){ engIssueInvoice(b.getAttribute('data-eng-vsave')); }); });
  }
  function engIssueInvoice(id){
    var s=COST_STMTS.find(function(x){ return x.id===id; }); if(!s) return;
    if(!(ENGV.no||'').trim()){ showToast('رقم الفاتورة مطلوب.'); return; }
    if(!ENGV.date){ showToast('تاريخ الفاتورة مطلوب.'); return; }
    if(!ENGV.file){ showToast('ارفع نسخة الفاتورة الضريبية (PDF).'); return; }
    var tot=costTotal(s);
    s.vinv={ no:ENGV.no.trim(), date:(ENGV.date||'').split('-').reverse().join('/'), amount:tot, file:'فاتورة_'+ENGV.no.trim()+'.pdf' };
    costLog(s,'رفع الفاتورة '+s.vinv.no+' على المسير',ENG_OFFICE);
    s.status='inv_received'; ENGV={id:null,no:'',date:'',file:false};
    renderEngFees(); showToast('صدرت الفاتورة '+s.vinv.no+' مطابقةً لمسير الصرف '+s.ref+' وأُرسلت للمالية للمطابقة.');
  }

  function renderInspector(){
    var p=FIN_PARTIES.find(function(x){ return x.role==='insp'||/معاين/.test(x.name); });
    var lines=p?COST_LINES.filter(function(l){ return l.payee===p.name; }):[];
    var tracked=p&&p.trackDues!==false;
    var body=finInfo('بوابة المعاين — <b>لبيان أثر المالية فقط</b>. المعاين مستحق بصفة «فرد»: لا فاتورة ولا مسير صرف، ويُصرف له بأمر صرف مباشر. وإن كانت متابعة مستحقاته معطّلة من ملف المستخدم فلا تظهر له أرقام على هذا النظام لأنها تُدار في برنامج التقييم.')+
      (p?'<div class="card" style="padding:12px 15px;margin-bottom:12px;display:flex;align-items:center;gap:12px;flex-wrap:wrap">'+
        '<span style="font-size:14.5px;font-weight:800;color:var(--heading)">'+esc(p.name)+'</span>'+
        pill(FIN_ROLE[p.role]?FIN_ROLE[p.role].t:'معاين', FIN_ROLE[p.role]?FIN_ROLE[p.role].c:NAVY)+
        pill(FIN_PTYPE[finPtype(p)].t, FIN_PTYPE[finPtype(p)].c)+
        '<span style="margin-inline-start:auto">'+pill(tracked?'متابعة المستحقات مفعّلة':'متابعة المستحقات معطّلة', tracked?GREEN:GRAY)+'</span>'+
      '</div>':'');
    if(!tracked){
      body+='<div class="card" style="padding:22px;text-align:center"><div style="font-size:13.5px;font-weight:700;color:var(--heading);margin-bottom:6px">لا مستحقات تُتابع على هذا النظام</div><div style="font-size:12px;color:var(--text-3);line-height:1.8;max-width:420px;margin:0 auto">متابعة مستحقات هذا المستخدم معطّلة من ملفه، فتُدار أتعابه في برنامج التقييم العقاري. يظهر حسابه لدى المالية هويةً فقط بلا أرقام.</div></div>';
    } else {
      var cols='minmax(150px,1.5fr) minmax(110px,.9fr) minmax(110px,.9fr) minmax(120px,1fr)';
      var rows=lines.map(function(l){ var st=COST_ST[l.st]||COST_ST.due;
        return '<div class="row" style="grid-template-columns:'+cols+';min-height:46px">'+
          '<div class="td"><div style="display:flex;flex-direction:column;gap:2px"><span dir="ltr" style="font-weight:700;color:var(--gold-d);font-size:12.5px;text-align:end">'+esc(costRefMain(l))+'</span><span style="color:var(--text-3);font-size:11px">'+esc(costRefSub(l))+'</span></div></div>'+
          '<div class="td" style="font-size:12.5px;color:var(--text-2)">'+revDate(l.at||'')+'</div>'+
          '<div class="td" style="font-weight:700;font-size:13px;color:var(--heading)">'+fmtSAR(l.amount)+'</div>'+
          '<div class="td">'+pill(st.t,st.c)+'</div>'+
        '</div>'; }).join('');
      body+='<div class="card"><div class="scroll"><div class="grid" style="min-width:100%"><div class="thead" style="grid-template-columns:'+cols+'"><div class="th">المرجع</div><div class="th">تاريخ الاستحقاق</div><div class="th">المبلغ</div><div class="th">الحالة</div></div><div>'+(rows||'')+'</div></div></div></div>'+
        (lines.length?'':finEmpty('لا بنود مستحقة'));
    }
    document.getElementById('view-generic').innerHTML='<div id="inspWrap">'+body+'</div>';
    setHeader('بوابة المعاين', crumb(['بوابات الأطراف','المعاين']));
    navActive('المعاين'); showView('generic');
  }

  /* ═══════════ تشغيل ═══════════ */
  document.querySelectorAll('[data-fin-nav]').forEach(function(n){
    n.addEventListener('click', function(){ FINX.tab=n.getAttribute('data-fin-nav'); FINX.party=null; navActive(''); renderFinance(); });
  });
  var ef=document.getElementById('navEngFees');
  if(ef) ef.addEventListener('click', function(){ renderEngFees(); });
  var ni=document.getElementById('navInspector');
  if(ni) ni.addEventListener('click', function(){ renderInspector(); });
  (function(){
    var app=document.querySelector('.app'); if(!app) return;
    if(localStorage.getItem('navCollapsed')==='1') app.classList.add('nav-collapsed');
    var b=document.getElementById('navCollapseBtn');
    if(b) b.addEventListener('click', function(){ var on=app.classList.toggle('nav-collapsed'); try{ localStorage.setItem('navCollapsed',on?'1':'0'); }catch(e){} });
  })();
  FINX.tab='inbox';
  renderFinance();
})();
</script>
</body>
</html>