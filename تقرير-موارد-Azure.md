# تقرير موارد Azure — اشتراك "Azure subscription 1"

**التاريخ:** 2026-08-13
**الحساب:** admin@buad.com.sa (بُعد الرقمية — BUAD.COM.SA)
**عدد الموارد:** 46 مورد — كلها تقريباً في منطقة UAE North (الإمارات الشمالية)

---

## نظرة عامة

الاشتراك يحتوي على **5 مشاريع/أنظمة رئيسية**، كل واحد منها عبارة عن موقع أو تطبيق ويب مع قاعدة بيانات وتخزين ملفات. إليك شرح كل نظام ووش يسوي:

---

## 1. مشتملات (Moshtmlat) — النظام الأكبر

هذا أضخم نظام في الاشتراك، ويبدو أنه نظام إدارة عقارات/مشتملات أملاك.

| المورد | النوع | الوصف |
|---|---|---|
| `moshtmlat` | App Service (تطبيق ويب) | التطبيق الرئيسي — للإنتاج |
| `moshtmlat/dev` | Slot (نسخة فرعية) | نسخة التطوير — للتجارب قبل النشر |
| `moshtmlat/test` | Slot (نسخة فرعية) | نسخة الاختبار |
| `buad` | سيرفر Azure SQL | سيرفر قواعد البيانات الرئيسي |
| `moshdb` | قاعدة بيانات SQL | **قاعدة الإنتاج الحالية** (~0.64 GB) — فيها جداول مستخدمين (AspNetUsers) وقاعات (Halls) ومواقع (Locations) وأنواع إنشاءات (ConstructionTypes) — تطبيق ASP.NET |
| `moshdb_prod_17012024` | قاعدة بيانات SQL | نسخة قديمة من الإنتاج بتاريخ 17 يناير 2024 |
| `moshdb_prod_25012024` | قاعدة بيانات SQL | نسخة قديمة من الإنتاج بتاريخ 25 يناير 2024 |
| `moshdb_Testing` | قاعدة بيانات SQL | قاعدة الاختبار |
| `moshdb-ejadah` | قاعدة بيانات SQL | قاعدة صغيرة (30 MB) مرتبطة بإجادة |
| `buad-testing` | سيرفر SQL ثاني | سيرفر اختبار وفيه قاعدة `reapms-db` (نظام PMS عقاري تجريبي على ما يبدو) |
| `moshtmlat` (تخزين) | Storage Account | **41 GB** — أكبر تخزين عندك؛ فيه مرفقات النظام: صكوك أراضي PDF، ملفات جمعيات خيرية، وملفات تطبيق ClickOnce اسمه MomraData (تطبيق ويندوز يوزع من نفس التخزين) |
| `moshtmlatBV` | Backup Vault | خزنة نسخ احتياطية داخلية في Azure |
| `moshKV` | Key Vault | خزنة مفاتيح وأسرار + شهادة توقيع IdentityServer |
| `moshtmlat` (Insights) | Application Insights | مراقبة أداء التطبيق وتنبيهات الأعطال |

**الخلاصة:** نظام ASP.NET متكامل (إنتاج + تطوير + اختبار) بقواعد بيانات متعددة وأرشيف مستندات ضخم.

---

## 2. إجادة (Ejadah) — الموقع التسويقي

| المورد | النوع | الوصف |
|---|---|---|
| `ejadah` | App Service | الموقع القديم |
| `ejadah-new-website` | App Service (Linux, PHP 8.2) | **الموقع الجديد** — PHP، على الأغلب ووردبريس، ومربوط بشهادة SSL لدومين ejadah-sa.com |
| `sql-for-website` | MySQL Flexible Server | قاعدة بيانات الموقع الجديد — **على شبكة خاصة (Private Access)** يعني ما ينوصل لها إلا من داخل شبكة Azure |
| `ejadahwebsite` | Storage Account | تخزين صغير فيه: بروشورات (brouchor)، عروض أسعار (quotation)، خلفيات (wallpaper) |
| `EjadahWebsite-webserver` | App Service Plan | خطة استضافة المواقع |

**الخلاصة:** موقع شركة تسويقي بنسختين (قديم وجديد) مع قاعدة MySQL وملفات تسويقية.

---

## 3. eed — نظام إدارة مستندات/مشروع

| المورد | النوع | الوصف |
|---|---|---|
| `eed-server` | App Service | الواجهة الخلفية (Backend) — تطبيق ASP.NET Core فيه نظام مستخدمين وصلاحيات JWT |
| `eed-web` | Static Web App | الواجهة الأمامية (Frontend) — موقع ثابت (منطقة East Asia) |
| `eed-dbserver` | PostgreSQL Flexible Server | قاعدة البيانات (PostgreSQL 16) — البيانات في قاعدة `postgres` نفسها (~4 GB) |
| `eedcontents` | Storage Account | تخزين المستندات (container اسمه documents) |

**الخلاصة:** نظام حديث بمعمارية منفصلة (واجهة أمامية + خلفية + قاعدة PostgreSQL) لإدارة مستندات.

---

## 4. معيار (Mieyar)

| المورد | النوع | الوصف |
|---|---|---|
| `mieyar-server` | App Service | التطبيق الرئيسي (UAE North) |
| `mieyar` | App Service | تطبيق ثاني قديم (Central US — Linux) |
| `mieyar` | سيرفر SQL | فيه قاعدة `meiyardb` (صغيرة جداً — شبه فاضية) |
| `mieyar` | Storage Account | فيه container `$web` يعني مستخدم كموقع ثابت |

**الخلاصة:** مشروع أصغر، يبدو في بداياته أو تجريبي — قاعدة بياناته شبه فاضية.

---

## 5. buadpms + أملج (Umluj)

| المورد | النوع | الوصف |
|---|---|---|
| `buadpms` | App Service | نظام PMS (إدارة عقارات/ممتلكات) |
| `pmscontents` | Storage Account | ملفات النظام (~5 GB) |
| `pmsvault` | Key Vault | خزنة مفاتيح للنظام |
| `umluj` | Storage Account | ~1.2 GB — فيه ملفات PDF (صكوك وأوراق رسمية لجمعيات) |
| `umlujweb` | Storage Account | موقع ثابت (container `$web`) |

**الخلاصة:** نظام إدارة ممتلكات + أرشيف مستندات خاص بمشروع أملج.

---

## موارد مساندة

- **3 مساحات عمل Log Analytics** (سجلات ومراقبة) — منها اثنتين افتراضية بمنطقة East US
- **4 قواعد تنبيهات ذكية** (Failure Anomalies) — تنبهك لو صار خلل بالتطبيقات
- **خطط استضافة (App Service Plans):** 6 خطط مختلفة موزعة على المشاريع

---

## ملاحظات مما شفته

1. **نسخ قواعد البيانات القديمة** (`moshdb_prod_17012024` و`moshdb_prod_25012024`) شغالة من يناير 2024 — قواعد حية تدفع عليها وهي مجرد نسخ قديمة.
2. **أسرار مكشوفة:** إعدادات تطبيق `eed-server` فيها كلمة سر قاعدة البيانات ومفتاح التخزين ومفتاح JWT وكلمة سر الأدمن كلها **نص واضح** — مع إن عندك Key Vaults جاهزة (`moshKV` و`pmsvault`) الأفضل نقلها لها.
3. **قاعدة MySQL** الوحيدة اللي على شبكة خاصة هي `sql-for-website` — حماية زينة، لكن تخلي النسخ الاحتياطي الخارجي أصعب.
4. **توزيع جغرافي متناثر:** أغلب الموارد UAE North، لكن فيه موارد بـ East US وCentral US وEast Asia — الأفضل توحيدها لتقليل التكلفة والتأخير.

---

*هذا التقرير يصف ما شوهد في الاشتراك بتاريخ 2026-08-13 أثناء عملية النسخ الاحتياطي إلى `D:\AzureBackup_2026-08-13`.*
