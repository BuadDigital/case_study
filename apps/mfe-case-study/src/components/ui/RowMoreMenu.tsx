"use client";

/** Re-export from ui-kit — keep import paths stable for case-study / keys / evaluator. */
export {
  RowMoreMenu,
  RowMoreMenuIcons,
  type RowMoreMenuItem,
} from "@platform/ui-kit";
