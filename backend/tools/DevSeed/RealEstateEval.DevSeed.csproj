<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>RealEstateEval.Infrastructure.Data</RootNamespace>
    <AssemblyName>RealEstateEval.DevSeed</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <!-- Leaf project (A8): may reference the global Infrastructure and, as contexts move
         physically, each context Infrastructure — nothing references this project except
         the Case Study dev host and the container tests. -->
    <ProjectReference Include="..\..\RealEstateEval.Infrastructure\RealEstateEval.Infrastructure.csproj" />
  </ItemGroup>
</Project>
