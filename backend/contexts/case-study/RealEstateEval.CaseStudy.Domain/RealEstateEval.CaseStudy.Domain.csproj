<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>RealEstateEval.CaseStudy.Domain</RootNamespace>
  </PropertyGroup>

  <!-- PropertyGroup members and the ops-metrics rules reference the shared WorkOrder /
       WorkflowTask entities, which stay in the global Domain until the wire DTOs and
       cross-context reads are de-typed (see the A8 defer list). -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\shared\RealEstateEval.Shared.Contracts\RealEstateEval.Shared.Contracts.csproj" />
  </ItemGroup>

</Project>
