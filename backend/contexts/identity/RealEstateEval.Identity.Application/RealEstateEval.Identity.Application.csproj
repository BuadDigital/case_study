<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>RealEstateEval.Identity.Application</RootNamespace>
  </PropertyGroup>

  <!-- No Identity.Domain project: the ASP.NET Identity entities are pinned to global
       Infrastructure by IdentityDbContext and the Identity EF package. -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\RealEstateEval.Application\RealEstateEval.Application.csproj" />
  </ItemGroup>

</Project>
