<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>RealEstateEval.Platform.Domain</RootNamespace>
  </PropertyGroup>

  <!-- DifferenceFactorCatalog.Seed() keys its factors off the shared
       MarketAdjustmentFactorKeys, which stays in the global Domain for Valuation. -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\shared\RealEstateEval.Shared.Contracts\RealEstateEval.Shared.Contracts.csproj" />
  </ItemGroup>

</Project>
